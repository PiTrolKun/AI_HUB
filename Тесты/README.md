# Тесты — AI_HUB

Будущая папка для тестов проекта.

Когда появится код, проверки должны соответствовать затронутым частям проекта. Для C# / .NET базовый ориентир:

- `dotnet restore`;
- `dotnet build`;
- `dotnet test`, если тесты уже есть.
