# CONTEXTHUB — AI_HUB

Дата старта контекста: 2026-05-30

## Назначение

Этот файл — главный живой контекст проекта AI_HUB. Его нужно обновлять после значимых решений, крупных задач и изменений архитектуры.

## Текущая идея проекта

AI_HUB — русскоязычный локальный AI-хаб для Windows 11 в стиле проводника Windows.

Программа должна помогать пользователю находить, скачивать, запускать, хранить и использовать локальные бесплатные ИИ-модели без терминала, гайдов и ручной настройки.

## Главный пользовательский принцип

Пользователь не программист. Кодингом, настройкой структуры, проверками и техническими решениями занимается Codex.

## Текущий технический ориентир

- Платформа: Windows 11.
- Стек: C# / .NET / WPF.
- Локальные данные: SQLite в будущих этапах.
- Визуальная философия: Windows-проводник для локального ИИ.

## Ближайший MVP

Первый MVP: визуал + скачивание моделей.

В MVP должны войти:

- WPF-приложение в стиле проводника.
- Каталог моделей.
- Установленные модели.
- Скачивание моделей.
- AI-хранилище.
- Выбор дисков и лимитов.
- Статусы моделей.
- Ручное закрепление и удаление.
- Базовый паспорт ПК.

## Подтверждённые решения

- Проект начинается не с голосового ассистента, а с фундамента: визуал, каталог, скачивание, хранение.
- Основной будущий локальный интеллект: Qwen3 32B.
- Интернет для модели — инструмент хаба, а не свободный прямой доступ модели.
- Backends должны быть встроены или устанавливаться автоматически.
- Результаты пользователя нельзя удалять без прямого подтверждения.
- Закреплённые модели защищены от автоудаления.
- Перед крупной новой реализацией создаётся ТЗ в папке `ТЗ`.

## Текущее состояние

- Текущая версия проекта: `0.0.11-dev`.
- Есть итоговая концепция: `local_ai_hub_final_summary_ru.md`.
- Есть инструкции Codex: `Инструкции/AGENTS.md` и `Инструкции/CODEX.md`.
- Создаётся стартовый скелет проекта: документация, папки, runtime-зоны и заготовки для будущих модулей.
- Подтверждён стек `C# / .NET / WPF` для первого приложения.
- Установлен Microsoft .NET SDK 10.0.300.
- Проверено: WPF-шаблон доступен, временный WPF-проект на `net10.0-windows` собирается без ошибок и предупреждений.
- Создан GitHub-репозиторий: `https://github.com/PiTrolKun/AI_HUB`.
- Установлен GitHub CLI 2.93.0 для будущей авторизации и публикации.
- Локальный Git-репозиторий инициализирован на ветке `main`.
- Первый коммит `9a5e76c Initial project skeleton` отправлен в `origin/main`.
- GitHub сообщает, что репозиторий `PiTrolKun/AI_HUB` приватный, default branch: `main`.
- Добавлено правило строгой нумерации версий.
- Добавлено правило публикации актуального состояния проекта в GitHub при переносе ТЗ в архив.
- ТЗ подготовки .NET/WPF окружения перенесено в архив; по прямому указанию пользователя повторный push не выполнялся.
- Добавляется правило: после каждого выполненного действия делать запись в истории проекта.
- Начата задача первого визуального прототипа: стартовое WPF-окно мастера первого запуска.
- Создан WPF-проект `Исходники/AIHub`.
- Реализован первый прототип стартового окна AI_HUB: мастер первого запуска с тремя шагами настройки.
- Проверки прототипа: `dotnet restore`, `dotnet build`, scanner кириллицы. Сборка прошла без ошибок и предупреждений.
- Приложение запущено для визуальной оценки пользователем.
- В стартовое окно добавлена кнопка переключения светлой и тёмной темы в правом верхнем углу.
- Проверки темы: `dotnet build`, scanner кириллицы. Сборка прошла без ошибок и предупреждений.
- Проверено аварийное закрытие стартового окна: Windows Event Log показал `XamlParseException` из-за ранней ссылки на ресурс `WindowBackgroundBrush`.
- Исправлен запуск окна: начальный фон окна задан напрямую, ресурсы темы переведены на `DynamicResource`, а переключение темы заменяет кисти ресурсов.
- Добавлены `Запустить_AI_HUB.cmd` и `start-aihub.ps1` для запуска dev-версии двойным кликом. Скрипт выполняет `dotnet build` и запускает текущий `AIHub.exe`.
- Smoke-проверка запуска после исправления: процесс AI_HUB остался активен после старта, немедленного падения нет.
- Проверен клик по кнопке `Тёмная тема` через Windows UI Automation: процесс остался активен, новых событий падения не найдено.
- ТЗ стартового WPF-окна закрыто по подтверждению пользователя и перенесено в `ТЗ/Архив/2026-05-30_прототип_стартового_окна_wpf.md`.
- По прямому указанию пользователя GitHub push при этом архивировании не выполнялся.
- По решению пользователя первая официальная dev-версия заготовки зафиксирована как `0.0.1-dev`.
- Версия записана в `VERSION` и в свойства WPF-проекта `Исходники/AIHub/AIHub.csproj`.
- Исправлен запускатор `start-aihub.ps1`: он больше не ищет `AIHub.csproj` рекурсивно и не может случайно собрать backup-копию проекта из `Backups`.
- В нижней строке стартового окна добавлен автоматический вывод версии. Окно читает версию из `AssemblyInformationalVersion`, которая задаётся в `Исходники/AIHub/AIHub.csproj`.
- В правила добавлено требование держать одну истину о версии программы: будущие UI-элементы, установщик, updater, release notes и модули должны читать версию из основного источника, а не хранить отдельные ручные копии.
- По уточнению пользователя версия перенесена из нижней статусной строки в системный заголовок окна. Заголовок теперь формируется автоматически как `AI HUB 0.0.1-dev`, без подчёркивания в названии.
- По подтверждению пользователя выполнено закрытие состояния по правилам: активных ТЗ вне архива нет, актуальная локальная история готовится к публикации в GitHub.
- Актуальное состояние проекта опубликовано в GitHub: `main` отправлен в `origin/main`, коммит `0281b34 Record project state before GitHub publish`.
- Запись об успешной публикации также отправлена в GitHub коммитом `19f046c Record successful GitHub publish`.
- Добавлен первый рабочий логотип приложения. Пользовательский PNG из `Данные_для_внедрения/Фото` скопирован в `Исходники/AIHub/Assets`, из него создан `AppIcon.ico`.
- `AppIcon.ico` подключён как `ApplicationIcon` WPF-проекта и как `Icon` стартового окна. Этот же файл зафиксирован как будущий источник значка exe, рабочего стола и установщика.
- Версия повышена с `0.0.1-dev` до `0.0.2-dev`, потому что добавление логотипа меняет приложение и exe.
- Проверки логотипа: `dotnet build`, `Запустить_AI_HUB.cmd`, scanner кириллицы; заголовок окна показывает `AI HUB 0.0.2-dev`, связанный exe-значок извлекается из `AIHub.exe`.
- Верхняя панель стартового окна очищена: надпись `Первый запуск` удалена, переключатель темы прижат к правому краю и заменён на кнопку-иконку. В светлой теме показана луна с подсказкой переключения на тёмную тему, в тёмной теме показано солнце с подсказкой переключения на светлую тему.
- Версия повышена с `0.0.2-dev` до `0.0.3-dev`, потому что изменён UI стартового окна.
- Значок солнца на кнопке темы сделан цветным: в тёмной теме он отображается тёпло-жёлтым цветом `#FBBF24`.
- Версия повышена с `0.0.3-dev` до `0.0.4-dev`, потому что изменён UI стартового окна.
- Создано ТЗ `ТЗ/2026-05-30_системная_темная_рамка_windows.md` для синхронизации системного заголовка окна с темой Windows.
- Стартовое окно теперь при запуске читает настройку темы Windows для приложений и применяет её как начальную тему AI HUB.
- Системный заголовок окна синхронизируется с темой приложения через Windows DWM API без кастомной рамки.
- Версия повышена с `0.0.4-dev` до `0.0.5-dev`, потому что изменено поведение окна при запуске и переключении темы.
- Пользователь подтвердил выполнение ТЗ системной тёмной рамки Windows.
- ТЗ перенесено в `ТЗ/Архив/2026-05-30_системная_темная_рамка_windows.md`; по правилу архивации актуальное состояние проекта публикуется в GitHub.
- Создано новое рабочее ТЗ `ТЗ/2026-05-30_логика_стартового_окна_и_паспорт_пк.md`.
- В ТЗ зафиксировано решение: кнопки `Начать настройку` и `Перенастроить` ведут в один сценарий настройки, текущие кнопки нужно оживить, а пока они должны открывать только заготовку страницы настройки внутри основного окна.
- В ТЗ также зафиксировано: паспорт компьютера создаётся автоматически при первом запуске и пересоздаётся при `Перенастроить`.
- Реализована первая логика стартового окна: `Позже` заменена на `Перенастроить`, обе кнопки открывают страницу настройки внутри главного окна, а страница настройки имеет рабочую кнопку `Назад`.
- Добавлено хранение состояния в `%LOCALAPPDATA%/AI_HUB/state.json` и паспорта компьютера в `%LOCALAPPDATA%/AI_HUB/computer-passport.json`.
- Паспорт компьютера создаётся при старте приложения и пересоздаётся при `Перенастроить`.
- Версия повышена с `0.0.5-dev` до `0.0.6-dev`.
- Пункт `Паспорт компьютера` на главной странице теперь после сканирования показывает сводку: CPU, RAM, GPU, VRAM и диски.
- Паспорт ПК расширен списком GPU и объёмом VRAM, если эти данные доступны через стандартный реестр Windows.
- Старый текст `Посмотрим RAM, GPU, VRAM и свободное место.` оставлен в XAML как стартовый текст до завершения сканирования.
- Версия повышена с `0.0.6-dev` до `0.0.7-dev`.
- Мини-правка перед закрытием ТЗ: кнопка `Перенастроить` расширена, чтобы текст не зажимался в рамке.
- Версия повышена с `0.0.7-dev` до `0.0.8-dev`.
- Пользователь подтвердил, что после мини-правки ТЗ логики стартового окна и паспорта ПК можно считать выполненным.
- ТЗ перенесено в `ТЗ/Архив/2026-05-30_логика_стартового_окна_и_паспорт_пк.md`; по правилу архивации актуальное состояние проекта публикуется в GitHub.
- Создано новое рабочее ТЗ `ТЗ/2026-05-31_настройки_хранилищ_моделей_и_результатов.md`.
- В ТЗ зафиксировано: нужно реализовать настройки `Хранилище моделей` и `Папка результатов` с несколькими адресами хранения, ручным вводом пути, выбором папки через Windows, лимитами в ГБ, временным превышением в ГБ, порядком/приоритетом адресов и отображением выбранного на главной странице.
- Настройки хранения должны сохраняться отдельно в `%LOCALAPPDATA%/AI_HUB/storage-settings.json`.
- Реализованы настройки `Хранилище моделей` и `Папка результатов` на странице настройки внутри основного окна.
- Для каждой категории добавлены ручной ввод пути, стандартный выбор папки Windows, добавление/удаление адресов, изменение порядка `Вверх`/`Вниз`, лимит адреса, общий лимит, переключатель временного превышения и размер превышения в ГБ.
- Сводки пунктов 1 и 2 на главной странице обновляются после сохранения настроек и показывают адрес по умолчанию, дополнительные адреса, общий лимит и временное превышение.
- Настройки сохраняются в `%LOCALAPPDATA%/AI_HUB/storage-settings.json`; тестовые значения после smoke-проверки очищены до пустой структуры, чтобы не оставлять пользовательскую среду с проверочными путями.
- Версия повышена с `0.0.8-dev` до `0.0.9-dev`.
- По замечанию пользователя добавлены видимые подписи `Адрес хранения` и `Лимит адреса, ГБ` над полями ввода адреса и лимита адреса; теперь не нужно догадываться по tooltip.
- Зафиксировано правило лимитов: общий лимит категории и лимиты отдельных адресов могут отличаться, а в будущей реальной логике будет работать более строгая фактическая граница.
- Версия повышена с `0.0.9-dev` до `0.0.10-dev`.
- По замечанию пользователя добавлена логика завершения настройки: после сохранения хотя бы одного адреса для моделей и одного адреса для результатов главная кнопка меняется с `Начать настройку` на `Начать работу`.
- Пока рабочий режим не реализован, нажатие `Начать работу` показывает статус-заглушку.
- Версия повышена с `0.0.10-dev` до `0.0.11-dev`.
- По замечанию пользователя исправлен разбег между кнопкой `Начать работу` и нижней статусной строкой: теперь при завершённой настройке статус показывает, что настройка завершена, а при возврате со страницы настройки берёт актуальное состояние.
- Версия повышена с `0.0.11-dev` до `0.0.12-dev`.
- Для будущих тестовых установщиков создана папка `Тесты/Установщики`.
- Пользователь подтвердил выбранный вариант установщика: Inno Setup. Будущий запуск сборки должен быть доступен простым кликом через `.cmd`-обводку над PowerShell-скриптом.
- Создано рабочее ТЗ `ТЗ/2026-05-31_тестовый_установщик_inno_setup.md`.
- Добавлен первый установочный контур: `Собрать_установщик_AI_HUB.cmd`, `Инструменты/build-installer.ps1` и `Инструменты/Installer/AI_HUB.iss`.
- Inno Setup 6.7.3 установлен через `winget` по прямому подтверждению пользователя и добавлен в `REESTR.md`.
- Скрипт выполняет self-contained `dotnet publish win-x64` и собирает тестовый установщик в `Тесты/Установщики`.
- Проверено, что `.cmd`-обводка запускается через `cmd.exe`, выполняет publish, собирает Inno Setup установщик и оставляет окно на `pause`.
- Сформирован тестовый установщик `Тесты/Установщики/AI_HUB_Setup_0.0.13-dev.exe`.
- Версия повышена с `0.0.12-dev` до `0.0.13-dev`.
- В расширенные правила добавлена релизная пометка: текущие dev/test-установщики могут ставиться в `%LOCALAPPDATA%\Programs`, но перед публичным релизом нужно отдельно пересмотреть установку в `C:\Program Files\AI HUB`, режим `для всех пользователей`, права администратора, uninstall и миграцию настроек.
- Пользователь подтвердил, что все активные ТЗ решены, включая забытое.
- ТЗ `2026-05-31_настройки_хранилищ_моделей_и_результатов.md` перенесено в `ТЗ/Архив`.
- ТЗ `2026-05-31_тестовый_установщик_inno_setup.md` перенесено в `ТЗ/Архив`.
- По правилу архивации ТЗ готовится публикация актуального состояния проекта в GitHub.
- Актуальное состояние проекта опубликовано в GitHub: `main` отправлен в `origin/main`, коммит `b19c002 Archive tasks and add installer tooling`.
- Создано новое рабочее ТЗ `ТЗ/2026-05-31_локализация_и_общие_настройки.md`.
- Заложена простая JSON-локализация интерфейса: `ru.json` и `en.json`.
- Добавлены настройки приложения `settings.json` в `%LOCALAPPDATA%\AI_HUB`.
- Добавлена папка пользовательских переводов `%LOCALAPPDATA%\AI_HUB\Localization`.
- В верхнюю панель добавлена кнопка настроек с иконкой шестерёнки.
- Внутри основного окна добавлена страница общих настроек; пока реализован только выбор языка интерфейса.
- Программа умеет применять русский и английский интерфейс, сохранять выбор языка и при неполном пользовательском переводе брать недостающие строки из русского fallback.
- В `AGENTS.md` и `CODEX.md` добавлено правило: перед закрытием ТЗ и публикацией в GitHub проверять и пополнять английскую базу локализации, если появились новые user-facing строки.
- Версия повышена с `0.0.13-dev` до `0.0.14-dev`.
- Пользователь подтвердил, что ТЗ локализации и общих настроек выполнено.
- ТЗ `2026-05-31_локализация_и_общие_настройки.md` перенесено в `ТЗ/Архив`; перед публикацией проверяется сборка, scanner кириллицы, совпадение ключей `ru.json`/`en.json` и отсутствие активных ТЗ вне архива.
- Актуальное состояние проекта с локализацией и общими настройками опубликовано в GitHub: `main` отправлен в `origin/main`, коммит `b28fcd2 Archive localization settings task`.
- Создано рабочее ТЗ `ТЗ/2026-05-31_страница_начала_работы.md`.
- Реализована новая страница `Начало работы` внутри основного окна: кнопка `Начать работу` открывает её после завершённой настройки.
- На странице пока доступен один режим `Рассуждение / изучение`; кнопка выбора режима показывает статус-заглушку до будущего подключения реального AI-режима.
- Внизу страницы добавлен изначально свернутый блок `Ранее начатое` с примером будущей записи и неактивной кнопкой `Продолжить`.
- Новые строки добавлены в `ru.json` и `en.json`, версия повышена с `0.0.14-dev` до `0.0.15-dev`.
- Пользователь подтвердил, что ТЗ страницы начала работы выполнено.
- ТЗ `2026-05-31_страница_начала_работы.md` перенесено в `ТЗ/Архив`; перед публикацией проверяется сборка, scanner кириллицы, совпадение ключей `ru.json`/`en.json`, отсутствие активных ТЗ вне архива и сборка тестового установщика.
- По замечанию пользователя добавлено правило в `AGENTS.md` и `CODEX.md`: тестовый установщик не собирать автоматически после каждого ТЗ; собирать только по прямой команде пользователя, кроме случаев, когда проверка установленной версии критична для задачи.
- Актуальное состояние проекта со страницей начала работы опубликовано в GitHub: `main` отправлен в `origin/main`, коммит `8dc7c64 Archive work start page task`.
- После дисконнекта выполнена контрольная проверка: рабочее дерево чистое, `main` синхронизирован с `origin/main`, последний коммит `884fc73 Record work start task publish` уже опубликован. Добавляется отдельная контрольная запись, чтобы факт завершения был виден в конце истории.
- Уточнена концепция основного ИИ-ядра: вместо `Qwen3 32B` базовым ядром проекта выбран `Qwen3 8B Q4_K_M`, чтобы оно комфортно работало на среднем игровом ПК уровня i5 / RTX 3060 / 16 ГБ RAM.
- Создано рабочее ТЗ `ТЗ/2026-05-31_менеджер_основного_ядра_qwen3_8b.md`.
- В ТЗ зафиксировано: модель не включается в установщик, скачивается через AI HUB после подтверждения пользователя, источник `Qwen/Qwen3-8B-GGUF`, файл `Qwen3-8B-Q4_K_M.gguf`, размер около `5.03 ГБ`, лицензия `apache-2.0`.
- Реализован менеджер основного ядра `Qwen3 8B Q4_K_M`: проверка выбранной папки моделей при запуске и после сохранения настроек, prompt `Скачать / Открыть настройки / Позже`, нижняя панель прогресса, resume через `.part`, manifest `core-model.json`, проверка размера и SHA-256.
- Версия повышена с `0.0.15-dev` до `0.0.16-dev`.
- По прямому подтверждению пользователя модель скачана в `H:\AI_HUB\Данные_для_внедрения\Модели\Core\Qwen3-8B\Qwen3-8B-Q4_K_M.gguf`.
- Проверены размер модели `5027783488` байт и SHA-256 `d98cdcbd03e17ce47681435b5150e34c1417f50b5c0019dd560e4882c5745785`; рядом создан manifest со статусом `installed`.
- `REESTR.md` и `THIRD_PARTY_NOTICES.md` обновлены под модель Qwen3 8B GGUF. Модель не входит в установщик и не должна публиковаться в GitHub.
- В `.gitignore` добавлено правило `Данные_для_внедрения/Модели/**`, чтобы локально скачанные GGUF-файлы не попали в репозиторий.
- По дополнению к текущему ТЗ добавлено debug-окно проверки моделей: отдельное окно `Debug Model Tester` открывается по `F12`, показывает все скачанные `.gguf` из папок моделей, содержит минимальный чат, логи, выбор модели и кнопки `Отправить`, `Стоп`, `Очистить чат`, `Очистить лог`.
- Добавлен первый локальный runtime-backend `llama.cpp b9442` для Windows CUDA 12.4 x64. Runtime лежит локально в `Runtime/Backends/llama.cpp/b9442/win-cuda-12.4-x64` и не публикуется в GitHub.
- Из-за нестабильности/блокировки `llama-server.exe` первая интеграция использует `llama-cli.exe` как дочерний процесс на один запрос. Модель не получает доступ к файлам, интернету, shell, инструментам и настройкам Windows.
- Пользователь вручную проверил debug-окно: F12 открывает окно, Qwen3 8B видна в списке, prompt отправляется, ответ приходит, логи отображаются.
- Codex выполнил короткий живой тест модели через `llama-cli.exe`: exit code `0`, ответ `Модель работает.`
- Версия повышена с `0.0.16-dev` до `0.0.17-dev`.
- Проверки после debug-окна: `dotnet build` успешно без предупреждений, scanner кириллицы успешно, ключи `ru.json` и `en.json` совпадают `165/165`.
- Установщик не собирался по правилу проекта: сборка установщика выполняется только по прямой команде пользователя, если тест установленной версии не критичен.
- Мини-правка по замечанию пользователя: окно `Debug Model Tester` теперь получает светлую/тёмную тему основного окна, а выбор модели, выпадающий список, поля ввода и списки имеют явные читаемые цвета для обеих тем.
- Версия повышена с `0.0.17-dev` до `0.0.18-dev`.
- Проверки после мини-правки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно, ключи `ru.json` и `en.json` совпадают `165/165`.
- Установщик не собирался по правилу проекта.

## Открытые вопросы

- Решить отдельным ТЗ, нужен ли fallback backend CPU/Vulkan для машин без подходящей NVIDIA/CUDA.
- Точный стартовый каталог моделей.
- Формат локальной базы и миграций.
- Проверка установки AI HUB на другом ПК.

## 2026-06-01 — контекст ядра и журнал сессий

- Создано рабочее ТЗ `ТЗ/2026-06-01_контекст_ядра_и_журнал_сессий.md`.
- Зафиксировано решение: дата/время берутся локально из Windows перед запросом к модели; примерное местоположение определяется по IP и сохраняется в `user-profile.json`; будущий ручной ввод местоположения будет приоритетнее IP.
- Fallback из Windows-региона не используется.
- Добавлены сервисы профиля пользователя, IP-геолокации, скрытого runtime-контекста ядра, выбора папок сессий и JSONL-журналирования.
- 1 запуск программы создаёт 1 core-session файл `jsonl`; при штатном закрытии пишется `session_end`.
- Если настроена папка результатов, файлы сессии пишутся в первый адрес результатов: `<Папка результатов>\AI_HUB\Core\Sessions`.
- Debug-окно `F12` получило отдельную зону `<Папка результатов>\AI_HUB\Debug\ModelTester\Sessions` и пишет туда `debug_session_start`, `context_snapshot`, debug-логи, будущие user/model сообщения.
- `llama-server` и `llama-cli` fallback получают скрытый служебный system/context prompt с датой, временем, часовым поясом и примерным местоположением, если оно известно.
- В `.gitignore` добавлены правила, чтобы пользовательские папки `AI_HUB\Core\Sessions`, `AI_HUB\Debug\ModelTester`, `AI_HUB\Tasks` и `AI_HUB\Tools` не попали в GitHub, даже если папка результатов выбрана внутри репозитория.
- `REESTR.md` и `THIRD_PARTY_NOTICES.md` обновлены под внешний IP-сервис `ipwho.is`.
- Версия повышена с `0.0.22-dev` до `0.0.23-dev`.
- Установщик не собирался по правилу проекта.
- Пользователь подтвердил, что ТЗ выполнено.
- ТЗ `2026-06-01_контекст_ядра_и_журнал_сессий.md` перенесено в `ТЗ/Архив`.
- Финальные проверки перед публикацией: `dotnet build`, scanner кириллицы, сверка локализаций, JSONL-проверка core/debug-сессий, контроль ignore-правил для пользовательских сессий.
- После проверок актуальное состояние публикуется в GitHub по правилу закрытия ТЗ.

## 2026-06-01 — закрытие ТЗ основного ядра Qwen3 8B

- Пользователь подтвердил, что ТЗ `2026-05-31_менеджер_основного_ядра_qwen3_8b.md` выполнено.
- ТЗ перенесено в `ТЗ/Архив/2026-05-31_менеджер_основного_ядра_qwen3_8b.md`.
- Итоговая версия проекта: `0.0.20-dev`.
- В состав задачи вошли: менеджер скачивания/проверки Qwen3 8B Q4_K_M, manifest модели, проверка модели при запуске и после настройки, prompt скачивания, нижняя панель прогресса, debug-окно `F12`, локальный backend `llama.cpp b9442` через `llama-cli.exe`, живой тест модели, исправление crash по `F12` и читаемый выбор модели в debug-окне.
- Модель и runtime backend остаются локальными и не публикуются в GitHub: `Данные_для_внедрения/Модели/**` и `Runtime/**` игнорируются.
- Установщик не собирался по правилу проекта.
- По правилу архивации после проверок актуальное состояние проекта публикуется в GitHub.

## 2026-06-01 — диагностика llama-server

- Создано рабочее ТЗ `ТЗ/2026-06-01_диагностика_llama_server_backend.md`.
- Найдена точная причина первого сбоя `llama-server.exe`: в основной runtime-папке отсутствовал `llama-server-impl.dll`, из-за чего Windows возвращала `0xC0000135` (`STATUS_DLL_NOT_FOUND`).
- Официальный архив `llama-b9442-bin-win-cuda-12.4-x64.zip` содержит `llama-server-impl.dll`, значит текущая runtime-папка была неполной.
- Создана чистая диагностическая папка `Runtime\Backends\llama.cpp\b9442\server-diagnostic-win-cuda-12.4-x64`, туда заново распакованы официальный CUDA 12.4 backend и CUDA DLL archive.
- `llama-server.exe --version` в чистой папке завершился успешно.
- Недостающий `llama-server-impl.dll` скопирован в основную папку `Runtime\Backends\llama.cpp\b9442\win-cuda-12.4-x64`.
- Основной `llama-server.exe` теперь стартует с Qwen3 8B, `/health` возвращает `ok`, `/v1/chat/completions` вернул ответ `Основной сервер работает.`
- Для Qwen3 server-запуска нужен параметр `--reasoning off`; без него OpenAI-compatible `message.content` может быть пустым из-за thinking-режима.
- `llama-cli.exe` fallback не тронут и остаётся рабочим.
- Следующий шаг отдельным ТЗ: интегрировать server-backend в AI HUB как управляемый локальный сервис с health-check, остановкой и fallback на `llama-cli`.
- По решению пользователя интеграция server-backend добавлена вторым шагом в текущее ТЗ.
- Добавлен `LlamaServerRuntimeService`: он запускает `llama-server.exe` скрытым дочерним процессом, выбирает свободный loopback-порт, ждёт `/health`, отправляет chat-запросы в `/v1/chat/completions` и останавливает server при закрытии debug-окна.
- Debug-окно теперь предпочитает `llama-server`, а при ошибке переключается на `llama-cli` fallback.
- Кнопка `Стоп` отменяет текущий запрос и останавливает server-процесс.
- Версия повышена с `0.0.20-dev` до `0.0.21-dev`.
- Проверки шага 2: `dotnet build` успешно без предупреждений, scanner кириллицы успешно, ключи `ru.json`/`en.json` совпадают `173/173`, `F12` smoke-test прошёл, server endpoint вернул ответ `Сервер бэкенда работает корректно.`
- Установщик не собирался по правилу проекта.
- Мини-правка debug-окна: в чате и логах отключён горизонтальный scrollbar, строки теперь переносятся по словам через wrapping-шаблон элементов списка.
- Версия повышена с `0.0.21-dev` до `0.0.22-dev`.
- Проверки после мини-правки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно.
- Пользователь подтвердил, что ТЗ диагностики и интеграции `llama-server` выполнено.
- ТЗ `2026-06-01_диагностика_llama_server_backend.md` перенесено в `ТЗ/Архив`.
- Итоговая версия задачи: `0.0.22-dev`.
- В состав закрытого ТЗ вошли: восстановление неполной runtime-папки `llama.cpp b9442`, проверка `llama-server.exe`, интеграция server-backend в debug-окно, fallback на `llama-cli.exe`, остановка server-процесса при `Стоп`/закрытии окна, исправление дублирования prompt-а и перенос строк в чате/логах debug-окна.
- Runtime backend и скачанная GGUF-модель остаются локальными и не публикуются в GitHub.
- Установщик не собирался по правилу проекта.
- Финальные проверки закрытия прошли: `dotnet build`, scanner кириллицы, сверка ключей локализации `173/173`, контроль активных ТЗ, `F12` smoke-test и endpoint-test `llama-server`.
- После финальных проверок актуальное состояние проекта публикуется в GitHub.

## 2026-06-01 — интернет-инструменты ядра

- Создано рабочее ТЗ `ТЗ/2026-06-01_интернет_инструменты_ядра.md`.
- Реализован первый слой `Tool Gateway`: команды `web_search:`, `web_read:` и `web_download:` вызывают инструменты AI HUB, а не дают модели прямой сетевой доступ.
- `WebSearchTool` использует dev-провайдер DuckDuckGo Lite без API-ключа и сохраняет JSON-результат в `<Папка результатов>\AI_HUB\Tools\Web\Search`.
- `WebPageReaderTool` читает web-страницу, извлекает заголовок/текст и сохраняет JSON в `Tools\Web\Pages`.
- `WebDownloadTool` скачивает файл по прямой ссылке, сохраняет его в `Tools\Web\Downloads`, считает размер и SHA-256, не запускает файл.
- В debug-окно `F12` добавлен сценарий `core_tool_test:`: ядро должно само попросить tool-команду, получить результат инструмента и продолжить шаги.
- Проверка подтвердила: Qwen3 8B попросила `web_download:` для прямой ссылки Hugging Face, после чего AI HUB скачал тестовую модель `Qwen3-0.6B-Q4_K_M.gguf`.
- Для снятия путаницы по скачанным файлам: ранние файлы `03:39` и `03:43` созданы ручными smoke-test командами Codex, файл `03:47` создан через запрос ядра в `core_tool_test`.
- По замечанию пользователя добавлен визуальный статус скачивания в debug-окне: прогресс, скачанный объём, общий объём и скорость.
- По уточнению пользователя F12 debug-режим переведён в режим всех доступных возможностей ядра: обычный prompt теперь тоже проходит через инструментальный контур, где модель отвечает `FINAL:` или просит `web_search`, `web_read`, `web_download`.
- Smoke-test подтвердил, что обычная просьба скачать файл по прямой ссылке вызвала `web_download` и сохранила `download.bin` в `Тесты\1\AI_HUB\Tools\Web\Downloads`.
- Пользователь проверил запрос `Найди и скачать фото любого котика`: ядро скачало HTML-страницу Яндекс.Картинок без расширения вместо изображения.
- Исправлено скачивание по типу ответа: `WebDownloadTool` добавляет расширение по `Content-Type`, возвращает `Content-Kind`, предупреждает об HTML-странице и больше не заставляет ядро завершать сценарий после любого `web_download`.
- Smoke-test на URL Яндекс.Картинок теперь сохраняет `search.html` и возвращает предупреждение `Downloaded content is an HTML page, not a direct file/image`.
- После тестов разных типов файлов расширена классификация `Content-Kind`: `html`, `json`, `text`, `image`, `document`, `audio`, `video`, `archive`, `binary`.
- Проверены прямые скачивания HTML, JSON, TXT, PNG, JPEG, PDF, MP3 и ошибка `403`; прямой MP3 сохраняется как `.mp3` и помечается `Content-Kind: audio`.
- Добавлен recovery для отказа модели: если debug-ядро на первом шаге отвечает `нет доступа`, хотя инструменты доступны, AI HUB принудительно запускает первый `web_search` по запросу пользователя и возвращает результат ядру.
- Ошибки инструментов теперь возвращаются в сценарий как `Tool error`, а не обрывают всю цепочку.
- Версия повышена до `0.0.27-dev`.
- Установщик не собирался по правилу проекта.
- По уточнению пользователя расширен сценарий выбора источников: F12 agent-loop теперь допускает до 10 tool-запросов на задачу, а `web_search` отдаёт до 10 результатов. Ядро получает инструкцию не ограничиваться первым сайтом, продолжать после HTML/ошибки/неподходящего результата и выбирать лучший источник. Версия повышена до `0.0.28-dev`; проверки прошли: `dotnet build`, scanner кириллицы, сверка локализаций `186/186`. Установщик не собирался по правилу проекта.
- Пользователь заметил, что debug-ядро пишет о найденном/сохранённом файле, хотя в логах были только `web_search` и фактического скачивания не было. Добавлен guard для download-задач: если пользователь просит скачать, `FINAL:` без успешного `Web download complete` от `web_download` блокируется, ядро получает повторную инструкцию искать прямую ссылку; после лимита AI HUB честно пишет, что файл не скачан. Версия повышена до `0.0.29-dev`; проверки прошли: `dotnet build`, scanner кириллицы, локализации `186/186`. Установщик не собирался по правилу проекта.
- Пользователь повторно проверил скачивание фото: ядро нашло прямой файловый URL, но вызывало `web_read` вместо `web_download`, а один из URL был собран неверно и давал `404`. Исправлено в `0.0.30-dev`: для задач скачивания `web_read` по прямому файловому URL автоматически нормализуется в `web_download`; `WebDownloadTool` получил `User-Agent`; `web_read` теперь извлекает кандидаты прямых файлов из HTML. Smoke-test подтвердил скачивание PNG-кандидата из статьи Wikipedia в `Tools\Web\Downloads`. Проверки прошли: `dotnet build`, scanner кириллицы, локализации `186/186`; установщик не собирался по правилу проекта.
- По итогам обсуждения специализированной интернет-модели добавлена подготовка к reranker-инструменту: `BAAI/bge-reranker-v2-m3` будет автоматически скачиваться после основного Qwen3 8B в выбранную папку моделей и регистрироваться через `tool-model.json`.
- На версии `0.0.31-dev` reranker ещё не подключён к логике web-поиска: программа только умеет скачать, проверить, зарегистрировать и показать его в F12 как служебную `tool-only` модель без запуска чата.
- Обновлены `REESTR.md` и `THIRD_PARTY_NOTICES.md` для модели `BAAI/bge-reranker-v2-m3` с лицензией Apache-2.0.
- Проверки шага: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`194` текстовых файла), ключи локализации `ru.json`/`en.json` совпадают `199/199`.
- Установщик не собирался по правилу проекта; модель вручную не скачивалась, скачивание должен выполнить сам AI HUB.
- Пользователь подтвердил, что `BAAI bge-reranker-v2-m3` скачалась и отображается в F12, затем разрешил подключить её к логике.
- Добавлен `WebSearchRerankerService`: `web_search` после получения результатов DuckDuckGo Lite вызывает локальный Python/Transformers backend и пересортировывает результаты по score модели.
- Для запуска создано dev-окружение `Runtime/Python/reranker/.venv` с `torch 2.12.0`, `transformers 5.9.0`, `safetensors 0.7.0`; runtime-папка не входит в GitHub и установщик.
- Если Python/model backend недоступен, поиск не падает и использует `lexical-fallback` с явной пометкой в результате `Rerank: ...`.
- Скрипт `Исходники/AIHub/Tools/bge_rerank.py` проверен напрямую: модель оценила релевантный документ выше нерелевантного.
- Scanner кириллицы обновлён, чтобы не проверять скачанные модели и runtime-пакеты как исходники проекта: исключены `Runtime`, `.venv`, `Модели`. Это понадобилось из-за нормальных токенов в `tokenizer.json` и стороннего LICENSE внутри `.venv`.
- Версия повышена до `0.0.32-dev`.
- По итогам рассуждения о роли ядра создано новое ТЗ `ТЗ/2026-06-01_реестр_возможностей_и_huggingface_provider.md`.
- В новом ТЗ зафиксировано: Qwen3 8B — лёгкое ядро-диспетчер и бытовой собеседник, а новые сценарии обязаны описывать нужные возможности, проверку установленного и поведение при отсутствии компонентов.
- Пользователь согласился закрыть текущее ТЗ интернет-инструментов как выполненное и перенести его в архив, потому что основная часть реализована, а следующие идеи выделены в новое ТЗ.
- ТЗ `2026-06-01_интернет_инструменты_ядра.md` перенесено в `ТЗ/Архив`.
- Финальные проверки закрытия: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`203` текстовых файла), локализации совпадают `199/199`.
- Установщик не собирался по правилу проекта.
- По правилу закрытия ТЗ актуальное состояние публикуется в GitHub.

## 2026-06-01 — исправление установленной версии: backend llama.cpp

- Пользователь собрал установщик и нашёл баг только в установленной версии: debug-окно `F12` видело модель `Qwen3-8B-Q4_K_M.gguf`, но не находило `llama-server.exe` и `llama-cli.exe`.
- Причина: установленное приложение ищет backend в `%LOCALAPPDATA%\AI_HUB\Runtime\Backends\llama.cpp\b9442\win-cuda-12.4-x64`, а установщик не копировал туда runtime-файлы `llama.cpp`.
- Исправлено в сборке установщика: `build-installer.ps1` теперь передаёт путь локального backend-а в Inno Setup, а `AI_HUB.iss` кладёт содержимое `Runtime\Backends\llama.cpp\b9442\win-cuda-12.4-x64` в пользовательскую runtime-папку установленной версии.
- Версия повышена до `0.0.33-dev`.
- Установщик был пересобран как исключение из общего правила, потому что ошибка проявляется именно в установленной версии.
- Новый установщик: `Тесты\Установщики\AI_HUB_Setup_0.0.33-dev.exe`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно, локализации совпадают `199/199`, сборка установщика прошла успешно.

## 2026-06-01 — реестр возможностей, Hugging Face provider и диагностика нулевого поиска

- Реализуется рабочее ТЗ `ТЗ/2026-06-01_реестр_возможностей_и_huggingface_provider.md`.
- Добавлены модели и сервисы первого реестра возможностей: `CapabilityInventoryService` показывает роли `core`, `web_search`, `web_read`, `web_download`, `hf_provider`, `llama_backend`, `reranker`, `embedding`.
- В `F12` добавлены команды `inventory:`, `task_plan:`, `hf_find_model:` и `hf_model_files:`.
- `TaskPlannerService` пока работает как простой эвристический планировщик: определяет задачи поиска, скачивания, подбора моделей и RAG/embedding, показывает установленные и недостающие роли.
- `HuggingFaceProviderTool` работает через Hugging Face API, возвращает кандидатов моделей, лицензию, pipeline, downloads/likes, размеры файлов и прямые `resolve` ссылки. Проверка `hf_find_model` для embedding нашла `nomic-ai/nomic-embed-text-v1.5-GGUF` и файлы `.gguf`.
- `web_search` теперь явно возвращает `Search status`, `Results found`, `HTTP status`, `Possible reason` и `Recommended next steps`. Нулевой поиск фиксируется как диагностический факт, а не как повод для выдуманного ответа.
- Debug-agent теперь блокирует финальный пересказ актуальных фактов, если все поиски пустые, и просит пробовать более простой, английский, смешанный или официальный запрос. Если результаты есть, перед пересказом он должен использовать `web_read`.
- Исправлен дополнительный дефект качества поиска: `bge_rerank.py` теперь читает входной JSON как `utf-8-sig`, поэтому reranker больше не падает на BOM и `web_search` использует `python-transformers-cpu`, а не слабый `lexical-fallback`.
- Парсер `hf_find_model` теперь понимает многострочные/многословные значения `query=...`, например `query=nomic embed text gguf`.
- Версия повышена до `0.0.34-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`259` текстовых файлов), локализации совпадают `199/199`, smoke-test `inventory`, `task_plan`, `hf_find_model`, `web_search` с результатами и `web_search` с нулём результатов прошёл.
- Установщик не собирался по правилу проекта: installed-only поведение в этом ТЗ не проверялось.

## 2026-06-01 — архитектурный принцип Codex-подобной среды

- Зафиксирован общий ориентир разработки: AI HUB строится как Codex-подобная рабочая среда для локальных моделей, но не только про кодинг, а про любые пользовательские задачи.
- Локальная модель считается не всемогущей сущностью, а участником управляемого контура: инструменты, контекст, планирование, инвентарь возможностей, проверка фактов, журналы действий, статусы, разрешения и откаты.
- Новые сценарии нужно проектировать от возможностей среды: если модели нужен интернет, файл, поиск, установка, память или выбор инструмента, это должно быть явным сервисом/инструментом AI HUB с логированием и проверяемым результатом.
- Обновлены `Инструкции/AGENTS.md`, `Инструкции/CODEX.md` и `Документы_проекта/АРХИТЕКТУРА.md`.
- Backup перед правкой: `Backups/20260601_081500_codex_like_concept`.

## 2026-06-01 — закрытие ТЗ реестра возможностей без публикации

- Пользователь подтвердил закрытие текущего ТЗ `2026-06-01_реестр_возможностей_и_huggingface_provider.md`.
- ТЗ перенесён в `ТЗ/Архив/2026-06-01_реестр_возможностей_и_huggingface_provider.md`.
- Закрытый состав: первый реестр возможностей, `inventory:`, `task_plan:`, Hugging Face API provider, диагностика нулевого поиска, запрет выдумывать результаты при `0`, требование `web_read` перед пересказом актуальных фактов, исправление BOM в reranker.
- Пользователь прямо указал закрыть ТЗ без публикации в GitHub. Это исключение из обычного правила архивации зафиксировано в rollback-журнале.
- Следующий обсуждаемый этап: отдельное ТЗ на улучшение интернет-поиска через Codex-подобный `SearchStrategyService`, чтобы модель получала готовую таблицу результатов, а не сама придумывала поисковую стратегию.

## 2026-06-01 — стратегический web research для F12

- Создано и реализуется ТЗ `ТЗ/2026-06-01_стратегический_web_research.md`.
- Добавлен отдельный `SearchStrategyService`: он строит несколько поисковых запросов, запускает поиск, фильтрует нерелевантные результаты, читает до 3 страниц через `web_read` и сохраняет итоговый JSON в `Tools\Web\Research`.
- В `ToolGateway` добавлена команда `web_research: запрос`; в F12 prompt-е для актуальных фактов и новостей теперь предпочтителен `web_research`, а не одиночный `web_search`.
- `WebSearchTool` получил резервный Bing HTML fallback после пустого DuckDuckGo; переходные ссылки Bing распаковываются до реальных URL.
- Добавлена защита от ложноположительных результатов: бессмысленный точный запрос больше не считается успешным только потому, что Bing вернул нерелевантные страницы.
- Версия повышена до `0.0.35-dev`.
- Проверки на момент записи: `dotnet build` успешно без предупреждений; scanner кириллицы успешно (`177` текстовых файлов); локализации совпадают `199/199`; smoke-test `web_research` по космическим новостям дал `Research status: ok` и 3 прочитанных источника; заведомо пустой точный запрос дал `Research status: empty` с причиной `search_results_found_but_filtered_as_irrelevant`.
- Установщик не собирался по правилу проекта.

## 2026-06-01 — F12: датированные пункты и длина ответов

- После пользовательской проверки `web_research` добавлен блок `Dated items`: research-слой извлекает из прочитанных страниц фрагменты рядом с датами, чтобы модель меньше пересказывала меню и навигацию страниц.
- Для новостных запросов добавлен фильтр окна времени: например, при запросе "за 3 дня" старые даты вроде `May 25, 2008` отбрасываются как нерелевантные.
- В F12 prompt добавлено правило: подтверждённые источниками факты и собственные рассуждения модели нужно разделять.
- Пользователь заметил, что debug-ответы подозрительно короткие; подтверждено, что runtime сам ограничивал генерацию `256` токенами.
- В debug-режиме искусственный лимит генерации убран: `llama-server` больше не получает `max_tokens`, fallback `llama-cli` использует `--predict -1`; системная инструкция больше не требует всегда отвечать кратко.
- Версия повышена до `0.0.37-dev`.
- Проверка на момент записи: `dotnet build` успешно без предупреждений.

## 2026-06-01 — закрытие ТЗ стратегического web research без публикации

- Пользователь подтвердил закрытие ТЗ `2026-06-01_стратегический_web_research.md`.
- ТЗ перенесён в `ТЗ/Архив/2026-06-01_стратегический_web_research.md`.
- Перед закрытием выполнен частичный UI-прогон F12 через реальное debug-окно: успело стартовать 57 заданий из 100, 52 завершились, 5 ушли в тайм-аут до перезагрузки ПК.
- Анализ сохранён в `Тесты/1/AI_HUB/Diagnostics/core_ui_run_2026-06-01_10-20-53/analysis_report.md`.
- Главный вывод: `web_research`, HF provider и снятие лимита ответа работают, но следующий этап должен улучшить orchestration F12: разделить обычный чат и tool-agent, ввести строгий протокол команд инструментов и запретить скачивание выдуманных URL.
- GitHub commit/push не выполнялись по прямому указанию пользователя закрыть ТЗ без публикации.
- Новый ТЗ пока не создан: пользователь попросил сначала обсудить следующий этап.

## 2026-06-01 — F12 structured tool-calling

- После обсуждения документации и форумов создано ТЗ `ТЗ/2026-06-01_structured_tool_calling_f12.md`.
- Проверено, что текущая связка `llama-server b9442 + Qwen3-8B-Q4_K_M.gguf` поддерживает официальный OpenAI-compatible `tools` / `tool_calls`.
- В `LlamaServerRuntimeService` добавлена отправка structured tools и чтение `message.tool_calls`.
- В F12 debug-окне обычные вопросы теперь идут обычным чатом, прямые команды инструментов выполняются сразу, а задачи с интернетом, скачиванием, inventory, task planning и Hugging Face используют structured tool-calling.
- Старый текстовый протокол инструментов оставлен как fallback.
- Для `web_download` добавлена защита: structured-режим не скачивает URL, если он не был дан пользователем и не найден предыдущими инструментами.
- Версия повышена до `0.0.38-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно, локализации совпадают `203/203`, независимый smoke-test `/v1/chat/completions` вернул structured `web_search` tool call.
- Установщик не собирался по правилу проекта.

## 2026-06-01 — закрытие ТЗ structured tool-calling

- Пользователь подтвердил, что ТЗ `2026-06-01_structured_tool_calling_f12.md` выполнено.
- ТЗ перенесён в `ТЗ/Архив/2026-06-01_structured_tool_calling_f12.md`.
- Состав закрытого этапа: официальный structured tool-calling в F12, разделение обычного чата и tool-agent, fallback на старый текстовый протокол, защита `web_download` от выдуманных URL, локализация новых debug-логов.
- В `.gitignore` добавлено правило для локальных диагностических результатов `**/AI_HUB/Diagnostics/**`, чтобы не публиковать служебные прогоны F12 в GitHub.
- Финальные проверки закрытия: `dotnet build` успешно без предупреждений, scanner кириллицы успешно, локализации совпадают `203/203`.
- Установщик не собирался по правилу проекта.
- По правилу архивации ТЗ актуальное состояние проекта опубликовано в GitHub: commit `0b60253`, push в `origin/main`.

## 2026-06-01 — профиль пользователя

- Создано и реализуется ТЗ `ТЗ/2026-06-01_профиль_пользователя.md`.
- Добавлена кнопка профиля в верхнюю панель рядом с настройками и темой. Если профиль неполный, кнопка мягко моргает.
- Профиль открывается как страница внутри основного окна, не как отдельное окно.
- В профиль добавлены имя/ник, ручное местоположение, предпочтения формата ответов и режим нагрузки: лёгкий, оптимальный, экстрим.
- Все элементы профиля условно обязательные: начать работу можно и без полного профиля, но при нажатии `Начать работу` программа предлагает дозаполнить данные и объясняет пользу профиля.
- Данные сохраняются в существующий локальный `user-profile.json`; добавлен `profileVersion`.
- Скрытый пользовательский контекст ядра теперь умеет учитывать имя, ручное местоположение, стиль ответов и выбранный режим нагрузки.
- Версия повышена до `0.0.39-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`186` текстовых файлов), локализации совпадают `241/241`, smoke-запуск приложения прошёл и окно закрылось штатно.
- Установщик не собирался по правилу проекта.

## 2026-06-01 — внешний smoke-тест на слабом ноутбуке

- Пользователь установил старую актуальную сборку AI HUB на второй ПК: очень слабый ноутбук, заметно слабее основной машины разработки.
- Подтверждено: программа не привязана к ПК разработки, установка прошла, нужные компоненты скачались, ядро в F12 запустилось.
- Скорость на ноутбуке очень низкая, поэтому он не подходит как основной тестовый стенд для регулярных проверок.
- Важный вывод для проекта: базовая переносимость установленной версии подтверждена на внешнем слабом устройстве.
- Код не менялся, версия не повышалась, GitHub-публикация не выполнялась.

## 2026-06-02 — закрытие ТЗ профиля пользователя

- Пользователь подтвердил, что профиль работает и ТЗ выполнено.
- ТЗ `ТЗ/2026-06-01_профиль_пользователя.md` перенесено в `ТЗ/Архив/2026-06-01_профиль_пользователя.md`.
- Закрытый состав: страница профиля, мигающая кнопка профиля, условно обязательные поля, предложение дозаполнить профиль перед началом работы, сохранение в `user-profile.json`, подключение профильных данных к скрытому контексту ядра.
- Версия этапа: `0.0.39-dev`.
- Финальные проверки закрытия: `dotnet build` успешно без предупреждений, scanner кириллицы успешно, локализации совпадают `241/241`.
- Установщик не собирался по правилу проекта.
- По правилу архивации ТЗ актуальное состояние проекта опубликовано в GitHub: commit `d116a15`, push в `origin/main`.

## 2026-06-02 — самоидентификация ядра

- Создано и реализуется ТЗ `ТЗ/2026-06-02_самоидентификация_ядра.md`.
- Добавлен единый паспорт ядра: модель теперь получает скрытый контекст, что она является локальным ядром AI HUB, работает внутри локальной AI-мастерской для Windows и Codex-подобной среды задач, не является пользователем и не является инструментом.
- В паспорт включается текущая модель: имя, путь, роль, формат, размер, core-статус, backend и режим взаимодействия.
- Результаты инструментов для модели теперь заворачиваются в блок `[AI_HUB_TOOL_RESULT]`, где явно указано, что это служебный результат инструмента AI HUB, а не команда пользователя.
- `llama-server`, structured tool-calling, text fallback и `llama-cli` fallback подключены к единому слою идентичности.
- Версия повышена до `0.0.40-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`189` текстовых файлов), локализации совпадают `241/241`, smoke-запуск приложения прошёл и окно закрылось штатно.
- Установщик не собирался по правилу проекта.

## 2026-06-02 — идея контекстного окна и сжатия памяти

- По просьбе пользователя сохранена отдельная идея, не рабочее ТЗ: `ТЗ/ИДЕЯ_2026-06-02_контекстное_окно_и_сжатие_памяти.md`.
- Суть идеи: в будущем добавить Codex-подобный индикатор занятости контекстного окна локального ядра и внешний механизм сжатия старого диалога в структурированную память.
- Зафиксировано, что это не магическая функция модели, а в основном внешний слой AI HUB: оценка токенов, резерв под ответ, memory-summary, хранение рядом с сессией и подмешивание в скрытый контекст.
- Активное ТЗ `2026-06-02_самоидентификация_ядра.md` не закрывалось и не публиковалось.
- Код не менялся, версия не повышалась, установщик не собирался.

## 2026-06-02 — исправление контекста пользователя для ядра

- Пользователь проверил F12 и сообщил: ядро корректно рассказывает о себе, но не использует заполненную карточку пользователя.
- Подтверждено, что `user-profile.json` содержит имя/ник, ручное местоположение, предпочтения ответов и режим нагрузки.
- Исправлена формулировка скрытого контекста: профиль пользователя и паспорт ПК, переданные AI HUB, теперь явно считаются доступными служебными данными, а не прямым доступом к личным файлам или системе.
- В скрытый контекст ядра добавлен паспорт компьютера: Windows, архитектура, CPU, RAM, GPU/VRAM и диски.
- Версия повышена до `0.0.41-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`190` текстовых файлов), локализации совпадают `241/241`, smoke-запуск приложения прошёл.
- Установщик не собирался по правилу проекта.

## 2026-06-02 — закрытие ТЗ самоидентификации ядра без публикации

- Пользователь подтвердил, что ТЗ самоидентификации ядра выполнено: F12 корректно показывает знание ядра о себе, пользователе и паспорте ПК.
- ТЗ `ТЗ/2026-06-02_самоидентификация_ядра.md` перенесено в `ТЗ/Архив/2026-06-02_самоидентификация_ядра.md`.
- Закрытый состав: единая идентичность ядра, паспорт текущей модели, явное разделение пользователя и инструментов, упаковка tool-result в `[AI_HUB_TOOL_RESULT]`, подключение к `llama-server`/structured tool-calling/text fallback/`llama-cli`, профиль пользователя и паспорт ПК в скрытом контексте.
- Версия этапа: `0.0.41-dev`.
- Последние проверки перед закрытием уже были выполнены: `dotnet build`, scanner кириллицы, сверка локализаций `241/241`, smoke-запуск приложения.
- Установщик не собирался по правилу проекта.
- GitHub commit/push не выполнялись по прямой команде пользователя: "Пока не публикуй".

## 2026-06-02 — контекстная память ядра

- Создано и реализовано ТЗ `ТЗ/2026-06-02_контекстная_память_ядра.md`.
- Добавлен переносимый сервис `CoreContextMemoryService`: оценивает заполнение текущего рабочего контекста ядра, автоматически сжимает старую часть F12-диалога и сохраняет memory-summary рядом с debug-сессией.
- В нижней инфо-зоне главного окна добавлен простой индикатор памяти ядра: `🧠` + шкала; при близком заполнении значок меняется на `🤯`, при неактивном F12 элемент притемняется, во время сжатия шкала работает в неопределённом режиме.
- Сжатая память подмешивается в последующие запросы как служебный `system`-контекст, а не как сообщение пользователя.
- Официальные лимиты Qwen3-8B зафиксированы отдельно (`32768` native, `131072` YaRN по карточкам Qwen на Hugging Face), но фактическая шкала сейчас ориентируется на текущий запуск backend-а `--ctx-size 4096`.
- Версия повышена до `0.0.42-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`193` текстовых файла), локализации совпадают `247/247`, smoke-запуск приложения прошёл.
- Установщик не собирался по правилу проекта.

## 2026-06-02 — правка индикатора памяти ядра

- Пользователь проверил F12 и заметил два дефекта: значок `🧠` плохо дружит с тёмной темой, а шкала сразу съедает десятки процентов из-за искусственных резервов.
- Исправлено: значок памяти теперь явно окрашивается под состояние (`голубой` в норме, `оранжевый` около заполнения, вторичный цвет в неактивном состоянии).
- Из расчёта шкалы убраны искусственные резервы под системный prompt и ответ; учитываются только реальные сообщения, pending prompt и memory-summary.
- Индикатор и порог автосжатия теперь ориентируются на официальный нативный лимит Qwen3-8B `32768`, а не на временный backend `--ctx-size 4096`. Сам `--ctx-size` пока не увеличивался, чтобы не поднять расход памяти на слабых ПК без отдельного решения.
- Версия повышена до `0.0.43-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`193` текстовых файла), локализации совпадают `247/247`, smoke-запуск приложения прошёл.
- Установщик не собирался по правилу проекта.

## 2026-06-02 — повышение backend-контекста ядра

- Пользователь уточнил, что целевой пользователь AI HUB имеет минимум средний игровой ПК, поэтому временный backend-лимит `4096` излишне жёсткий.
- Реальный backend `--ctx-size` повышен до официального нативного лимита Qwen3-8B `32768`.
- `llama-server` и `llama-cli` fallback теперь читают общий `CoreContextRuntimeLimits.CurrentBackendContextLimit`, чтобы не было двух ручных истин.
- YaRN-лимит `131072` оставлен только как официальная справочная граница на будущее, без включения в текущий запуск.
- Версия повышена до `0.0.44-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`193` текстовых файла), локализации совпадают `247/247`, smoke-запуск приложения прошёл.
- Установщик не собирался по правилу проекта.

## 2026-06-02 — проверка смысловых потерь при сжатии памяти ядра

- По просьбе пользователя выполнен временный harness-тест текущего `CoreContextMemoryService` на длинном литературном отрывке.
- Тест показал границы текущей реализации: последние сообщения сохраняются хорошо, но старый длинный смысловой фрагмент без отдельного semantic-summary теряется.
- Контрольные результаты: свежий отрывок сохранил `9/10` смысловых маркеров, отрывок рядом с границей сжатия — `2/10`, отрывок далеко в старой истории — `0/10`.
- Вывод: для текущей роли ядра-диспетчера уровня достаточно, потому что ядро в основном должно понять задачу, выбрать процесс и передать работу инструменту. Для книг, документов и длинных материалов понадобится отдельный инструментовый контекст/файловое хранилище и позже умное semantic-summary.
- Код проекта не менялся, версия не повышалась, установщик не собирался.

## 2026-06-02 — модельное сжатие памяти ядра и чтение лога сессии

- Пользователь уточнил принцип памяти: сжатие должно выполнять само ядро, потому что механическое сжатие ненадёжно сохраняет смысл. Механический вариант оставлен только как fallback.
- Активное ТЗ `ТЗ/2026-06-02_контекстная_память_ядра.md` расширено: memory-summary должен сохранять выборы пользователя, цель задачи, сделанное, план, открытые вопросы, важные пути/ссылки/модели/инструменты и ограничения.
- В `CoreContextMemoryService` добавлен модельный план сжатия: сервис выбирает старую часть истории, формирует служебный prompt для ядра и применяет ответ модели как новую память сессии.
- F12 теперь при опасном заполнении сначала просит само ядро обновить память; если это не удалось, используется старое аварийное механическое сжатие.
- Добавлен диагностический инструмент `session_log`: ядро может прочитать последние записи текущего JSONL-лога или найти фрагмент по строке, чтобы восстановить детали текущей сессии.
- Инструмент читает только текущий файл сессии и не даёт модели произвольный доступ к файлам пользователя.
- Версия повышена до `0.0.45-dev`.
- Проверки: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`203` текстовых файла), локализации совпадают `250/250`, smoke-запуск приложения прошёл.
- Установщик не собирался по правилу проекта. ТЗ не архивировалось и в GitHub не публиковалось.

## 2026-06-02 — повторный книжный тест модельной памяти

- По просьбе пользователя повторно проверено сжатие памяти по старому сценарию с литературным отрывком, теперь уже через модельное сжатие и реальный `llama-server`.
- Тест выполнялся временным harness-проектом в `%TEMP%`, исходники AI HUB не менялись.
- Проверялись 10 смысловых маркеров: компас, Светлоград, Марфа, лодка, зелёная лампа, седьмая ступень, северная дверь, Ромашка, яблоки и часы капитану.
- Результаты:
  - отрывок в начале старой истории: `0/10`;
  - отрывок рядом с границей сжатия: `9/10`;
  - отрывок глубоко в середине старой истории: `0/10`.
- Вывод: модельное сжатие хорошо работает, когда важный фрагмент попал в выбранный участок истории, но текущая нарезка истории всё ещё ненадёжна для важных фрагментов внутри длинного диалога. Следующий слой памяти должен выделять защищённые якоря вокруг слов `запомни`, `важно`, `решили`, а также использовать `session_log` для восстановления деталей.
- Код проекта не менялся, версия не повышалась, установщик не собирался.

## 2026-06-02 — закрытие ТЗ контекстной памяти ядра

- Пользователь подтвердил, что все активные ТЗ выполнены, а файл идеи тоже должен уйти в архив.
- В архив перенесены:
  - `ТЗ/Архив/2026-06-02_контекстная_память_ядра.md`;
  - `ТЗ/Архив/ИДЕЯ_2026-06-02_контекстное_окно_и_сжатие_памяти.md`.
- Закрытый состав этапа:
  - индикатор заполнения контекста ядра в нижней инфо-зоне;
  - официальный native context limit Qwen3-8B `32768` для текущего backend;
  - автоматическое сжатие старой F12-истории;
  - модельное сжатие как основной путь;
  - механическое сжатие как fallback;
  - `session_log` для чтения текущего JSONL-лога;
  - тесты смысловых потерь на книжном сценарии.
- Зафиксирован вывод на будущее: дальнейшие улучшения памяти нужно проверять уже в полноценном сценарии работы, потому что ядро AI HUB будет жить не в обычном чате, а в сценарном диалоге, похожем на NPC/квестовую карточку.
- Версия этапа: `0.0.45-dev`.
- Установщик не собирался по правилу проекта.
- Проверки закрытия прошли: `dotnet build` успешно без предупреждений, scanner кириллицы успешно (`203` текстовых файла), локализации совпадают `250/250`, smoke-запуск приложения прошёл.
- По стандартному правилу закрытия ТЗ актуальное состояние опубликовано в GitHub: commit `2fb4151`, push в `origin/main`.

## 2026-06-03 — первый боевой сценарий выбора

- Пользователь решил, что полноценный RAG лучше отложить до появления первого реального сценария.
- Создано рабочее ТЗ `ТЗ/2026-06-03_первый_боевой_сценарий_выбора.md`.
- Суть сценария: AI HUB помогает пользователю сделать выбор по любому вопросу.
- Зафиксирована архитектурная роль: ядро AI HUB подготавливает задачу и передаёт её более мощной общепрофильной рабочей модели.
- Окончательное название рабочей модели пока не выбрано; в коде допустима нейтральная роль `general_worker`.
- Если отдельная рабочая модель ещё не установлена, сценарий может временно использовать текущее основное ядро, но должен честно показывать это в UI и логах.
- Интернет допускается только через существующий Tool Gateway и только при необходимости; приоритет остаётся за знаниями и рассуждением модели.
- Концепция сценария уточнена: это не прямой чат, а NPC-опросник / режим планирования.
- Правило шага сценария: начинать с крупных групп направлений и постепенно сужать; стараться держать максимум 6 готовых вариантов ответа в 3 строки плюс отдельный `Свой вариант` 4-й строкой.
- `Свой вариант` не должен превращать сценарий в свободный чат: ядро классифицирует ответ и возвращает пользователя в управляемую структуру.
- Визуал вариантов уточнён: интерфейс должен показывать ровно столько кнопок, сколько вариантов предложило ядро, без пустых слотов; базовая раскладка — две колонки до 6 вариантов и отдельная широкая строка `Свой вариант`.
- Допустимы спокойные Windows-подобные анимации появления вариантов: плавное проявление, лёгкий сдвиг и мягкая подсветка выбранного варианта.
- Уточнена граница роли ядра: сценарий должен дойти почти до конца подготовки, собрать карточку задачи, критерии, ограничения, будущий prompt и предложить оптимальную модель-исполнитель.
- Ядро не должно перепрыгивать с подготовки к финальному решению, потому что глубокий разбор выбора — задача рабочей модели. Скачивание новой модели на этом этапе не подключается.
- Добавлено правило визуальной рекомендации: если ядро считает один из вариантов желательным, оно должно передать структурный признак рекомендации и короткую причину, а UI мягко выделит вариант цветом обводки/фона или меткой.
- В активное ТЗ добавлен раздел по официальным документам: WPF data binding, `ItemsControl`, `ObservableCollection<T>`, WPF animations, а также `llama.cpp` OpenAI-compatible server, function calling/tool use и JSON/schema-constrained output.
- Зафиксирован главный технический вывод: программа не должна вылавливать варианты из обычного текста ядра; ядро должно отдавать валидируемую JSON-структуру шага, а UI рисует кнопки из данных.
- Для `Режима неопределенности` добавлено правило подбора исполнителя: ядро не должно предлагать модель/инструмент слабее себя как оптимальный путь; выбор исполнителя должен учитывать паспорт ПК и настройки/профиль пользователя.
- Если более сильный исполнитель не установлен, финальная карточка должна честно показать это и не запускать скачивание автоматически.
- Выбран фиксированный первый шаг `Режима неопределенности`: вопрос `К чему ближе вопрос?` и варианты `Знания`, `Вещи`, `Жизнь`, `Технологии`, `Люди`, `Цели`, плюс `Свой вариант`.
- Этот первый выбор может использоваться как часть конструктора стартового prompt для ядра, чтобы ядро сразу получало крупную область неопределённости.
- В ТЗ зафиксирован рабочий prompt-контур ядра: на каждом шаге выполнять “перезалив контекста” с ролью ядра, правилами сценария, текущей карточкой состояния, последним выбором пользователя, профилем, паспортом ПК, доступными моделями/инструментами и JSON-схемой ответа.
- `Свой вариант` ограничен как короткая метка направления на 1-3 слова, а не свободный вопрос или мини-чат. Если ввод длинный, UI должен попросить сократить.
- Финальный подбор исполнителя выполняется после подготовки карточки; интернет/Hugging Face инструменты уместны прежде всего на этом этапе или когда явно нужны актуальные факты.
- После финального анализа ТЗ уточнены границы первой реализации: существующая заглушка переименуется в `Режим неопределенности`, первый этап проверяет сценарное поведение ядра и заканчивается карточкой задачи, предложенным исполнителем и prompt, без скачивания и запуска новой модели.
- Добавлены состояния сценария: `start_fixed_step`, `question_step`, `custom_input`, `final_task_card`, `structure_error`.
- Добавлены правила устойчивости: кнопка `Назад`, повторный запрос исправления JSON, спокойная ошибка после двух невалидных структур, ориентиры длины текста для кнопок.
- При подборе исполнителя ядро сначала проверяет установленное, затем при необходимости ищет в интернете; если интернета нет, предлагает текущее ядро только как временный fallback.
- Начата реализация первого слоя `Режима неопределенности`.
- Добавлены модели сценария: `ChoiceScenarioOption`, `ChoiceScenarioStep`, `ChoiceTaskCard`.
- Добавлен `ChoiceScenarioService` с фиксированным первым шагом `К чему ближе вопрос?`, вторым демонстрационным шагом сужения и финальной preview-карточкой подготовки.
- В `MainWindow` добавлена внутренняя страница `ChoiceScenarioPage`, открываемая из существующей заглушки режима без нового окна.
- Добавлены динамические варианты через `ItemsControl`: показывается ровно столько вариантов, сколько есть в текущем шаге, до 6; `Свой вариант` отдельной строкой.
- Добавлена зона `Мысль ядра` над кнопками как безопасное короткое объяснение шага, не скрытая цепочка рассуждений.
- Добавлен ввод `Свой вариант` с ограничением 1-3 слова.
- Добавлена финальная карточка подготовки: область, фокус, предлагаемый исполнитель, статус исполнителя, причина, нужен ли интернет и prompt для будущего исполнителя.
- Режим пока не запускает и не скачивает новую модель; текущий слой проверяет сценарный контур.
- Версия повышена до `0.0.46-dev`.
- Проверки `dotnet build` и shell-backup не удалось запустить из-за системной sandbox-ошибки запуска процессов `CreateProcessAsUserW failed: 5`.
- После пользовательского запуска сборки обнаружена ошибка `CS0104`: `Button` был неоднозначен между `System.Windows.Controls.Button` и `System.Windows.Forms.Button`.
- Исправлено точечно: в обработчиках `ChoiceOptionButton_Click` и отправке custom-варианта явно указан `System.Windows.Controls.Button`.
- После визуального прогона пользователь уточнил, что текущий кодовый второй шаг неверен: после первого фиксированного шага все последующие шаги должно предлагать ядро, а количество шагов должно решаться ядром.
- ТЗ дополнено: добавлена команда `Перейти к финалу`, адаптивность страницы, и правило ядро-ведомого сценария после первого шага.
- Реализация переделана: `ChoiceScenarioService` теперь содержит стартовый шаг, fallback-шаг, prompt-контур ядра и парсинг JSON-шага.
- `LlamaServerRuntimeService` получил метод `GenerateScenarioJsonAsync` для запроса JSON-шага сценария через текущую core-модель.
- `MainWindow` теперь после выбора отправляет историю ответов ядру, получает JSON следующего шага, валидирует его и только потом показывает варианты. Жёсткий второй шаг используется только как fallback при недоступности ядра.
- Добавлена кнопка `Перейти к финалу`: она просит ядро собрать `final_task_card`, а не строит финал в коде.
- Добавлена модель `ChoiceScenarioAnswer`; история ответов используется для prompt-контекста и кнопки `Назад`.
- По замечанию пользователя исправлена неудачная адаптивность страницы: `WrapPanel` с фиксированной шириной карточек создавал лишний скролл и одноколоночный столбик.
- Варианты возвращены в двухколоночный `UniformGrid`, основной блок центрирован, горизонтальный скролл отключён.
- Повторная правка адаптивности: внутренний `ScrollViewer` первого экрана сценария заменён на центрированный `Grid`, уменьшены вертикальные отступы, размер заголовка вопроса и высота карточек вариантов, чтобы 6 вариантов и кнопки помещались без лишнего скролла в обычном окне.
- По скриншоту пользовательского прогона выявлена ошибка prompt-контракта: ядро после раннего `Перейти к финалу` выдумало конкретную задачу про кота и назначило `владельца кота` исполнителем вместо подбора AI-модели/инструмента.
- Prompt `Режима неопределенности` усилен: ядру запрещено выдумывать конкретную задачу пользователя; `final_task_card` запрещён, пока неизвестен конкретный предмет неопределённости; `recommendedExecutor` может быть только моделью, backend-ом, инструментом или ролью AI HUB.
- Кнопка `Перейти к финалу` скрыта на первом шаге и появляется только после хотя бы одного выбора.
- Добавлено правило и реализация журналов сценария: `Режим неопределенности` создаёт отдельный JSONL-журнал в `<Папка результатов>\AI_HUB\Scenarios\Uncertainty\Sessions`.
- Добавлен `ScenarioSessionLog`, который пишет `scenario_session_start`, `scenario_context_snapshot`, `scenario_user_choice`, `scenario_core_prompt`, `scenario_core_raw_response`, `scenario_parsed_step`, `scenario_structure_error`, `scenario_final_task_card`, `scenario_core_unavailable`, `scenario_session_end`.
- Для будущих инструментов сценария зарезервирована область `<Папка результатов>\AI_HUB\Scenarios\Uncertainty\Tools`.
- После пользовательского build исправлена ошибка `CS0246` в `ScenarioSessionLog.cs`: добавлен `using System.IO;` для `StreamWriter`, `File` и `Path`.

## 2026-07-10 — аудит и укрепление «Режима неопределенности»

- Реализованы практические исправления, найденные при полном аудите проекта.
- Создан path-safe backup `Backups/20260710_full_project_audit_remediation`.
- Сценарные JSONL-журналы исключены из Git; журнал получил GUID, schema/session metadata, локальное/UTC-время, признак чувствительных данных, фоновую запись и fallback в `%LOCALAPPDATA%`.
- Устранён двойной выбор: busy-state проверяется до записи ответа, интерактивные элементы блокируются на время запроса.
- Добавлены снимки шагов: `Назад` восстанавливает точный предыдущий экран без повторного запроса к модели. Учтён ручной и автоматический переход к финалу.
- Ядро подключено к общей идентичности AI HUB через режим `ScenarioPlanner`.
- Добавлен `ChoiceScenarioOrchestrator`: structured tool loop, разрешённые web/inventory/Hugging Face tools без скачивания, две JSON repair-попытки и финальная проверка.
- Финал требует результаты `inventory` и `hf_find_model`; web-инструмент не может стать моделью-исполнителем. `current_core` разрешён только как `core_fallback` при недоступном поиске.
- Добавлены строгий `response_format`/JSON Schema, повторная валидация и защита от зацикливания по fingerprint вопроса и вариантов.
- Стартовые/fallback-строки и финальная карточка локализованы. UI центрирован без постоянного scroll viewer, уменьшается через `Viewbox` при нехватке места; варианты получили fade/slide-анимацию.
- Общий каталог сценарных инструментов переиспользуется F12; пути llama.cpp сведены в `LlamaBackendPaths`; `VERSION` стал единственным источником версии для MSBuild.
- Добавлен `AIHub.Tests` на `MSTest.Sdk 4.2.3`; прошло 8 тестов контракта, loop detection, возврата и журнала.
- Активное ТЗ переписано как единый источник истины. Обновлены README, ROADMAP, REESTR, THIRD_PARTY_NOTICES, CODEX и каталог повторяемых багов.
- Проверки: restore, Debug/Release build без предупреждений, 8/8 тестов, format check, UTF-8 scanner (`223` файла), локализации `315/315`, smoke-запуск.
- Реальный `llama-server b9442 + Qwen3 8B` принял `response_format=json_schema` и вернул валидный шаг с двумя вариантами.
- Версия остаётся `0.0.46-dev`. Установщик не собирался. Commit/push не выполнялись: активное ТЗ ещё не закрыто.

## 2026-07-11 — исправление финального подбора модели

- По пользовательскому тесту разобран журнал `2026-07-10_14-33-05_uncertainty_8e42ff4bc45c4129b9a8aa96ecc9adbd.jsonl`.
- Найден тупик: ядро предлагало установленное Qwen3 8B, структурный валидатор отклонял карточку до запуска обязательных `inventory`/`hf_find_model`, а repair-запросы без инструментов повторяли тот же выбор.
- Финальный orchestration изменён: при ручном запросе финала `inventory` и `hf_find_model` принудительно вызываются через structured `tool_choice`; автоматический `final_task_card` распознаётся до полной валидации и запускает ту же проверку.
- Добавлено жёсткое правило профиля: при `balanced` и `extreme` рабочая модель должна быть строго мощнее 8B; текущее Qwen3 8B используется только как ядро-планировщик и поисковик. Исключение для лёгкого исполнителя возможно только при явно выбранном `light`.
- В task card добавлен `executorCapabilityClass`; программа проверяет класс мощности и явные размеры в названии модели. Qwen3 8B нельзя выдать за `above_8b`.
- `UserContextSnapshot` теперь содержит `WorkloadMode`, поэтому правило нагрузки явно попадает в prompt и журнал.
- Ошибочная смесь `question_step + taskCard` больше не прерывает ветку: преждевременная карточка игнорируется для вопроса. JSON Schema больше не заставляет question step обязательно заполнять `taskCard`.
- Anti-loop расширен семантическим шаблоном: цепочка вида `Какой конкретный аспект...` останавливается после трёх однотипных сужений и отправляется на другую ось или финал.
- Добавлены тесты политики мощности, смешанного question/taskCard и семантического цикла. Итог: 14/14 тестов.
- Проверки: Debug/Release build без предупреждений, 14/14 тестов, format check, UTF-8 scanner (`225` файлов), smoke-запуск.
- Живой тест текущего `llama-server b9442 + Qwen3 8B` подтвердил принудительный `tool_choice`: backend вернул structured-вызов `inventory` и `finish_reason=tool_calls`.
- Версия остаётся `0.0.46-dev`; установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — бюджет шагов «Режима неопределенности»

- По решению пользователя перед выбором крупной области добавлен фиксированный экран `За сколько шагов подобрать инструмент?`.
- Варианты: `Быстро — до 4`, `Обычно — до 10`, `Подробно — до 20`, `По ситуации`.
- `По ситуации` позволяет ядру закончить по достаточности данных, но имеет защитный предел 30 содержательных шагов.
- Выбор глубины не считается содержательным шагом. Первый учитываемый шаг — выбор крупной области. JSON errors, repair, tool calls и заблокированные повторы бюджет не расходуют; `Назад` освобождает связанный ответ.
- При каждом запросе ядро получает mode, maximum, used и remaining. Prompt требует распределять уточнения под бюджет, объединять второстепенные вопросы и готовить результат не позднее последнего шага.
- При одном оставшемся шаге ядро получает предупреждение выбрать самый информативный вопрос. При нулевом остатке новый `question_step` запрещается и программа валидирует только `final_task_card`.
- В интерфейсе показывается текущий номер содержательного вопроса и максимальный/защитный предел.
- Возврат с экрана крупной области на выбор глубины сбрасывает прежний бюджет и позволяет выбрать другой.
- Добавлены модели бюджета и тесты фиксированных вариантов, подсчёта содержательных шагов, автоматического safety limit и hard-final prompt.
- Проверки: Debug/Release build без предупреждений, 18/18 тестов, format check, UTF-8 scanner (`226` файлов), локализации `329/329`, smoke-запуск.
- Версия остаётся `0.0.46-dev`; установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — устранение повторного срыва финала после бюджета шагов

- Разобран журнал `2026-07-11_04-18-48_uncertainty_fd030b38d39a406e98d414456e9087b9.jsonl`: бюджет корректно довёл сценарий до обязательного финала, `inventory` и `hf_find_model` исполнились, но ядро искало модель по теме `управление государством`, получило ноль кандидатов и снова выбрало Qwen3 8B.
- Сообщение policy repair было неоднозначным и подталкивало ядро поменять роль на `core_fallback`, сохранив запрещённую 8B-модель. Тексты ошибок и repair-инструкция исправлены.
- При пустом поиске программа теперь выполняет резервный запрос по семейству модели, подходящему режиму нагрузки. Для `balanced` используется семейство `Qwen3-14B-GGUF`; запрос проверен на официальном Hugging Face API и возвращает актуальные репозитории.
- Добавлен программный отбор кандидата: обязательный явный размер выше 8B, блокировка нежелательных модификаций и предпочтение доверенных репозиториев. Если ядро всё равно выбирает себя, поле исполнителя автоматически заменяется найденным кандидатом и карточка повторно валидируется.
- Добавлено отдельное журналирование резервного поиска и программной коррекции исполнителя.
- Проверки: Debug/Release build без предупреждений, 20/20 тестов, format check, scanner `230` файлов, локализации `329/329`, `git diff --check` и smoke-запуск.
- Версия остаётся `0.0.46-dev`; установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — изолированный парсер каталога Hugging Face

- По решению пользователя начат отдельный этап локального каталога моделей без подключения к ядру, сценарию, UI или автозапуску.
- Создано ТЗ `ТЗ/2026-07-11_парсер_каталога_huggingface.md` и backup `Backups/20260711_huggingface_catalog_parser`.
- Добавлены `HuggingFaceCatalogCollector`, `HuggingFaceCatalogParser`, модели стабильной схемы и отдельный диагностический CLI `AIHub.CatalogProbe`.
- Компонент сохраняет `catalog.json`, сырой search JSON, detail JSON и Model Card. Для источников фиксируются URL, время получения, revision SHA и SHA-256 локального raw-файла.
- Metadata имеет evidence `huggingface_hub_api`; извлечённое описание имеет evidence `model_card_author_claim` и не считается проверенным фактом.
- Живой пробный сбор показал, что поиск одного веса смешивает официальный вариант, обычные упаковки и сторонние fine-tune. Для `Qwen3-14B-GGUF` сохранено пять записей; для `Qwen/Qwen3-14B-GGUF` — три, включая официальный репозиторий.
- Для официальной Qwen-записи вручную подтверждено совпадение repo id, SHA ревизии, лицензии, base model, parameter count и context length с raw API. Контрольные суммы API/Model Card совпали.
- По уточнению пользователя обнаружение сделано открытым: `search` ищет по теме/семейству, `author` обновляет любого переданного автора, `latest` получает общую ленту новых публичных моделей без allowlist. Известные создатели в будущем будут только источником приоритетного обновления, а не фильтром допуска.
- Живые пробы `latest` и `author:Qwen` успешно получили соответственно модели неизвестных заранее разработчиков и новые записи заданного автора.
- Артефакты проверки находятся в `Тесты/HuggingFaceCatalogProbe` и исключены из Git.
- Проверки: Debug/Release build без предупреждений, Release-сборка probe, 23/23 теста, format check, scanner `283` файлов, локализации `329/329`, `git diff --check` и smoke-запуск. Полный обход Hub пока не выполнялся.
- Версия остаётся `0.0.46-dev`; установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — удаление программного уклона к Qwen в выборе исполнителя

- По наблюдению пользователя проведён аудит кода и журналов. Подтверждено, что сценарий программно задавал fallback `Qwen3-14B-GGUF` для balanced, а также Qwen 9B/27B для других режимов; tool description и selector дополнительно закрепляли этот выбор.
- Журналы научно-текстового теста подтвердили прямой вызов `query=Qwen3-14B-GGUF` и итог `Qwen3-14B-Q4_K_M.gguf` независимо от широкого класса подходящих моделей.
- Создан backup `Backups/20260711_remove_scenario_model_bias`.
- Удалены семейный fallback, список предпочтительных издателей и автоматическая замена ответа ядра кандидатом, выбранным программой.
- Пустой поиск теперь возвращается ядру с нейтральным требованием самостоятельно изменить стратегию; AI HUB не называет семейство или издателя.
- Программа только валидирует результат: запрещает current core/слабую модель по режиму, проверяет точное присутствие repo/file в фактической выдаче и не подставляет альтернативу.
- По дополнительному решению пользователя модель семейства текущего ядра допускается только при явно значительно более новом поколении. Больший размер той же генерации недостаточен: Qwen3 14B отклоняется, а явно более новое поколение вроде Qwen3.5 проходит эту отдельную проверку. До подключения локального каталога используется консервативный порог номера поколения `+0.5`.
- Проверки: Debug/Release build без предупреждений, Release probe build, 24/24 теста, format check, scanner `283` файлов, локализации `329/329`, `git diff --check`, smoke-запуск и отдельный bias scan runtime-файлов.
- Версия остаётся `0.0.46-dev`; установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — накопительный каталог и радар популярных моделей Hugging Face

- По решению пользователя диагностический парсер расширен выбранным каталогом для всей программы, но по-прежнему не подключён к ядру, сценарию, UI или автозапуску основного приложения.
- Создан `Каталоги/huggingface-catalog-seed.json`: 11 направлений по 9 слотов, всего 99 размещений и 78 уникальных проверенных repo id.
- Малые специализированные модели оформлены как 12 явных ручных слотов-исключений; автоматический радар принимает только модели с подтверждённым размером строго больше 8B.
- `sync` объединяет данные с прошлой базой, отслеживает revision SHA, доступность и существенные metadata, сохраняет raw API/Model Card и пишет изменения в JSONL.
- Радар не имеет allowlist разработчиков: он объединяет глобальные trending/likes/downloads и отдельные trending-ленты поддерживаемых pipeline, отмечает новых авторов и балансирует кандидатов по направлениям.
- Чистые quantized-упаковки автоматически отбрасываются; содержательные fine-tune/distill могут остаться отдельными кандидатами.
- Первый живой проход и повторные синхронизации успешно создали локальный каталог `Runtime/Каталоги/HuggingFace`. После очистки политикой: 94 уникальные записи, из них 78 посевных и 16 радарных; дублей, недоступных репозиториев и предупреждений нет.
- Добавлены тесты посевной структуры, разбора кратких radar metadata, накопления состояния, смены revision SHA и строгого фильтра `>8B`. Финальные проверки: 28/28 тестов, Debug/Release build без предупреждений, format check, scanner `303` файлов, локализации `329/329`, `git diff --check` и smoke-запуск.
- Версия остаётся `0.0.46-dev`; модели не скачивались, установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — переориентация «Режима неопределенности» на AI-исполнителя

- Пользователь предоставил набросок ТЗ и разрешил свободно доработать его. Главная проблема подтверждена: ядро исследовало предметную тему, хотя должно было определить требования к будущей модели и инструментам.
- Активное ТЗ дополнено принципом: сценарий строит профиль AI-исполнителя и не проводит предметное интервью вместо рабочей модели.
- Добавлены capability profile, закрытые `decisionDimension`/`selectionImpact`, статусы измерений и `profileEffects` вариантов. Ответ теперь обязан структурированно менять требования к исполнителю.
- Программа требует сначала определить `task_type`, не позволяет без причины повторять/перезаписывать resolved-измерения, блокирует узкое предметное сужение и проверяет различие эффектов вариантов.
- Снимки состояния теперь включают capability profile; `Назад` откатывает профиль вместе с ответом.
- После повторного предметного нарушения orchestration принудительно формирует финал по известному профилю. Финальная карточка показывает профиль и поручает рабочей модели недостающие предметные вопросы.
- Добавлены события журналов для измерений, влияний, обновления профиля и предметного нарушения; UI-композиция не менялась.
- Живой Qwen3 8B прогон подтвердил новый JSON-контракт и правильную последовательность `task_type → input_modality → knowledge_freshness`. Полный финал с обязательными инструментами прошёл после двух repair и выбрал подтверждённый `ScienceOne-AI/S1-DeepResearch-32B`.
- Финальные проверки: restore, Debug/Release build без предупреждений, 36/36 тестов, format check, scanner `306` файлов, локализации `353/353`, `git diff --check` и smoke-запуск версии `0.0.47-dev`.
- Версия повышена до `0.0.47-dev`; накопительный каталог моделей пока не подключён к ядру, установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — локальный каталог подключён как инструмент ядра

- Создано отдельное ТЗ `ТЗ/2026-07-11_инструмент_локального_каталога_моделей.md` и backup `Backups/20260711_local_catalog_tool`.
- Добавлен structured tool `model_catalog_search`: ядро передаёт направление, тип задачи, capabilities, уровень нагрузки и лимит, а получает до шести проверяемых кандидатов.
- Каталог остаётся независимым сервисом: полный `catalog.json` не попадает в prompt, tool не синхронизирует базу и не выбирает победителя.
- Финальная оркестрация требует `inventory` и локальный каталог; `hf_find_model` принудителен только при пустой/недоступной локальной выдаче и доступен для добровольной актуальной проверки.
- Выбранный `repoId` обязан присутствовать в evidence одного из поисковых инструментов. Для balanced/extreme дополнительно проверяется подтверждённый каталогом размер строго больше 8B.
- В диагностический CatalogProbe добавлен офлайн-режим `local-search`. Живой локальный прогон по `text_knowledge,science_professional` вернул 6 разных кандидатов без издательского bias.
- Версия повышена до `0.0.48-dev`. Установщик и GitHub-публикация не выполнялись.
- Финальные проверки: 40/40 тестов, Debug/Release build без предупреждений, Release CatalogProbe build, format check, scanner `316` файлов, локализации `353/353`, `git diff --check` и smoke-запуск.

## 2026-07-11 — исправление финального repair по пользовательскому журналу

- Скриншот и свежий JSONL показали, что локальный каталог исправно вернул 3 кандидата 30–32B; проблема находилась после инструмента.
- Ядро дважды использовало близкие aliases закрытого контракта, затем роль `data_analyst`, после чего повторило запрещённую `Qwen3-Coder-30B-A3B-Instruct` в обеих repair-попытках.
- Добавлена консервативная нормализация однозначных aliases и память отклонённых executor repo на время генерации. Repair теперь буквально запрещает повторять конкретные отклонённые модели, но не выбирает замену за ядро.
- Политика семейства не ослаблялась: Qwen3 того же поколения по-прежнему запрещён, если это не значительно более новое поколение.
- Общая UI-ошибка уточнена: на экране остаётся предыдущий шаг, а проверку не прошёл следующий ответ.
- Версия повышена до `0.0.49-dev`.
- Проверки: 42/42 теста, Debug/Release build без предупреждений, format check, scanner `318` файлов, локализации `353/353`, `git diff --check` и smoke-запуск.
- Установщик не пересобирался, commit/push не выполнялись.

## 2026-07-12 — самостоятельное сужение задачи исполнителем и ролевые голоса

- После успешного запуска Qwen3.6 27B выявлено, что ядро передавало вымышленную «готовую цель», а естественный вопрос исполнителя UI ошибочно считал финальным результатом.
- Handoff разделён на `programFacts`, `userSignals`, `coreHypotheses`, `capabilityProfile` и `unknowns`; гипотезы ядра явно не являются постановкой пользователя.
- Исполнитель ведёт собственную долгоживущую цепочку уточнений с 2–6 вариантами и свободным ответом. Допустимы только явные статусы `clarification_step`, `final_result`, `cannot_continue`; некорректная проза проходит repair.
- Добавлены кнопки голоса и завершения сессии. Завершение отменяет генерацию и озвучку, останавливает runtime и пишет причину в executor JSONL.
- Установлены официальные SAPI-профили RHVoice `Elena` и `Bdl`; они закреплены за исполнителем «Режима неопределённости». `Aleksandr / Slt` остаются общими голосами ядра во всех сценариях.
- В правилах запрещено использовать любой из этих четырёх профилей для инструментов других сценариев без прямой просьбы пользователя.
- Версия повышена до `0.0.54-dev`. Новые AI-модели не скачивались; установщик не пересобирался, commit/push не выполнялись.

## 2026-07-12 — исправление executor pipeline после первого сквозного теста

- Пользовательский тест выявил отсутствие Matrix, нестабильный выбор модели и невозможность запуска скачанного исполнителя.
- Аудит подтвердил: Matrix-панель находилась на скрытой странице настроек; вместо основных 31B-весов был выбран 515-МБ `mtp-google_gemma-4-31B-it-Q8_0.gguf`; manifest считал файл runnable до реального запуска. `llama-server b9442` отклонил архитектуру `gemma4-assistant`.
- По прямой команде пользователя рабочая копия ошибочной загрузки и шесть её executor-журналов удалены после rollback-backup. Каталожные карточки Gemma и журналы выбора ядра сохранены как полезные данные.
- В `0.0.53-dev` resolver глобально сравнивает репозитории, отсекает auxiliary/split GGUF и неправдоподобные размеры. Installer читает `general.architecture`, а `installed/runnable` появляется только после настоящего запуска и health-check текущего `llama-server`; ошибка сохраняется как `runtime_incompatible`.
- Локальный каталог безопасно расширяет слишком узкое направление, runnable inventory признаётся evidence, live HF-поиск отбрасывает выдачу без подходящих файлов и использует широкий repository fallback. Программа не назначает победителя за ядро.
- Если полный GPU-offload корректной тяжёлой модели не поднимается, runtime автоматически повторяет запуск через CPU/RAM; это согласует запуск с ранее разрешённым hybrid-fit.
- Matrix перенесён в рабочую область сценария. Новые модели в тестах не скачивались; ожидается повторный пользовательский сквозной тест.
- Внутренние проверки: Release build без предупреждений, 72/72 теста, format check, scanner `207` файлов, локализации `393/393`, `git diff --check`, publish-smoke и контроль отсутствия удалённой модели/журналов. UI автоматически не запускался.

## 2026-07-12 — пользовательская проверка установщика 0.0.51-dev

- После закрытия голосового ТЗ пользователь самостоятельно собрал и проверил установщик актуальной версии AI HUB.
- По результату пользовательской проверки установка и работа сборки корректны; проблем не обнаружено.
- Это подтверждённый ручной smoke-тест установщика, а не автоматическая проверка Codex.

## 2026-07-12 — старт этапа модели-исполнителя

- Пользователь согласовал следующий сквозной этап песочницы: безопасно скачать отсутствующую модель-инструмент, удерживать тяжелый runtime загруженным, передать ему финальную карточку и получить результат.
- Контекст исполнителя отличается от ядра: накапливается примерно до 80% рабочей области, затем уплотняется без выгрузки модели.
- Для ядра и исполнителя согласована крупная Matrix-анимация, питаемая реальными символами потокового ответа; текстовые статусы сохраняются отдельно.
- Создано активное ТЗ `ТЗ/2026-07-12_загрузка_и_передача_задачи_модели_исполнителю.md`.
- По прямому указанию пользователя тесты не должны скачивать новые реальные модели.

## 2026-07-12 — реализация загрузки и передачи задачи исполнителю

- Добавлен разрешитель выбранной модели в самостоятельный GGUF с приоритетом `Q4_K_M`, поиском упаковки через Hugging Face API и запретом скрытой замены несвязанным семейством.
- Добавлен подтверждаемый installer: HTTPS, `.part`, продолжение, свободное место, размер, SHA-256 и `executor-model.json`. Автотесты используют только маленький fake payload.
- Установленные исполнители обнаруживаются discovery/inventory и не скачиваются повторно.
- Добавлена отдельная долгоживущая executor-сессия с web tools, бюджетом контекста 80%, контрольной точкой без выгрузки runtime, отдельным JSONL и связью с parent core session.
- Исполнитель может вернуть одно структурированное уточнение с кнопками и продолжить ту же сессию после ответа.
- `llama-server` получил SSE parser; реальные `delta.content` питают крупный Matrix-дождь ядра и исполнителя, полный ответ продолжает проходить обычную проверку.
- Живой внутренний probe на уже установленном Qwen3 8B подтвердил 4 streaming chunks и полный собранный ответ; новые модели не скачивались.
- Версия повышена до `0.0.52-dev`. Финальные проверки и пользовательский сквозной тест ещё не завершены.
- Финальные внутренние проверки: Release build без предупреждений, 68/68 тестов, format check, scanner `206` файлов, локализации `392/392`, `git diff --check`, self-contained publish smoke без `.gguf` и `.part`.
- Новые AI-модели не скачивались; UI не управлялся автоматически. Установщик не пересобирался, commit/push не выполнялись. ТЗ остается активным до пользовательской проверки.

## 2026-07-11 — активное ТЗ локального голоса ядра

- Пользователь уточнил назначение функции: короткие реплики ядра должны помогать тем, кому проще слушать, а постепенное раскрытие не должно позволять прочитать вопрос заметно раньше речи.
- Создано активное ТЗ `ТЗ/2026-07-11_локальный_голос_ядра_и_синхронизация_вопросов.md`.
- В первой интеграции произносятся только `coreThought`, затем `question` динамического шага «Режима неопределенности»; заголовки, варианты, кнопки, статус и итоговая карточка не озвучиваются.
- Голос ядра отделён от будущих реалистичных голосов моделей-инструментов. Для ядра выбран локальный компактный синтетический контур; голосовые исполнители будут отдельной будущей системой.
- Основной технический кандидат — eSpeak NG через native API с PCM и word events; WAV оставлен необязательной деталью/fallback.
- Зафиксированы пропуск речи, полное текстовое раскрытие при любой ошибке, блокировка вариантов только на время реплики, доступность, отмена поздних callback-ов, чистый publish smoke и лицензионный учёт runtime.
- На этом проходе изменена только документация; код, версия, runtime и локализации не менялись. Установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — голос ядра: eSpeak NG и альтернатива RHVoice

- Реализован общий голосовой контур ядра с последовательным чтением `coreThought` и `question`, синхронным раскрытием текста, отменой и безопасным полным раскрытием при ошибке.
- eSpeak NG остаётся движком по умолчанию и показывается пользователю как `Привет из 80-ых` (`Hello from the 80s`).
- По новой вводной добавлен необязательный RHVoice через Windows SAPI с профилями `Aleksandr` и `Slt`; в выборе он называется `Просто ИИ голос` (`Just an AI voice`).
- Если RHVoice не установлен или недоступен, маршрутизатор автоматически использует eSpeak NG. RHVoice и его установщики не включаются в обычный publish.
- Добавлены настройки движка, русский и английский словари, скрипты подготовки runtime, лицензионные записи и тесты маршрутизации.
- Проверки: Debug/Release без предупреждений, 62/62 теста, scanner `193` файлов, локализации `371/371`, RHVoice live probe на русском и английском с native word events, self-contained publish smoke.
- Пользователь уже подтвердил работу eSpeak. Активное ТЗ остаётся открытым до ручной проверки альтернативы RHVoice; установщик не собирался, commit/push не выполнялись.

## 2026-07-11 — закрытие ТЗ голоса ядра

- Пользователь вручную проверил альтернативный RHVoice и подтвердил, что голос прошёл тест; ТЗ признано выполненным.
- ТЗ `2026-07-11_локальный_голос_ядра_и_синхронизация_вопросов.md` перенесено в `ТЗ/Архив`; активных ТЗ не осталось.
- Перед закрытием создан backup `Backups/20260711_close_core_voice_spec`.
- По правилу закрытия выполняются финальные проверки, commit и push актуального состояния в `origin/main`.
- Установщик не пересобирается: пользовательская проверка голосов завершена, installer flow в ТЗ не изменялся.
- Финальные проверки закрытия: Release build без предупреждений, 62/62 теста, format check, scanner `193` файлов, локализации `371/371`, `git diff --check`, активных ТЗ `0`.
- Основной commit `d83e308` (`Add synchronized local core voices`) успешно отправлен в `origin/main`.

## 2026-07-11 — закрытие активных ТЗ версии 0.0.50-dev

- Пользователь подтвердил завершение всех активных ТЗ и дал команду закрыть их по регламенту.
- Создан backup `Backups/20260711_close_active_specs`.
- В `ТЗ/Архив` перенесены: первый боевой сценарий, парсер накопительного каталога Hugging Face и подключение каталога как инструмента ядра.
- В корне `ТЗ` остался только `README.md`; активных ТЗ нет.
- Визуальная переработка и подключение рабочей модели не входят в закрытые ТЗ и потребуют новых ТЗ.
- По правилу закрытия выполняется публикация всего актуального состояния проекта в `origin/main`.
- Установщик не пересобирается: закрытые ТЗ не изменяют installer flow и отдельной команды на его сборку не было.
- Scanner исключает папку `Тесты` с внешними диагностическими результатами: корректное испанское слово в сохранённой web-выдаче давало ложный mojibake-маркер; пользовательский файл не изменялся.
- Финальная проверка закрытия: Release build без предупреждений, 54/54 теста, scanner `178` исходных текстовых файлов, локализации `353/353`, `git diff --check`, ноль активных ТЗ.
- Актуальное состояние опубликовано в GitHub: commit `6571c12` (`Implement uncertainty scenario and model catalog`) успешно отправлен в `origin/main`.

## 2026-07-11 — PC-fit, metadata lineage и startup sync каталога

- Пользователь подтвердил ещё один успешный гибридный тест и поручил закрыть четыре оставшихся технических пункта активных ТЗ; визуал оставлен на потом.
- Создан backup `Backups/20260711_catalog_hardware_lineage_startup`, активные ТЗ актуализированы.
- Добавлен `ModelHardwareCompatibilityService`: оценка Q4 + 20% overhead, бюджеты RAM/VRAM по режиму и проверка свободного диска. Неподходящие кандидаты отсекаются tool-выдачей и повторно проверяются перед финалом.
- Lineage текущего ядра проверяется по `baseModels`, `modelType` и repo metadata; имя используется только при отсутствии каталожного evidence.
- Добавлена проверка непротиворечивости `needsWeb`, web tools и offline/no_external_data профиля.
- Добавлен `HuggingFaceCatalogStartupService`: фоновая синхронизация раз в 24 часа, process lock, журналирование и сохранение прежней базы при полном сетевом отказе.
- Живой локальный probe на паспорте 128 GB RAM / 24 GB VRAM: 32B кандидаты проходят как GPU-fit; из extreme text-кандидатов 5 слишком больших исключены, GPT-OSS 120B проходит как hybrid-fit.
- Версия повышена до `0.0.50-dev`.
- Финальные проверки: 54/54 теста, Debug/Release и CatalogProbe Release build без предупреждений, format check, scanner `324` файлов, локализации `353/353`, bundled seed, `git diff --check` и smoke-запуск.
- Core JSONL smoke-сессии зафиксировал `catalog_startup_sync_started` и `catalog_startup_sync_finished: skipped_fresh` без блокировки UI.
- Установщик не пересобирался, commit/push не выполнялись.
## 2026-07-14 — два кандидата исполнителя и проверка Defender

- В `0.0.55-dev` финальная карточка «Режима неопределённости» требует ровно две разные модели: одну установленную и подтверждённую runtime inventory, вторую отсутствующую и подтверждённую каталогом или Hugging Face.
- Для обеих моделей ядро возвращает преимущество, ограничение и причину; ровно один вариант отмечается как предпочтительный, но подготовка начинается только после явного выбора пользователя.
- Выбранная карточка обновляет модель, роль, capability class и причину, которые получает resolver и будущий handoff исполнителя.
- Repair исправлен для парного выбора: ошибка одного кандидата больше не заносит в запрет одновременно второй подтверждённый вариант.
- Диагностическая PowerShell-команда Codex с `Assembly.LoadFrom` и reflection `Invoke` была эвристически заблокирована Defender как `Trojan:Win32/Steanoz.Z!MTB`. Запись относится к командной строке, а не к AIHub-файлу; `DidThreatExecute=False`, угроза неактивна. Повторная целевая проверка папки Debug-сборки и официальных RHVoice installers угроз не нашла. Динамическая reflection-диагностика больше не используется.
- Финальные внутренние проверки: Debug/Release без предупреждений, 79/79 тестов, format check, scanner `211` файлов, локализации `415/415`, `git diff --check` и publish-smoke из `455` файлов без `.gguf`/`.part`.
- Новые AI-модели не скачивались, UI автоматически не управлялся, установщик не пересобирался; commit/push были отложены до пользовательской проверки.

## 2026-07-14 — доверенный пул исполнителей

- Живой журнал показал, что каталог корректно находил `gpt-oss-20b`, `gemma-4-31B-it` и `Qwen3.6-27B`, но ядро ошибалось при повторном переносе статусов и могло выбрать две упаковки одного веса.
- В `0.0.56-dev` inventory, каталог, live fallback, семейство и совместимость с ПК обрабатывает программа в `ChoiceExecutorCandidatePoolService`.
- Ядро получает только проверенные `installed_*` и `alternative_*` ID, выбирает по capability profile и описывает плюсы/ограничения.
- Установленная модель может быть рекомендована, если она достаточна; загружаемая альтернатива всё равно показывается и обязана быть из другого семейства.
- Модельные structured tools больше не доступны ядру этого сценария; точные повторные web-вызовы блокируются.
- Внутренние проверки: Release build без предупреждений и 84/84 unit tests.

## 2026-07-14 — закрытие ТЗ модели-исполнителя

- Пользователь подтвердил успешный сквозной тест `0.0.56-dev`: доверенный пул сформировал установленную Qwen3.6 27B и альтернативу GPT-OSS 20B другого семейства.
- Ядро обоснованно предпочло установленный вариант, выбор перешёл в отдельную сессию исполнителя, модель запустилась и вернула `final_result`.
- Пользователь отдельно отметил существенное ускорение и улучшение стабильности после переноса технической проверки кандидатов из ядра в программу.
- Активное ТЗ признано выполненным и перенесено в архив с обязательной публикацией текущего состояния в `origin/main`.
- Нынешнее отображение финального результата исполнителя не считается целевым UX. Это осознанно вынесено за границы закрываемого ТЗ и записано следующим отдельным направлением.
- Закрывающие проверки: Release build без предупреждений, 84/84 теста, format check, scanner 214 файлов, локализации 415/415, `git diff --check`, активных ТЗ 0 и publish-smoke без `.gguf`/`.part`.
- Установщик не пересобирался по правилу проекта.
- Основной commit закрытия `f22af34` (`Complete executor handoff workflow`) успешно отправлен в `origin/main`; отдельный документационный commit фиксирует сам факт публикации.

## 2026-07-23 — многоэтапная сессия исполнителя

- Анализ последнего executor JSONL подтвердил, что Qwen3.6 27B действительно запускалась отдельным `llama-server`, но программа приняла служебную строку `final_result` за готовую работу.
- Пользователь закрепил новую границу: ядро быстро подбирает исполнителя, а предметная работа может продолжаться минуты или часы и завершается только вручную.
- В `0.0.57-dev` добавлены четыре пользовательских этапа: уточнение задачи, выбор способа, сбор данных и сборка результата. Экспорт зарезервирован для будущего ТЗ.
- Исполнитель больше не может закрыть сессию ответом. `result_ready` означает только текущую версию, после которой доступны варианты, свободный ввод и новые версии.
- Контракт использует `working / stage_ready / result_ready / blocked`; фиктивные метки и обещания выполнить работу отклоняются и получают до двух repair-попыток в той же сессии.
- `result_ready` программно запрещён до этапа сборки результата, а переходы выполняются только кнопками пользователя и пишутся в executor JSONL вместе со сводкой этапа.
- Контекст сохраняет текущий этап при 80-процентном сжатии. Кнопка завершения остаётся единственным терминальным действием.
- Внутренние проверки: Debug/Release без предупреждений, 88/88 тестов, format, scanner, локализации 433/433, `git diff --check`, publish smoke 922 файла без моделей.
- Новые AI-модели не скачивались, UI автоматически не управлялся, установщик не пересобирался. Активное ТЗ ожидает пользовательский сквозной тест.

## 2026-07-23 — закрытие ТЗ многоэтапной сессии исполнителя

- Пользователь провёл ручной тест и подтвердил закрытие ТЗ. Поведение оценено как не полное, но ожидаемое для текущего этапа песочницы.
- ТЗ `2026-07-23_многоэтапная_сессия_модели_исполнителя.md` принято и переносится в `ТЗ\Архив`; активных ТЗ после закрытия нет.
- Дальнейшая настройка поведения исполнителя, визуальная переработка, работа с файлами и экспорт остаются отдельными будущими задачами.
- Перед закрывающими изменениями создан path-safe backup `H:\AI_HUB\Backups\20260723_close_staged_executor_session` из 20 файлов.
- По обязательному регламенту закрытия выполняются финальные проверки, commit и push в `origin/main`.
- Новые AI-модели не скачивались. Установщик не пересобирается по правилу проекта: отдельной команды не было, installer flow не изменялся.
- Финальные проверки закрытия: restore, Debug/Release без предупреждений, `88/88` тестов, format check, scanner `216` файлов, локализации `433/433`, publish-smoke `922` файла без `.gguf` и `.part`.
- Основной commit закрытия `8f2bd3f` (`Add staged executor sessions`) успешно отправлен в `origin/main`.
- Перед записью факта публикации создан дополнительный backup `H:\AI_HUB\Backups\20260723_close_staged_executor_publish_record`; эта запись публикуется отдельным документационным commit.

## 2026-07-23 — автономная рабочая фаза исполнителя

- Последний executor JSONL показал протокольный тупик: модель вернула `stage_ready` и предложила текстовый вариант перехода, но программа передала выбор как обычный ответ и одновременно запретила модели менять этап.
- Пользователь согласовал отдельное подтверждение постановки и самостоятельную работу исполнителя после него. Вмешательство пользователя требуется только при критической неопределённости, разрешении или готовом результате.
- В автономной фазе добавляется команда `Выдать результат`: она создаёт версионный снимок текущей работы в отдельном немодальном окне, не завершает сессию и не очищает контекст.
- Создано активное ТЗ и backup `H:\AI_HUB\Backups\20260723_executor_autonomy_snapshots`.
- Реализация завершена в `0.0.58-dev`: введены действия `ask_user / confirm_brief / continue_work / request_tool / present_result / blocked`, фиксированное подтверждение постановки, автоматический цикл той же модели и безопасное динамическое подключение web-инструментов.
- Кнопка `Выдать результат` создаёт полную версию Markdown-документа и открывает её в отдельном немодальном окне. Снимки сохраняются в памяти активной сессии и executor JSONL.
- UI автоматически не тестируется, модели не скачиваются. Активное ТЗ остаётся открытым до пользовательского сквозного теста.
- Первый ручной тест `0.0.58-dev` дошёл до `solution_method`, но Qwen3.6 27B повторила action `confirm_brief` при готовности перейти к `data_collection`. Repair дважды повторил ошибку, а UI остался на первом этапе при уже продвинутой внутренней сессии.
- Протокол укреплён: повторное подтверждение после первого этапа нормализуется в `continue_work`, автономные исключения возвращают безопасную паузу с фактическим stage, а кнопка результата скрыта до визуального выхода из `task_definition`.
- Повторные внутренние проверки: Debug/Release без предупреждений, `94/94` теста, format и scanner. Требуется новый ручной сквозной тест.

## 2026-07-23 — двухэтапная работа исполнителя

- Пользователь заменил автоматическую четырёхэтапную схему на техническое уточнение и продолжительный практический диалог.
- В практической фазе основной вопрос находится слева, а справа показывается краткий пересказ доступного результата на текущий момент. Ответы закреплены в нижней части рабочего пространства.
- Исполнитель больше не готовит полный результат самостоятельно: отдельный Markdown-документ создаётся только ручной командой, а сессия после этого продолжается.
- Контракт `0.0.59-dev` использует только этапы `task_definition / practical_clarification`, требует `currentResultSummary` и исключает автоматические `continue_work / present_result / result_ready`.
- Добавлена нормализация противоречий handoff: явная работа без интернета выключает web и удаляет web-инструменты до запуска исполнителя.
- Внутренние проверки прошли: Debug/Release без предупреждений, `92/92` теста, format, scanner `280` файлов, локализации `447/447`, `git diff --check`, publish smoke `922` файла без моделей.
- Новые модели не скачивались, UI автоматически не управлялся, установщик не пересобирался. Активное ТЗ ожидает ручной сквозной тест.

## 2026-07-23 — исправление наложения executor UI и глобальный Matrix

- Ручной тест `0.0.59-dev` подтвердил логику двухэтапного исполнителя, но показал наложение нового workspace поверх старой карточки сценария.
- Причина: код скрывал Viewbox страницы настроек, тогда как настоящий Viewbox старого сценария не имел имени и оставался видимым в той же строке Grid.
- В `0.0.60-dev` старый экран подготовки и workspace исполнителя разделены. Первый этап центрируется как одна колонка, второй использует левую и правую панели, а общая панель ответов закреплена снизу.
- `Свой вариант` добавляется в ту же адаптивную коллекцию, что и остальные ответы, поэтому кнопки больше не используют две конкурирующие сетки.
- Matrix-дождь вынесен на корневой слой всей клиентской области: отдельная чёрная подложка имеет прозрачность 50%, непрозрачные символы рисуются поверх неё.
- Проверки прошли: Debug/Release без предупреждений, `92/92` теста в обеих конфигурациях, format, scanner `236` файлов, локализации `447/447`, `git diff --check`, publish smoke `922` файла без моделей.
- Новые модели не скачивались, UI автоматически не управлялся, установщик не пересобирался. Активное ТЗ ожидает ручную визуальную проверку.

## 2026-07-23 — принятие и закрытие двухэтапного исполнителя

- Пользователь подтвердил, что логика и визуальная компоновка `0.0.60-dev` работают ожидаемо. Активное ТЗ принято и переносится в архив.
- Закрытие выполняется без публикации по прямому указанию пользователя: локальный коммит создаётся, `push` в GitHub не выполняется.
- Последний executor JSONL `2026-07-23_16-07-02_executor_89649032c6ee4c0b97a80acc01a4d47c.jsonl` выявил отдельное семантическое несоответствие.
- В практической фазе модель обновляла `currentResultSummary` метаформулировками `Формируется обзор` и накапливала требования, структуру, тон и ограничения. При ручном snapshot она создала техническое задание, а не саму статью.
- Snapshot-промт запрещает заменять документ планом, но практический промт одновременно запрещает готовить полный документ и не требует постепенно строить содержательный ответ. Поэтому контекст к моменту snapshot состоит преимущественно из постановки.
- По текущей просьбе выполнен только анализ. Исправление промтов и контракта остаётся отдельной будущей задачей.
- Финальные проверки закрытия прошли; основной локальный commit `f101fbd` создан в `main`.
- `Push` и публикация не выполнялись по прямому указанию пользователя. После commit в рабочем дереве остался только незатронутый пользовательский файл `Данные_для_внедрения\Фото\I want to believe llm.png`.

## 2026-07-23 — дерево сессии и содержательный результат исполнителя

- Пользователь согласовал программное дерево, похожее на семейное: корень задачи, требования, решения, знания, фрагменты результата, вопросы, предположения и источники.
- Дерево хранится отдельно от памяти модели. Кнопки ответов создают активную ветвь и неактивные альтернативы, а модель получает только компактный активный срез.
- В шапке рядом с настройками добавлена кнопка только со знаком дерева. Она открывает немодальное живое окно с ростом ветвей, масштабированием, перетаскиванием, обзором, подробностями и сворачиванием.
- Практический контракт разделён на технический `stageSummary`, фактический `workingResultFragment` и короткий `currentResultSummary`.
- Метаописание результата программно заменяется фактическим фрагментом, а непрошенное техническое задание в Markdown-снимке вызывает отдельный семантический repair.
- Версия повышена до `0.0.61-dev`. Активное ТЗ остаётся открытым до ручного теста пользователя.
- Новые модели не скачиваются, UI автоматически не управляется, установщик не пересобирается, пользовательский файл изображения не изменяется.

## 2026-07-23 — ручная финализация executor-сессии и DOCX

- Создано и реализовано активное ТЗ `ТЗ/2026-07-23_ручная_финализация_и_экспорт_DOCX.md`.
- Executor-контракт получил нетерминальное действие `suggest_finalization` и обязательную краткую причину `completionReason`. Сигнал допускается только в подтверждённой практической фазе при наличии содержательного результата и отсутствии критических пробелов.
- Модельные варианты с командами завершения, сохранения, экспорта или показа результата отклоняются семантической политикой и проходят контрактный repair.
- При рекомендации готовности кнопка завершения плавно переезжает из верхней панели к ответам и получает короткий акцент. Пользователь может продолжить уточнение или добавить свой вариант; автоматического завершения нет.
- Диалог завершения предлагает закрыть сессию без результата либо сформировать итог через прямой вывод в AI HUB или экспорт в DOCX.
- Полный итог формируется отдельным запросом `[AI_HUB_FINAL_RESULT_REQUEST]`. Экран и DOCX используют один экземпляр Markdown; сессия закрывается только после успешного показа или записи файла.
- При отмене выбора пути, ошибке генерации или экспорта сессия остаётся активной. Предварительная кнопка `Выдать результат` сохраняет прежнее нетерминальное поведение.
- Добавлена runtime-зависимость Microsoft Open XML SDK `DocumentFormat.OpenXml 3.5.1` (MIT), сведения внесены в `REESTR.md` и `THIRD_PARTY_NOTICES.md`; проверка NuGet не выявила известных уязвимостей.
- Версия повышена до `0.0.62-dev`. Новые AI-модели не скачивались, UI автоматически не управлялся, установщик не собирался.
- Внутренние проверки: Debug/Release build без предупреждений, Debug/Release tests `99/99`, DOCX package test, format check, UTF-8 scanner `241` файл, локализации `491/491`, `git diff --check`, publish smoke `457` файлов с обеими OpenXML DLL и без `.gguf`/`.part`.
- ТЗ остаётся активным до ручного теста пользователя; commit и push не выполнялись.

## 2026-07-23 — исправление автоматической готовности завершения

- Пользователь подтвердил, что диалог завершения и DOCX-экспорт работают, но кнопка завершения не получила рекомендованное состояние.
- Последний executor-журнал показал, что интерфейс не терял событие: модель вернула `ask_user`, одновременно написала `Исследование завершено`, оставила `missingCriticalInputs` пустым и предложила только необязательный практический раздел.
- Причиной оказался слишком жёсткий двоичный контракт: исполнитель мог либо продолжать вопросами, либо отказаться от всех вопросов через `suggest_finalization`.
- В `0.0.63-dev` добавлен независимый обязательный признак `canFinalize`. Он разрешает сохранить обычный необязательный вопрос, но уже перенести и акцентировать кнопку завершения, если текущий результат самодостаточен.
- `suggest_finalization` оставлен для более узкого случая, когда полезных дополнительных вопросов уже нет.
- Готовность проверяется программой: подтверждён практический этап, есть содержательный фрагмент и пересказ, отсутствуют критические пробелы, указана причина готовности.
- Диалог завершения, финальная генерация и DOCX не менялись. Новые модели не скачивались, UI автоматически не управлялся, установщик не собирался.
- Внутренние проверки: Debug/Release build без предупреждений, Debug/Release tests `101/101`, format check, UTF-8 scanner `248` файлов, локализации `491/491`, `git diff --check`.
- Активное ТЗ обновлено и остаётся открытым до повторной ручной проверки кнопки.

## 2026-07-23 — принятие и архивирование дерева и ручной финализации

- Пользователь подтвердил выполнение всех активных ТЗ.
- `2026-07-23_дерево_сессии_и_содержательный_результат.md` и `2026-07-23_ручная_финализация_и_экспорт_DOCX.md` помечены принятыми и перенесены в `ТЗ\Архив`.
- В `ТЗ\README.md` активных задач больше нет; проект остаётся на версии `0.0.63-dev`.
- Закрывающие проверки прошли: Debug/Release build без предупреждений, Debug/Release tests `101/101`, format check, UTF-8 scanner `248` файлов, локализации `491/491`, `git diff --check`, проверка NuGet без известных уязвимостей.
- Self-contained publish smoke содержит `924` файла, `AIHub.exe`, обе Open XML DLL и не содержит `.gguf` либо `.part`.
- Новые модели не скачивались, UI автоматически не управлялся, установщик не собирался.
- Закрывающее состояние публикуется в `origin/main` по штатному правилу архивации ТЗ.
