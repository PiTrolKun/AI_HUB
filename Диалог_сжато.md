# Диалог сжато — AI_HUB

Дата фиксации: 2026-05-30

## Краткая история

Пользователь начал новый проект AI_HUB и передал итоговую концепцию русскоязычного локального AI-хаба для Windows 11.

После этого были добавлены инструкции для Codex в папку `Инструкции`:

- `AGENTS.md`
- `CODEX.md`

Codex прочитал инструкции и зафиксировал важные правила работы:

- пользователь не программист;
- перед новой задачей нужно сверять актуальные инструкции и контекст;
- перед крупной реализацией нужен ТЗ-файл;
- перед изменением существующих файлов нужен backup;
- проект ориентирован на C# / .NET / WPF;
- проверяемость и понятный откат важны с самого начала.

## Текущая задача

Создать полный стартовый скелет проекта:

- служебные документы;
- документацию проекта;
- папки для ТЗ, backup, исходников, тестов и инструментов;
- runtime-папки для моделей, результатов, backends, кэша, логов и RAG;
- README-файлы в важных папках.

## Ближайший следующий шаг после этой задачи

Подготовить ТЗ на первый MVP: WPF-каркас приложения в стиле проводника.

## 2026-05-30 — подготовка .NET/WPF окружения

Пользователь спросил, согласен ли Codex с языком программирования из идеи, и попросил проверить зависимости.

Codex подтвердил, что `C# / .NET / WPF` подходит проекту, потому что нужен нативный Windows 11 интерфейс в стиле проводника и глубокая работа с Windows.

Проверка показала:

- `git` установлен;
- `winget` установлен;
- PowerShell 7 установлен;
- .NET runtime 8 был установлен;
- .NET SDK отсутствовал.

Установлен Microsoft .NET SDK 10.0.300 через winget. После установки проверено, что WPF-шаблон доступен, а временный WPF-проект на `net10.0-windows` успешно собирается.

## 2026-05-30 — регистрация GitHub-репозитория

Пользователь создал GitHub-репозиторий:

`https://github.com/PiTrolKun/AI_HUB`

Codex начал подготовку локальной папки `H:\AI_HUB` к первой публикации:

- проверил, что папка ещё не является Git-репозиторием;
- проверил, что удалённый репозиторий пустой;
- установил GitHub CLI 2.93.0 через winget;
- обнаружил, что `gh` пока не авторизован;
- начал подготовку `.gitignore`, чтобы не публиковать backup-и и runtime-данные.

После подтверждения пользователя GitHub CLI успешно авторизован под аккаунтом `PiTrolKun`.

Выполнено:

- инициализирован Git-репозиторий в `H:\AI_HUB`;
- создана ветка `main`;
- подключён remote `origin` на `https://github.com/PiTrolKun/AI_HUB.git`;
- создан первый коммит `9a5e76c Initial project skeleton`;
- коммит отправлен в `origin/main`;
- GitHub подтвердил репозиторий `PiTrolKun/AI_HUB`, видимость `PRIVATE`, default branch `main`.

## 2026-05-30 — правила версий, архивации ТЗ и истории

Пользователь попросил добавить в правила строгую нумерацию версий и обязательную публикацию актуального состояния проекта в GitHub при переносе ТЗ в архив.

Codex обновил `AGENTS.md` и `CODEX.md`, добавил правило версионирования и правило GitHub-публикации при архивации ТЗ.

ТЗ подготовки .NET/WPF окружения перенесено в архив. По прямому указанию пользователя повторный push в GitHub не выполнялся, потому что заготовка проекта была опубликована прямо перед этим.

После замечания пользователя выявлено, что запись о действии не была добавлена в историю сразу. Добавляется новое правило: после каждого выполненного действия делать краткую запись в истории проекта, с датой, сутью действия, проверками, commit/push и исключениями из правил.

## 2026-05-30 — стартовый визуальный прототип

Пользователь сказал, что пока нет чёткой картины визуала, и попросил сделать реальное окно, чтобы посмотреть вживую.

Codex начал задачу прототипа стартового WPF-окна: создаётся ТЗ, затем минимальное приложение в `Исходники`, без каталога моделей и без реальных действий с диском.

Создан WPF-проект `Исходники/AIHub`. Стартовое окно заменено на прототип первого запуска: приветствие, объяснение локального хранения, три шага настройки и кнопки `Начать настройку` / `Позже`.

Проверки: `dotnet restore`, `dotnet build`, scanner кириллицы. Сборка прошла без ошибок и предупреждений. Приложение запущено видимым окном для оценки.

Пользователь сказал, что заготовка нравится и воспринимается как "семечка древа", затем попросил добавить переключение тёмной и светлой темы. Codex добавил кнопку `Тёмная тема` / `Светлая тема` в правый верхний угол стартового окна. Переключение работает без перезапуска окна.

Пользователь сообщил, что окно, похоже, сразу закрывается, и попросил проверить запуск, а также добавить автозапуск без необходимости просить Codex. Проверка Windows Event Log подтвердила падение `AIHub.exe`: `XamlParseException`, ресурс `WindowBackgroundBrush` искался до объявления ресурсов окна.

Codex исправил старт окна, добавил `Запустить_AI_HUB.cmd` и `start-aihub.ps1` для запуска dev-версии двойным кликом и проверил, что после старта процесс не завершается сразу.

При проверке обнаружилась ещё одна проблема: старый способ переключения темы мог падать из-за read-only кистей WPF. Codex перевёл кисти темы на `DynamicResource`, изменил замену кистей в code-behind и проверил клик по кнопке темы через Windows UI Automation: процесс остался активен.

Пользователь подтвердил, что ТЗ стартового окна закрыто, и попросил пока не отправлять изменения в GitHub. Codex перенёс ТЗ в архив и зафиксировал исключение: push при этом архивировании не выполняется по прямому текущему указанию пользователя.

Пользователь выбрал первую dev-версию заготовки: `0.0.1-dev`. Codex зафиксировал её как текущую версию проекта в корневом файле `VERSION`, в свойствах WPF-проекта и в истории.

После запуска `Запустить_AI_HUB.cmd` пользователь получил ошибку `CS5001`: программа не содержит статического метода `Main`. Codex воспроизвёл проблему и нашёл причину: `start-aihub.ps1` рекурсивно находил backup-копию `AIHub.csproj` в `Backups`, где нет полного WPF-проекта. Запускатор исправлен на точный путь `Исходники/AIHub/AIHub.csproj`.

Пользователь попросил показать версию в строке окна и не создавать несколько источников истины. Codex добавил автоматическое чтение версии из `AssemblyInformationalVersion`: строка окна теперь берёт версию из свойств сборки WPF-проекта.

Пользователь попросил закрепить это как правило на будущее. Codex добавил в `AGENTS.md` и `CODEX.md` правило одного источника версии: будущие UI-элементы, About, установщик, updater, release notes, диагностика и модули должны читать версию из основного источника проекта.

Пользователь уточнил, что версию нужно показывать в системном заголовке окна, где было `AI_HUB`, и попросил убрать подчёркивание. Codex перенёс автоматическую версию в `Window.Title`: заголовок формируется как `AI HUB 0.0.1-dev`, нижняя строка снова показывает только статус.

Пользователь попросил закрыть ТЗ по правилам. Codex проверил, что активных ТЗ вне архива нет, и начал публикацию накопленного актуального состояния проекта в GitHub с проверками и записью в историю.

Проверки перед публикацией прошли: `dotnet build`, запуск через `Запустить_AI_HUB.cmd`, scanner кириллицы. После этого Codex отправил `main` в GitHub до коммита `0281b34`.

Пользователь передал файл логотипа `Данные_для_внедрения/Фото/logo_crt_black_hole_matrix_tight_transparent_1x1.png` и попросил добавить его в программу, чтобы он отображался в заголовке, диспетчере задач, exe и будущих ярлыках установщика. Codex скопировал PNG в assets приложения, создал `AppIcon.ico` и подключил его к WPF-проекту.

Так как проект уже перешёл на строгую dev-нумерацию, Codex повысил версию с `0.0.1-dev` до `0.0.2-dev` для изменения с логотипом.

Проверка логотипа прошла: проект собирается, запускатор открывает окно `AI HUB 0.0.2-dev`, а из `AIHub.exe` извлекается связанный значок.

Пользователь попросил убрать надпись `Первый запуск`, прижать переключатель темы к правому краю и заменить текст кнопки на значки луны/солнца с подсказкой при наведении. Codex сделал кнопку-иконку: в светлой теме показана луна для перехода в тёмную, в тёмной теме солнце для перехода в светлую. Версия повышена до `0.0.3-dev`.

Пользователь уточнил, что нужно сделать цветным именно солнце на кнопке темы. Codex задал солнцу в тёмной теме тёпло-жёлтый цвет `#FBBF24` и повысил версию до `0.0.4-dev`.

Пользователь показал, что системный заголовок AI HUB остаётся светлым при тёмной теме Windows, и попросил идти лёгким путём: брать от Windows всё возможное. Codex создал ТЗ `ТЗ/2026-05-30_системная_темная_рамка_windows.md`, добавил чтение темы Windows для приложений при запуске и синхронизацию системного заголовка через DWM API. Версия повышена до `0.0.5-dev`.

Пользователь подтвердил, что ТЗ системной тёмной рамки выполнено. Codex переносит ТЗ в архив и публикует актуальное состояние проекта в GitHub по правилу архивации ТЗ.

Пользователь предложил следующую логику стартового окна: `Позже` заменить на `Перенастроить`, а `Начать настройку` и `Перенастроить` считать одним сценарием. Текущие кнопки нужно оживить, но пока они должны вести только к заготовке новой страницы настройки. Codex сохранил это как рабочее ТЗ `ТЗ/2026-05-30_логика_стартового_окна_и_паспорт_пк.md`.

Codex начал реализацию ТЗ: добавил JSON-состояние и паспорт ПК в `%LOCALAPPDATA%/AI_HUB`, заменил `Позже` на `Перенастроить`, оживил обе кнопки и сделал страницу настройки внутри основного окна. По замечанию пользователя отдельное окно настройки было заменено на страницу, добавлена рабочая кнопка `Назад`. Версия повышена до `0.0.6-dev`.

Пользователь уточнил, что основные данные паспорта ПК должны быть видны на главной странице в пункте 3, потому что на странице настройки этих данных не будет. Codex оставляет старый текст пункта 3 как стартовый текст до завершения сканирования, после скана заменяет его на сводку CPU/RAM/GPU/VRAM/диски и добавляет чтение GPU/VRAM через стандартный реестр Windows. Версия повышается до `0.0.7-dev`.

Пользователь заметил, что текст кнопки `Перенастроить` немного не влезает в рамку, и сказал, что после этой мини-правки ТЗ можно считать выполненным. Codex расширяет кнопку, повышает версию до `0.0.8-dev`, переносит ТЗ в архив и публикует актуальное состояние проекта в GitHub по правилу архивации ТЗ.

Пользователь начал новое ТЗ: реализовать настройки `Хранилище моделей` и `Папка результатов`. Согласовано, что обе настройки работают одинаково: несколько адресов хранения, добавление через выбор папки Windows или ручной ввод, порядок адресов с первым адресом по умолчанию, лимиты в ГБ, временное превышение в ГБ и отображение выбранных адресов/лимитов на главной странице. Codex сохранил это как ТЗ `ТЗ/2026-05-31_настройки_хранилищ_моделей_и_результатов.md`.

Codex реализовал ТЗ настроек хранения: добавлены модели и сервис `storage-settings.json`, на странице настройки появились секции `Хранилище моделей` и `Папка результатов` с ручным вводом, выбором папки Windows, несколькими адресами, лимитами, временным превышением и порядком `Вверх`/`Вниз`. Главная страница показывает сохранённые адреса и лимиты. Версия повышена до `0.0.9-dev`.

Пользователь заметил, что поле `лимит этого адреса` понятно только через tooltip, и попросил видимую подпись над зоной ввода. Codex добавляет подписи `Адрес хранения` и `Лимит адреса, ГБ`, фиксирует правило: общий лимит и лимиты адресов могут отличаться, но в будущей реальной логике будет работать более строгая фактическая граница. Версия повышена до `0.0.10-dev`.

Пользователь заметил, что забыта логика главной кнопки: после завершения настройки она должна меняться с `Начать настройку` на `Начать работу`. Codex добавляет завершение настройки после сохранения хотя бы одного адреса моделей и одного адреса результатов; пока рабочий режим не реализован, `Начать работу` показывает статус-заглушку. Версия повышена до `0.0.11-dev`.

Пользователь нашёл разбег текста с реальностью: кнопка уже показывала `Начать работу`, а нижняя строка всё ещё писала, что настройка не завершена. Codex синхронизирует статусную строку с тем же состоянием настройки, что управляет главной кнопкой, и повышает версию до `0.0.12-dev`.

Пользователь выбрал рекомендованный вариант установщика: Inno Setup. Согласовано, что тестовые установщики будут сохраняться в `H:\AI_HUB\Тесты`, в отдельной папке, а запуск сборки должен быть доступен простым двойным кликом через `.cmd`-обводку над PowerShell-скриптом. Codex создал папку `Тесты\Установщики` с README.

Пользователь сказал `Делай`, затем отдельно подтвердил `Установи`. Codex создал ТЗ тестового установщика, установил Inno Setup 6.7.3 через `winget`, добавил `Собрать_установщик_AI_HUB.cmd`, `Инструменты\build-installer.ps1` и `Инструменты\Installer\AI_HUB.iss`. Сборщик делает self-contained publish `win-x64` и кладёт установщик в `Тесты\Установщики`. `.cmd`-обводка проверена через `cmd.exe`; создан `AI_HUB_Setup_0.0.13-dev.exe`. Версия повышена до `0.0.13-dev`.

Пользователь заметил, что тестовый установщик не ставится в `C:\Program Files`, а ставится в `%LOCALAPPDATA%\Programs`. Codex объяснил, что это ожидаемо для dev/test-сборки без прав администратора. По просьбе пользователя в расширенные правила добавлена пометка: перед публичным релизом нужно отдельно пересмотреть установку в `C:\Program Files\AI HUB`, режим `для всех пользователей`, права администратора, uninstall и миграцию настроек.

Пользователь подтвердил, что все ТЗ решены, включая забытое. Codex закрывает и переносит в архив два активных ТЗ: настройки хранилищ моделей/результатов и тестовый установщик Inno Setup. По правилу архивации после проверок публикуется актуальное состояние проекта в GitHub.

Проверки перед публикацией прошли: `dotnet build`, scanner кириллицы, отсутствие активных ТЗ вне архива, пересборка Inno Setup установщика через `build-installer.ps1 -SkipPublish`. Codex сделал коммит `b19c002 Archive tasks and add installer tooling` и отправил `main` в GitHub.

Пользователь предложил внедрить простую файловую локализацию, пока программа не стала монолитной: русский основной, английский ведётся параллельно, будущие языки можно добавлять вручную JSON-файлом. Codex создал ТЗ `2026-05-31_локализация_и_общие_настройки.md`, добавил `ru.json` и `en.json`, сервис локализации, `settings.json`, кнопку настроек с шестерёнкой и страницу общих настроек с выбором языка. Версия повышена до `0.0.14-dev`. Добавлено правило: перед закрытием ТЗ проверять и пополнять английскую базу локализации.

Пользователь подтвердил, что ТЗ локализации и общих настроек выполнено. Codex переносит ТЗ в архив, проверяет сборку, scanner кириллицы, совпадение ключей русской и английской локализации и готовит публикацию актуального состояния проекта в GitHub.

Публикация выполнена: `main` отправлен в GitHub коммитом `b28fcd2 Archive localization settings task`.

Пользователь предложил следующий слой: страница `Начало работы`, которая открывается кнопкой `Начать работу` внутри основного окна. Согласовано, что пока добавляется только один режим `Рассуждение / изучение`, а `Ранее начатое` будет сворачиваемым блоком, изначально закрытым, с примером будущей записи. Codex сохранил ТЗ и реализовал страницу, заглушку выбора режима, кнопку `Назад`, русскую/английскую локализацию и повысил версию до `0.0.15-dev`.

Пользователь подтвердил, что ТЗ страницы начала работы выполнено. Codex переносит ТЗ в архив, проверяет сборку, scanner кириллицы, совпадение ключей локализации и готовит публикацию актуального состояния проекта в GitHub.

После timeout/дисконнекта на ожидании сборки установщика пользователь уточнил, что установщик собрался, и попросил закрепить правило: установщик собирать только по его команде, если тест установленной версии не критичен. Codex добавил правило в `AGENTS.md` и `CODEX.md`.

Публикация выполнена: `main` отправлен в GitHub коммитом `8dc7c64 Archive work start page task`.

После дисконнекта пользователь уточнил, что, вероятно, всё сделано кроме записи. Codex проверил состояние: рабочее дерево чистое, `main` синхронизирован с `origin/main`, коммит записи `884fc73 Record work start task publish` опубликован. Добавляется контрольная запись в историю.

Пользователь уточнил подход к основному ИИ-ядру: для базового ядра важна комфортная работа на среднем игровом ПК, а не максимальная мощность на уникальном ПК. Выбран вариант `Qwen3 8B Q4_K_M`. Codex сохранил ТЗ `2026-05-31_менеджер_основного_ядра_qwen3_8b.md`: реальное скачивание модели через AI HUB, подтверждение после сохранения настроек, проверка при запуске, нижняя панель прогресса, manifest, hash-проверка и запрет включать модель в установщик.

Пользователь уточнил: скачиваем всё необходимое, включая саму модель, и делаем максимально финально. Codex реализовал менеджер основного ядра: проверка папки моделей при запуске и после сохранения настроек, prompt `Скачать / Открыть настройки / Позже`, нижняя панель загрузки, resume через `.part`, manifest `core-model.json`, проверка размера и SHA-256. Версия повышена до `0.0.16-dev`.

Модель `Qwen3-8B-Q4_K_M.gguf` скачана в выбранную папку моделей `H:\AI_HUB\Данные_для_внедрения\Модели\Core\Qwen3-8B`. Размер `5027783488` байт и SHA-256 `d98cdcbd03e17ce47681435b5150e34c1417f50b5c0019dd560e4882c5745785` проверены, manifest записан как `installed`.

Чтобы скачанная модель не попала в GitHub, Codex добавил в `.gitignore` правило `Данные_для_внедрения/Модели/**`.

Пользователь предложил добавить к текущему ТЗ отладочное окно по `F12`, чтобы прямо из программы проверять живое состояние скачанных моделей. Согласовано: окно отдельное, не страница основного сценария; внутри минимальный чат, логи происходящего и выбор любой скачанной `.gguf` модели независимо от будущего назначения.

Codex скачал локальный backend `llama.cpp b9442` для Windows CUDA 12.4 x64 в `Runtime\Backends\llama.cpp\b9442\win-cuda-12.4-x64`. `llama-server.exe` на текущей машине оказался нестабилен/блокировался защитой Windows, поэтому обход защиты не выполнялся, а первая безопасная интеграция сделана через `llama-cli.exe`.

Реализовано debug-окно `Debug Model Tester`: `F12` открывает окно, список моделей видит скачанную `Qwen3-8B-Q4_K_M.gguf`, есть кнопки `Отправить`, `Стоп`, `Очистить чат`, `Очистить лог`, а служебный вывод backend-а отфильтровывается из чата. Модель запускается как дочерний процесс на один запрос и не получает доступ к файлам, интернету, shell, инструментам и настройкам Windows.

Пользователь вручную проверил окно: prompt отправляется, ответ приходит, логи отображаются. Codex отдельно выполнил короткий живой тест через `llama-cli.exe`: exit code `0`, ответ `Модель работает.` Версия повышена до `0.0.17-dev`; сборка, scanner кириллицы и сравнение ключей локализации прошли успешно. Установщик не собирался по правилу проекта.

Пользователь заметил, что в debug-окне список выбора моделей плохо читается: текст модели белый на светлом поле. Codex исправил тему debug-окна: окно получает текущую тему основного окна, обновляется при переключении темы, а `ComboBox`, выпадающий список, поля ввода и списки получили явные цвета для светлой и тёмной темы. Версия повышена до `0.0.18-dev`; сборка, scanner кириллицы и сравнение ключей локализации прошли успешно.

После этого пользователь сообщил, что программа падает при `F12`. Codex нашёл причину в журнале Windows: `System.InvalidOperationException`, попытка менять WPF-кисть ресурса в состоянии `только чтение`. Исправлено заменой ресурса кисти на новый `SolidColorBrush`, переводом theme-bound свойств на `DynamicResource` и защитой открытия debug-окна через `try/catch`. Версия повышена до `0.0.19-dev`; автоматический smoke-test `F12` прошёл.

Пользователь уточнил, что главная проблема была в нативном светлом `ComboBox`: белый текст на светлом фоне. Codex перестал бороться с системным шаблоном и задал для выбора модели отдельный тёмный текст `NativeComboTextBrush` при светлом фоне native-контрола. Версия повышена до `0.0.20-dev`; `dotnet build`, scanner кириллицы, сверка локализаций и `F12` smoke-test прошли.

Пользователь подтвердил, что ТЗ можно закрывать. Codex перенёс `ТЗ/2026-05-31_менеджер_основного_ядра_qwen3_8b.md` в архив. Установщик не собирался по правилу проекта; после финальных проверок актуальное состояние публикуется в GitHub.

Пользователь попросил победить проблему `llama-server.exe`. Codex создал ТЗ `2026-06-01_диагностика_llama_server_backend.md` и начал диагностику. Найдена причина первого сбоя: в основной runtime-папке отсутствовал `llama-server-impl.dll`, хотя официальный архив `llama-b9442-bin-win-cuda-12.4-x64.zip` этот файл содержит. После чистой распаковки backend-а server начал запускаться.

Недостающий `llama-server-impl.dll` скопирован в основную папку `Runtime\Backends\llama.cpp\b9442\win-cuda-12.4-x64`. Проверка `llama-server.exe --version` прошла, `/health` вернул `ok`, а `/v1/chat/completions` с Qwen3 8B вернул `Основной сервер работает.` Для Qwen3 в server-режиме нужен параметр `--reasoning off`, иначе поле `message.content` может быть пустым из-за thinking-режима. `llama-cli` fallback не тронут.

Пользователь решил, что интеграцию server-backend лучше делать вторым шагом текущего ТЗ. Codex добавил `LlamaServerRuntimeService`: debug-окно теперь предпочитает `llama-server`, запускает его скрытым дочерним процессом на свободном loopback-порту, ждёт `/health`, отправляет запросы в `/v1/chat/completions`, останавливает server при закрытии окна и при `Стоп`, а при ошибке server-а переключается на `llama-cli` fallback. Версия повышена до `0.0.21-dev`; сборка, scanner, сверка локализаций, F12 smoke-test и server endpoint prompt-test прошли.

Пользователь заметил горизонтальный скроллинг в чате и логах debug-окна. Codex отключил горизонтальный scrollbar для `ChatListBox` и `LogListBox`, добавил wrapping-шаблон элементов через `TextBlock` с переносом по словам и повысил версию до `0.0.22-dev`. Сборка и scanner кириллицы прошли.

Пользователь подтвердил, что ТЗ диагностики и интеграции `llama-server` выполнено. Codex перенёс ТЗ `2026-06-01_диагностика_llama_server_backend.md` в архив, фиксирует версию `0.0.22-dev`, оставляет runtime/model локальными и готовит публикацию актуального состояния проекта в GitHub. Финальные проверки прошли: build, scanner, локализации `173/173`, активные ТЗ, F12 smoke-test и endpoint-test `llama-server`. Установщик не собирался по правилу проекта.

Пользователь расширил новое ТЗ по ядру: модель должна знать точную текущую дату/время, примерное местоположение по IP, а также программа должна сохранять диалоги ядра в файл. Согласовано: 1 запуск программы = 1 core-session JSONL-файл; у задач и инструментов будут свои файлы; папка хранения берётся из выбранной пользователем папки результатов; `F12` пишет в отдельную debug-зону. Codex сохранил ТЗ `2026-06-01_контекст_ядра_и_журнал_сессий.md` и реализовал каркас: `user-profile.json`, IP-геолокация через `ipwho.is`, скрытый контекст для `llama-server`/`llama-cli`, core/debug JSONL-сессии и ignore-правила для пользовательских результатов. Версия повышена до `0.0.23-dev`.

Пользователь подтвердил, что ТЗ контекста ядра и JSONL-сессий выполнено. Codex перенёс ТЗ в архив, запускает финальные проверки и публикует актуальное состояние проекта в GitHub. Установщик не собирается по правилу проекта.

Пользователь попросил научить ядро искать актуальную информацию в интернете и скачивать файлы. Согласован первый слой через Tool Gateway: модель не получает прямой браузер/сеть, а просит команды `web_search:`, `web_read:` и `web_download:`, которые выполняет AI HUB с логированием. Создано ТЗ `2026-06-01_интернет_инструменты_ядра.md`.

Codex реализовал `WebSearchTool`, `WebPageReaderTool`, `WebDownloadTool` и подключил их к debug-окну `F12`. Добавлен сценарий `core_tool_test:`, где ядро само должно запросить инструмент. На тесте Qwen3 8B запросила `web_download:` и AI HUB скачал маленькую модель `Qwen3-0.6B-Q4_K_M.gguf` в `Тесты\1\AI_HUB\Tools\Web\Downloads`. Первые два скачанных файла были ручными smoke-test проверками Codex, файл с временем `03:47` скачан по команде ядра. По замечанию пользователя добавлен визуальный прогресс скачивания в debug-окне. Версия повышена до `0.0.24-dev`.

Пользователь уточнил, что в debug-режиме у ядра должны быть все доступные ему возможности. Codex подключил обычные prompt-ы F12-окна к инструментальному контуру: модель теперь получает список доступных инструментов и может сама попросить `web_search`, `web_read` или `web_download`, а не только отвечать как голый чат. Нижняя подпись debug-окна уточнена: прямого доступа у модели нет, действия выполняются через инструменты AI HUB. Smoke-test с прямой ссылкой `https://example.com/` привёл к `web_download` и скачиванию `download.bin`. Версия повышена до `0.0.25-dev`.

Пользователь сделал тест `Найди и скачать фото любого котика`: ядро скачало страницу поиска Яндекс.Картинок, а не изображение. Codex проверил файл: это HTML (`<!DOCTYPE html>`, `Яндекс Картинки`). Исправлено: `WebDownloadTool` теперь определяет `Content-Type`, добавляет расширение (`.html`, `.jpg`, `.png`, `.webp` и т.д.), возвращает `Content-Kind` и предупреждение для HTML. Agent-loop больше не завершает сценарий автоматически после любого скачивания, а возвращает результат ядру: если скачан HTML вместо картинки, ядро должно продолжать поиск прямой ссылки. Версия повышена до `0.0.26-dev`.

Пользователь уточнил, что не нужно зацикливаться на картинках, он пробует разные типы файлов. Codex расширил универсальную классификацию скачивания: `html`, `json`, `text`, `image`, `document`, `audio`, `video`, `archive`, `binary`. Проверены прямые загрузки HTML, JSON, TXT, PNG, JPEG, PDF, MP3 и ошибка `403`. MP3 сохраняется с расширением и `Content-Kind: audio`. Добавлен recovery: если модель на первом шаге отвечает `нет доступа`, AI HUB сам запускает `web_search` по пользовательскому запросу и возвращает результат ядру. Ошибки инструментов теперь возвращаются как текстовый `Tool error`, чтобы ядро могло искать другой источник. Версия повышена до `0.0.27-dev`.

Пользователь попросил расширить сценарий: ядро не должно ограничиваться одним сайтом, а должно делать примерно 10 запросов и выбирать максимально подходящий результат. Codex увеличил выдачу `web_search` до 10 результатов и расширил F12 agent-loop до 10 tool-запросов на задачу. В prompt добавлена инструкция сравнивать несколько источников и продолжать после ошибки, HTML вместо файла или неподходящего результата. Версия повышена до `0.0.28-dev`.

Пользователь заметил, что ядро пишет “нашёл/сохранил”, но файлов нет. Codex проверил JSONL: выполнялись только поисковые запросы `web_search`, без `web_download`; сохранялись JSON-файлы результатов поиска, а не нужный файл. Добавлен guard: для задач со словами “скачать/download” финальный ответ без `Web download complete` не принимается. Версия повышена до `0.0.29-dev`.

Пользователь повторно протестировал фото: ядро добиралось до прямой `.jpg`-ссылки, но ошибочно вызывало `web_read`. Codex добавил нормализацию: в задачах скачивания `web_read` на прямой файловый URL автоматически выполняется как `web_download`. `web_read` также научен показывать кандидаты прямых файлов из страницы. Smoke-test скачал PNG-кандидат из Wikipedia. Версия повышена до `0.0.30-dev`.

Пользователь решил добавить специализированную малую модель для будущего интернет-reranking: `BAAI/bge-reranker-v2-m3`. Codex добавил код автоскачивания сразу после основного ядра Qwen3 8B, manifest `tool-model.json`, проверку файлов и отображение служебной модели в F12 как `tool-only` без возможности запуска чата. К логике web-поиска модель пока не подключена. Версия повышена до `0.0.31-dev`; сборка, scanner кириллицы и сверка локализаций прошли. Модель руками не скачивалась.

Пользователь подтвердил, что reranker скачался и отображается в F12, затем разрешил подключить его к логике. Codex добавил локальное Python/Transformers окружение `Runtime/Python/reranker/.venv`, скрипт `bge_rerank.py` и сервис `WebSearchRerankerService`. Теперь `web_search` пересортировывает найденные результаты через `BAAI/bge-reranker-v2-m3`; если backend недоступен, используется `lexical-fallback`, чтобы поиск не ломался. Версия повышена до `0.0.32-dev`.

После установки runtime scanner кириллицы стал проверять сторонние файлы модели и Python-пакетов. Codex обновил scanner: исключены `Runtime`, `.venv` и скачанные `Модели`, потому что это не исходники проекта. Повторная проверка scanner прошла.

Пользователь уточнил будущую логику: пользователь не будет просить конкретные модели; ядро само должно понимать задачу, видеть нехватку возможности и предлагать докачать нужный компонент. Codex предложил следующий этап: реестр возможностей, inventory установленного, task planner в F12 и Hugging Face API provider. Пользователь согласился. Codex сохранил новое ТЗ `2026-06-01_реестр_возможностей_и_huggingface_provider.md` и закрывает толстое ТЗ интернет-инструментов как выполненное.

ТЗ интернет-инструментов перенесено в архив. Финальные проверки закрытия прошли: `dotnet build`, scanner кириллицы, локализации `199/199`. Установщик не собирался. По правилу проекта изменения публикуются в GitHub.

Пользователь собрал установщик и нашёл баг установленной версии: F12 видел модель, но не находил `llama-server.exe` и `llama-cli.exe` в `%LOCALAPPDATA%\AI_HUB\Runtime\Backends`. Codex исправил сборку установщика: теперь backend `llama.cpp b9442 win-cuda-12.4-x64` копируется в пользовательскую runtime-папку установленного приложения. Версия повышена до `0.0.33-dev`; из-за критичности installed-only бага установщик пересобран как исключение из правила. Новый файл: `Тесты\Установщики\AI_HUB_Setup_0.0.33-dev.exe`.

Пользователь попросил реализовать всё новое ТЗ, а не только последнюю тему нулевого поиска. Codex реализовал первый реестр возможностей, `inventory:`, `task_plan:`, Hugging Face API provider (`hf_find_model`, `hf_model_files`) и диагностику нулевых `web_search` результатов. Теперь ядро в F12 получает явный статус поиска, причину нулевой выдачи и инструкции пробовать простой/английский/официальный fallback, а для актуальных фактов должно читать страницы через `web_read`. Заодно исправлен BOM-сбой reranker-скрипта, из-за которого web-search уходил в слабый lexical fallback. Версия повышена до `0.0.34-dev`; проверки прошли: build, scanner, локализации и smoke-test новых инструментов.

Пользователь сформулировал важную концепцию: локальные модели слабее Codex, поэтому AI HUB должен быть для них Codex-подобной рабочей средой, а не просто чатом. Codex записал принцип в правила и архитектуру: новые сценарии строятся через инструменты, контекст, планирование, проверку фактов, логи, разрешения и понятные статусы.

Пользователь проверил результаты текущего ТЗ и попросил закрыть его без публикации в GitHub. Codex перенёс `2026-06-01_реестр_возможностей_и_huggingface_provider.md` в архив и зафиксировал исключение из правила публикации. Следующий этап обсуждения: улучшение интернет-поиска через отдельный стратегический слой, чтобы маленькая локальная модель не придумывала запросы сама.

## 2026-06-01 — стратегический web research

- Пользователь согласился попробовать новый подход к улучшению поиска в интернете: не заставлять маленькую локальную модель самой придумывать стратегию, а дать ей Codex-подобный инструмент `web_research`.
- Создано ТЗ `ТЗ/2026-06-01_стратегический_web_research.md`.
- Реализован `SearchStrategyService`, команда `web_research:`, Bing fallback для поиска, распаковка Bing redirect-ссылок и фильтрация нерелевантных результатов.
- Тест: запрос про последние новости космоса нашёл и прочитал источники вроде `Space.com` и `SpaceNews`; бессмысленный точный запрос корректно признан пустым, несмотря на мусорную выдачу Bing.
- После тестов добавлен блок `Dated items`, фильтр дат под пользовательское окно времени и правило разделять найденные факты от собственных рассуждений модели.
- Пользователь заметил короткие ответы в F12; найден жёсткий лимит `256` токенов. В debug-runtime искусственный лимит убран.
- Пользователь попросил прогнать ядро через реальное F12-окно. Частичный прогон после перезагрузки ПК проанализирован: 57 заданий стартовало, 52 завершились, 5 ушли в тайм-аут. Вывод: главная проблема теперь в orchestration debug-режима, где обычный чат и tool-agent смешаны.
- ТЗ `2026-06-01_стратегический_web_research.md` закрыто и перенесено в архив без публикации в GitHub по прямому указанию пользователя. Следующий ТЗ пока не создаётся, сначала обсуждается.

## 2026-06-01 — structured tool-calling для F12

- Пользователь попросил сохранить новое ТЗ и приступить к реализации улучшения orchestration debug-ядра.
- Codex создал `ТЗ/2026-06-01_structured_tool_calling_f12.md` и проверил официальный путь `llama-server` через OpenAI-compatible `tools` / `tool_calls`.
- Независимый smoke-test подтвердил: Qwen3 8B через `llama-server b9442` возвращает настоящий structured вызов `web_search` с JSON-аргументами.
- В `LlamaServerRuntimeService` добавлена поддержка structured tools; в F12 обычные вопросы отделены от tool-agent, а задачи с интернетом/скачиванием/inventory/task planning/Hugging Face идут через structured tool-calling.
- Старый текстовый протокол оставлен fallback-режимом. Скачивание в structured-режиме защищено от выдуманных URL.
- Версия повышена до `0.0.38-dev`; сборка, scanner кириллицы и сверка локализаций прошли.

Пользователь подтвердил, что ТЗ выполнено. Codex перенёс `2026-06-01_structured_tool_calling_f12.md` в архив, добавил ignore-правило для локальных diagnostic-прогонов F12, повторил проверки и опубликовал актуальное состояние проекта в GitHub: commit `0b60253`, push в `origin/main`.

Пользователь предложил следующий этап: профиль пользователя. Согласовано, что профиль условно обязательный: без него можно начать работу, но программа должна предлагать дозаполнить данные, потому что это повышает релевантность ответов. Codex создал ТЗ `2026-06-01_профиль_пользователя.md` и реализовал профиль как страницу внутри главного окна: имя/ник, ручное местоположение, предпочтения ответов, режим нагрузки `Лёгкий / Оптимальный / Экстрим`, мигающая кнопка профиля и промежуточная страница при `Начать работу`, если профиль неполный. Данные сохраняются в `user-profile.json`, скрытый контекст ядра получает профильные настройки. Версия повышена до `0.0.39-dev`; сборка, scanner кириллицы, сверка локализаций и smoke-запуск приложения прошли.

Пользователь на старой версии установил AI HUB на второй ПК: очень слабый ноутбук. Тест подтвердил переносимость за пределы ПК разработки: установка прошла, нужные файлы скачались, ядро в F12 запустилось. Ноутбук слишком медленный для регулярного тестирования, но важный факт старта и независимости от машины разработки зафиксирован.

Пользователь подтвердил, что профиль работает и ТЗ выполнено. Codex перенёс `2026-06-01_профиль_пользователя.md` в архив. Финальные проверки закрытия прошли: build, scanner кириллицы и локализации `241/241`. По обычному правилу закрытия ТЗ актуальное состояние проекта опубликовано в GitHub: commit `d116a15`, push в `origin/main`; установщик не собирался.

Пользователь предложил следующий фундаментальный шаг: ядро должно понимать, кто оно, на какой модели основано, в какой программе работает и чем пользователь отличается от инструментов. Codex создал ТЗ `2026-06-02_самоидентификация_ядра.md` и добавил единый слой `CoreIdentityService`: ядро получает паспорт AI HUB как локальной AI-мастерской/Codex-подобной среды, паспорт текущей модели и правила ролей. Результаты инструментов для модели теперь заворачиваются в `[AI_HUB_TOOL_RESULT]`, чтобы не смешивать tool-result с сообщением пользователя. Версия повышена до `0.0.40-dev`; build, scanner, локализации и smoke-запуск прошли.

Пользователь спросил, можно ли отслеживать занятость контекстного окна ядра и сделать Codex-подобное сжатие памяти, чтобы избежать Ollama-подобной амнезии. Codex объяснил, что это в основном внешний слой программы: оценка токенов, индикатор, резерв под ответ, сжатие старой истории в структурированную память и подмешивание summary в будущий контекст. По просьбе пользователя мысль сохранена как идея, а не рабочее ТЗ: `ТЗ/ИДЕЯ_2026-06-02_контекстное_окно_и_сжатие_памяти.md`.

Пользователь проверил самоидентификацию ядра: о себе ядро рассказало корректно, но заявило, что ничего не знает о пользователе, хотя карточка пользователя заполнена. Codex нашёл причину: профиль сохранён, но скрытое правило про отсутствие прямого доступа модель трактовала слишком широко. Исправлено: карточка пользователя и паспорт ПК теперь явно помечены как уже переданные AI HUB служебные данные, а в скрытый контекст добавлен паспорт компьютера. Версия повышена до `0.0.41-dev`; проверки прошли: build, scanner кириллицы, локализации `241/241`, smoke-запуск.

Пользователь подтвердил, что ТЗ самоидентификации ядра выполнено: F12 корректно использует профиль пользователя и паспорт ПК. Codex перенёс `2026-06-02_самоидентификация_ядра.md` в архив. GitHub-публикация не выполнялась по прямой команде пользователя "пока не публикуй"; это исключение зафиксировано в rollback-журнале.

Пользователь вернулся к идее управления контекстом ядра: нужен простой индикатор в нижней инфо-зоне без технических слов, `🧠` + шкала, `🤯` при заполнении, автоматическое Codex-подобное сжатие старой истории и официальные лимиты модели вместо догадок. Codex создал ТЗ `2026-06-02_контекстная_память_ядра.md`, добавил `CoreContextMemoryService`, индикатор памяти в главное окно, связь F12 с индикатором и автоматическое сжатие старой истории в memory-summary рядом с debug-сессией. Сжатая память передаётся модели как служебный контекст. Версия повышена до `0.0.42-dev`; build, scanner, локализации `247/247` и smoke-запуск прошли.

Пользователь проверил индикатор и заметил, что значок плохо виден в тёмной теме, а шкала слишком быстро заполняется из-за искусственных резервов. Codex исправил цвет значка под темы/состояния, убрал искусственные резервы из расчёта и перевёл пользовательский индикатор на официальный нативный лимит Qwen3-8B `32768`, не меняя пока backend `--ctx-size 4096`, чтобы не увеличить расход памяти на слабых ПК без отдельного решения. Версия повышена до `0.0.43-dev`; build, scanner, локализации и smoke-запуск прошли.

Пользователь уточнил, что целевой минимум AI HUB — средний игровой ПК, поэтому backend-лимит `4096` слишком жёсткий. Codex повысил реальный `--ctx-size` для `llama-server` и `llama-cli` fallback до официального нативного лимита Qwen3-8B `32768` через общий `CoreContextRuntimeLimits.CurrentBackendContextLimit`. YaRN `131072` оставлен как справочная граница на будущее. Версия повышена до `0.0.44-dev`; build, scanner, локализации и smoke-запуск прошли.

Пользователь попросил проверить, насколько текущая память ядра теряет смысл при сжатии длинного литературного отрывка. Codex сделал временный harness-тест без изменения кода проекта: свежий отрывок сохранил почти весь смысл (`9/10` маркеров), отрывок у границы сжатия сохранил только начало (`2/10`), а отрывок далеко в старой истории потерялся (`0/10`). Вывод: для ядра-диспетчера текущий уровень достаточен, но для книг/документов/длинных материалов нужен отдельный инструментовый контекст и позже semantic-summary.

Пользователь уточнил, что память ядра должна сжиматься самой моделью, а не механически: ядро обязано помнить выборы пользователя, суть текущей задачи, сделанное, план, ограничения и при необходимости перечитывать текущий лог. Codex расширил активное ТЗ контекстной памяти, добавил модельное сжатие как основной путь и оставил механическое сжатие аварийным fallback. Также добавлен инструмент `session_log` для F12: `tail` показывает последние записи текущей сессии, `search` ищет фрагменты в JSONL-логе. Версия повышена до `0.0.45-dev`; проверки build, scanner, локализации и smoke-запуск прошли.

Пользователь попросил повторить старый тест с книгой уже после внедрения модельного сжатия. Codex сделал временный harness-тест через реальный `llama-server`: отрывок рядом с границей сжатия сохранил `9/10` смысловых маркеров, но отрывок в начале или глубоко в середине старой истории дал `0/10`. Вывод: модельное сжатие лучше механического, но текущая нарезка истории ненадёжна для важных фрагментов внутри длинного диалога. Нужен следующий слой защищённых якорей вокруг важных пользовательских фраз и восстановление деталей через `session_log`.

Пользователь решил, что на одних диалоговых рассуждениях дальше память не поймать: ядро будет работать не как чат, а как сценарный диалог, похожий на NPC в RPG. Все активные ТЗ признаны выполненными. Codex перенёс в архив `2026-06-02_контекстная_память_ядра.md` и связанную идею `ИДЕЯ_2026-06-02_контекстное_окно_и_сжатие_памяти.md`. Версия этапа остаётся `0.0.45-dev`; дальше нужен боевой прогон полноценного сценария. Закрытие опубликовано в GitHub: commit `2fb4151`, push в `origin/main`.

## 2026-06-03 — старт первого боевого сценария

Пользователь решил не начинать с полноценного RAG, а сначала сделать первый реальный сценарий.

Выбран сценарий помощи в выборе по любому вопросу: ядро AI HUB готовит задачу, после чего передаёт работу более мощной общепрофильной модели. Название модели пока не выбрано; рабочая роль фиксируется нейтрально как `general_worker`. Интернет может использоваться через инструменты AI HUB при необходимости, но приоритет у знаний и рассуждения модели.

Codex создал рабочее ТЗ `ТЗ/2026-06-03_первый_боевой_сценарий_выбора.md`.

Пользователь уточнил концепцию: боевой режим не должен быть прямым чатом. Он должен работать как разговор с NPC в RPG, организационный опросник или режим планирования Codex. Сценарий начинается с крупных групп направлений, потом постепенно сужается до выбора модели/инструмента и полноценного prompt для исполнителя. В одном шаге желательно не больше 6 готовых вариантов в 3 строки плюс отдельный `Свой вариант` 4-й строкой. Codex зафиксировал это в активном ТЗ.

Пользователь уточнил визуал вариантов: использовать существующую заглушку режима, а варианты показывать красиво и динамически. Все 6 кнопок не должны висеть постоянно; интерфейс показывает ровно столько вариантов, сколько предложило ядро. Базовый вид: две колонки до 6 вариантов и отдельная широкая строка `Свой вариант`. Допустимы спокойные анимации появления и подсветки выбора. Codex зафиксировал это в активном ТЗ.

Пользователь уточнил границу первого этапа: при старте сценария нужно уметь пройти почти до конца подготовки, но не решать задачу силами ядра. Ядро должно собрать вводные, сузить направление, подготовить prompt и предложить оптимальную модель-исполнитель. Скачивание модели пока не подключается; ядро не должно перепрыгивать от подготовки сразу к финальному решению.

Пользователь добавил визуальное правило: если ядро считает один из вариантов желательным, это нужно показывать в интерфейсе, например цветом обводки кнопки. Codex зафиксировал в ТЗ структурный признак `isRecommended`, короткую причину рекомендации и мягкое UI-выделение без скрытия остальных вариантов.

Пользователь попросил записать в ТЗ выводы из официальной документации. Codex добавил раздел с опорой на Microsoft Learn по WPF data binding, `ItemsControl`, `ObservableCollection<T>` и animations, а также на официальные документы `llama.cpp` по OpenAI-compatible server, function calling/tool use и JSON/schema-constrained output. Главный вывод: варианты кнопок нельзя вылавливать из обычного текста ядра; ядро должно отдавать валидируемую JSON-структуру шага.

Пользователь уточнил особенность `Режима неопределенности`: в конце ядро не должно предлагать модель или инструмент слабее себя как оптимального исполнителя. Подбор исполнителя должен учитывать паспорт ПК, доступные backends, настройки профиля пользователя и режим нагрузки. Если более сильный исполнитель не установлен, нужно честно показать это в финальной карточке и не запускать скачивание автоматически.

Пользователь попросил сделать первый выбор ещё более общим, не через прикладные категории вроде работы или покупки. Согласован фиксированный стартовый вопрос: `К чему ближе вопрос?` с вариантами `Знания`, `Вещи`, `Жизнь`, `Технологии`, `Люди`, `Цели` и `Свой вариант`. Этот шаг можно использовать как часть конструктора первого prompt для ядра.

Пользователь предложил защиту от типичных ошибок локального ядра: на каждом шаге делать “перезалив контекста”, а не вести сценарий как обычный чат. Codex зафиксировал в ТЗ prompt-контур: роль ядра, правила сценария, карточка состояния, последний выбор, профиль, паспорт ПК, доступные модели/инструменты и JSON-схема ответа передаются заново. Также уточнено: `Свой вариант` — это короткая метка 1-3 слова, например `Смена работы`, а не вопрос или мини-промт. Финальный подбор исполнителя может использовать интернет/Hugging Face инструменты прежде всего на этапе выбора модели/инструмента, но скачивание пока не подключается.

После финального анализа ТЗ пользователь уточнил правки: первая реализация нужна для проверки поведения ядра, поэтому скачивание и запуск новой модели пока не подключаются; отдельный режим `Изменить ответы` на финальной карточке не нужен; ответы должны корректно помещаться в кнопки; при подборе исполнителя ядро сначала проверяет установленное, затем ищет подходящее в интернете, а если интернета нет, предлагает текущее ядро как временный fallback. Codex внёс эти уточнения в активное ТЗ.

Пользователь попросил приступить к реализации. Codex начал первый слой `Режима неопределенности`: добавлены модели `ChoiceScenarioOption`, `ChoiceScenarioStep`, `ChoiceTaskCard`, сервис `ChoiceScenarioService`, внутренняя страница `ChoiceScenarioPage` в `MainWindow`, динамические варианты через `ItemsControl`, отдельный `Свой вариант`, зона `Мысль ядра`, финальная preview-карточка подготовки и повышение версии до `0.0.46-dev`. Новая модель не скачивается и не запускается. Проверки через shell не удалось выполнить из-за sandbox-ошибки `CreateProcessAsUserW failed: 5`.

Пользователь запустил сборку и получил `CS0104`: `Button` неоднозначен между WPF и Windows Forms. Codex исправил два места в `MainWindow.xaml.cs`, явно указав `System.Windows.Controls.Button`.

Пользователь визуально проверил первый слой и отметил, что всё ещё не то: после фиксированного первого шага дальнейшие варианты должно предлагать ядро, а количество шагов должно решаться ядром, хоть 3, хоть 100. Также нужна кнопка `Перейти к финалу` под `Свой вариант`, а элементы страницы должны адаптироваться под размер окна. Codex дополнил ТЗ и переделал реализацию: добавлен `ChoiceScenarioAnswer`, prompt-контур и JSON-парсер в `ChoiceScenarioService`, метод `GenerateScenarioJsonAsync` в `LlamaServerRuntimeService`, отправка истории ответов ядру из `MainWindow`, кнопка `Перейти к финалу`, fallback-шаг только при недоступности ядра.

Пользователь заметил, что адаптивность сделана неудачно: появился лишний скролл, а варианты стали столбиком. Codex вернул варианты в двухколоночный `UniformGrid`, центрировал основной блок и отключил горизонтальный скролл.

Пользователь уточнил, что скролл всё ещё есть, а элементы должны быть центрированы и ужиматься за счёт пустого места. Codex убрал внутренний `ScrollViewer` первого экрана сценария, заменил его на центрированный `Grid`, уменьшил вертикальные отступы и высоту карточек вариантов.

Пользователь показал результат прогона: ядро после `Перейти к финалу` выдумало задачу про развитие логического мышления у кота и назначило `владельца кота` исполнителем. Codex определил, что prompt неверно ограничивал финал: ядро должно не искать ответ без вопроса, а подбирать AI-модель/инструмент. Prompt усилен: запрещено выдумывать задачу, final_task_card запрещён без конкретного предмета неопределённости, recommendedExecutor может быть только моделью/backend-ом/инструментом/ролью AI HUB. Кнопка `Перейти к финалу` скрыта на первом шаге.

Пользователь уточнил, что у каждого сценария должна быть своя папка, куда попадают JSONL-журналы ядра и будущих инструментов. Codex добавил `ScenarioSessionLog` и подключил журналирование `Режима неопределенности` в `<Папка результатов>\AI_HUB\Scenarios\Uncertainty\Sessions`. Журнал пишет старт, context snapshot, выборы пользователя, prompt к ядру, raw-ответ ядра, распарсенные шаги, ошибки структуры, финальную карточку, недоступность ядра и завершение сессии.

Пользовательский build показал `CS0246` в `ScenarioSessionLog.cs`: не найден `StreamWriter`. Codex добавил `using System.IO;`.

Пользователь попросил провести полный аудит проекта, а затем реализовать все предложенные исправления. Codex создал backup `Backups/20260710_full_project_audit_remediation` и укрепил сценарий: закрыл двойные клики, добавил точные снимки шагов для возврата, защиту от повторяющихся вопросов, строгий JSON Schema-контракт с двумя repair-попытками и отдельный orchestration-сервис.

Сценарное ядро теперь получает общую идентичность AI HUB и роль `ScenarioPlanner`. Оно может безопасно использовать inventory, web и Hugging Face tools через Tool Gateway, но не получает `web_download`. Перед финалом обязательны проверка установленного состава и актуальный поиск модели. Инструменты отделены от модели-исполнителя; текущее ядро допустимо только как `core_fallback` при недоступном поиске.

Журналы получили уникальные имена, schema/session metadata, фоновую запись, fallback-папку и Git-исключение. Интерфейс локализован, центрирован и масштабируется без постоянного скролла. Добавлен MSTest-проект. Прошли 8 тестов, Debug/Release build, format, UTF-8 scanner, локализации `315/315`, smoke-запуск и живой `json_schema` тест на Qwen3 8B/llama-server. Версия остаётся `0.0.46-dev`, установщик и GitHub-публикация не выполнялись.

Пользователь протестировал сценарий и не смог получить финальную рекомендацию модели. Анализ нового JSONL показал: ядро трижды строило финал с Qwen3 8B, валидатор отклонял текущее ядро, но обязательные `inventory` и `hf_find_model` ещё не запускались; repair без инструментов повторял ошибку. Дополнительно локальное ядро смешивало `question_step` и `taskCard`, а цепочка `Какой конкретный аспект...` уходила в смысловое зацикливание.

Пользователь зафиксировал продуктовое правило: если в профиле не выбран `Лёгкий` режим, 8B-модель может быть только поисковиком/ядром, а рабочий исполнитель обязан быть мощнее 8B. Codex реализовал правило программно через `executorCapabilityClass`, проверку размера в названии и workload mode. Финал теперь принудительно вызывает `inventory`, затем `hf_find_model` через structured `tool_choice`; question step игнорирует лишнюю task card, добавлена семантическая anti-loop защита. Прошли 14 тестов, Debug/Release, format, scanner и smoke; реальный llama-server подтвердил forced tool call.

Пользователь предложил ограничивать длину сценария первым вопросом. Согласованы варианты `Быстро — до 4`, `Обычно — до 10`, `Подробно — до 20`, `По ситуации`; последний режим имеет safety limit 30. Пользователь уточнил, что ядро должно не просто быть остановлено программой, а с самого начала понимать, через сколько содержательных шагов от него ждут результат.

Codex реализовал бюджет шагов: отдельный стартовый экран, счётчики maximum/used/remaining в каждом prompt, предупреждение на предпоследнем вопросе и обязательный `final_task_card` при нулевом остатке. Выбор глубины, repair и tools не считаются; возврат освобождает шаг. В UI показывается текущий вопрос и предел. Прошли 18 тестов, Debug/Release, format, scanner `226` файлов, локализации `329/329` и smoke-запуск.

Повторный пользовательский тест дошёл до обязательного финала, но снова завершился ошибкой. Журнал показал, что ядро корректно вызвало инструменты, однако искало модель по теме пользовательской задачи, получило ноль кандидатов и вновь назначило Qwen3 8B. Codex добавил резервный поиск по семейству модели, программный отбор безопасного кандидата выше 8B и автоматическую замену запрещённого выбора ядра. Исправление прошло 20 тестов, Debug/Release build, format, scanner `230` файлов, локализации `329/329` и smoke-запуск.

Пользователь предложил предварительно создать локальный список доступных моделей Hugging Face, чтобы слабое ядро выбирало среди подготовленных карточек и могло рекомендовать модель офлайн. Для первого этапа пользователь попросил только парсер данных без подключения к ядру. Codex добавил изолированные загрузчик и нормализатор, stable JSON-схему, сохранение raw API/Model Card с SHA-256 и отдельный диагностический CLI. Живые пробы выявили различия официальной модели, упаковок и fine-tune одного веса; основные поля официального Qwen3 14B совпали с raw API. Полный Hub и автозапуск пока не подключались.

Пользователь уточнил, что парсер должен видеть и новые модели известных создателей, и появление новых разработчиков. Codex исключил allowlist как ограничение: добавлены режимы произвольного автора и общей ленты последних публичных моделей. Оба режима проверены живыми запросами Hugging Face.

Пользователь заметил, что ядро в тестах постоянно выбирает следующий по размеру Qwen. Аудит подтвердил программный уклон: balanced fallback был жёстко задан как Qwen3 14B, tool description приводил Qwen как пример, selector предпочитал Qwen/известных упаковщиков, а программа могла сама заменить выбор ядра. Codex удалил эти механизмы: теперь ядро само выбирает семейство и издателя, а программа только проверяет нижний порог и наличие кандидата в выдаче. По уточнению пользователя модель семейства ядра допустима только как явно значительно более новое поколение; увеличение веса той же генерации недостаточно.

Пользователь согласовал широкий каталог для всей программы: 11 направлений, по 9 кандидатов, ручное добавление полезных моделей до 8B и автоматический поиск популярных новинок строго больше 8B. Codex добавил редактируемый посевной JSON на 99 слотов, накопительную schema v2, отслеживание revision SHA и JSONL-журнал изменений. Радар просматривает глобальные рейтинги и отдельные pipeline, не ограничен известными разработчиками, отмечает новых авторов и отбрасывает чистые quantized-упаковки. Живой каталог содержит 94 уникальные записи без дублей и недоступных репозиториев; 28/28 тестов проходят. К ядру и UI каталог пока не подключён.

Перед подключением каталога пользователь предоставил набросок переориентации `Режима неопределенности`. Codex доработал и реализовал его: сценарий теперь строит capability profile AI-исполнителя, а не углубляется в предметную тему. Каждый вопрос имеет закрытое измерение, влияние на выбор и структурированные эффекты вариантов; решённые измерения и узкие предметные вопросы блокируются, профиль откатывается кнопкой `Назад`. Живой Qwen3 8B прогон прошёл новый контракт и полный финал с `inventory`/`hf_find_model`; версия повышена до `0.0.47-dev`. Каталог моделей пока остаётся изолированным.

Пользователь поручил подключить каталог к ядру, сохранив его отдельным элементом. Codex добавил `model_catalog_search` как самостоятельный structured tool: ядро получает до шести локальных кандидатов, но само выбирает исполнителя. Перед финалом обязательны `inventory` и локальный каталог; живой Hugging Face поиск используется при отсутствии кандидатов или для актуальной проверки. Полная база не загружается в prompt, синхронизация не запускается tool-вызовом, точный repo и подтверждённый размер проверяются программой. Версия повышена до `0.0.48-dev`.

Первый пользовательский прогон после подключения каталога завершился общей ошибкой на предыдущем вопросе. JSONL подтвердил, что каталог работал и вернул три модели 30–32B. Сбой вызвали формальные aliases контракта и повтор ядром запрещённой Qwen3-Coder в repair. Codex добавил безопасную нормализацию aliases, память отклонённых моделей и явный запрет повторять их в следующем repair, не назначая альтернативу программно. Политика запрета того же поколения семейства ядра сохранена; UI-текст ошибки стал точнее. Версия `0.0.49-dev`.

Пользователь подтвердил успешный гибридный тест и поручил реализовать оставшиеся проверки ПК, metadata-lineage, согласованность финальной карточки и автообновление каталога; визуал отложен. Codex добавил Q4-оценку RAM/VRAM/диска, фильтрацию физически неподходящих моделей, приоритет `baseModels` для lineage, запрет противоречий web/offline и безопасную фоновую синхронизацию раз в 24 часа. Живой локальный probe на текущем ПК подтвердил GPU-fit 20–32B, hybrid-fit 120B и отсечение сверхкрупных моделей. Версия `0.0.50-dev`.

Пользователь подтвердил завершение всех активных ТЗ и поручил закрыть их по регламенту. Codex перенёс в архив ТЗ первого боевого сценария, парсера Hugging Face и подключения локального каталога к ядру. Активных ТЗ не осталось; визуал и подключение рабочей модели потребуют новых ТЗ. Актуальное состояние `0.0.50-dev` готовится к обязательной публикации в `origin/main`.

Закрытие прошло обязательные проверки: Release build, 54/54 теста, scanner исходников, локализации `353/353` и отсутствие активных ТЗ. Основной commit `6571c12` с завершённым сценарием и каталогом успешно отправлен в `origin/main`; установщик по правилу не пересобирался.

Пользователь предоставил набросок следующей задачи: локальный голос ядра с синхронным раскрытием вопросов. После уточнения закреплено, что в «Режиме неопределенности» голос произносит только содержимое `coreThought` и `question`; текст появляется примерно на слово позади речи, чтобы его нельзя было прочитать намного раньше, а варианты временно недоступны. Codex создал полноценное активное ТЗ. Компактный синтетический голос относится только к ядру; будущие модели-инструменты получат отдельный контур более реалистичных голосов. Реализация ещё не начиналась.

Codex реализовал голос ядра на eSpeak NG, а пользователь вручную подтвердил корректную работу русского и английского вариантов. Затем пользователь попросил добавить RHVoice как альтернативу, сохранив eSpeak по умолчанию. В настройках eSpeak называется `Привет из 80-ых` / `Hello from the 80s`, RHVoice — `Просто ИИ голос` / `Just an AI voice`. RHVoice подключён через системный Windows SAPI, имеет русский профиль Aleksandr и английский Slt, а при недоступности автоматически используется eSpeak. Внутренние проверки прошли; ручная проверка RHVoice пользователем ещё ожидается.

Пользователь вручную проверил RHVoice и подтвердил успешное прохождение теста. Голосовое ТЗ признано выполненным и закрывается по регламенту с переносом в архив и публикацией актуального состояния в GitHub. Установщик не пересобирается.

Закрытие голосового ТЗ прошло обязательные проверки: Release build, 62/62 теста, format, scanner, локализации `371/371` и отсутствие активных ТЗ. Основной commit `d83e308` с реализацией двух голосов успешно отправлен в `origin/main`.

Пока Codex закрывал ТЗ, пользователь самостоятельно собрал установщик версии `0.0.51-dev` и подтвердил, что установка и программа работают корректно. Результат отмечен как успешный ручной smoke-тест установщика.

Пользователь согласовал следующий этап: после финальной карточки ядро должно инициировать безопасную загрузку отсутствующей GGUF-модели, передать ей задачу и получить результат. Исполнитель работает в долгоживущей сессии, накапливает контекст примерно до 80% и сжимает его без выгрузки модели. Ядро и исполнитель получают крупную Matrix-анимацию из реального token stream. Создано активное ТЗ; реальные модели в автоматических тестах скачивать запрещено.

Codex реализовал сквозной sandbox-контур: разрешение конкретного GGUF, подтверждение и проверяемая установка, отдельный долговечный executor runtime, web tools, 80%-контекст, уточнение, результат и отдельный JSONL. Ядро и исполнитель переведены на SSE-поток для крупного Matrix-дождя. Внутренний live probe использовал только уже установленное ядро; новые модели не скачивались. Версия `0.0.52-dev`, ожидается пользовательская проверка.

Пользовательский тест показал отсутствие Matrix, нестабильный выбор модели и невозможность запуска скачанной Gemma. Аудит установил, что Matrix был помещён на скрытую страницу настроек, а вместо основных 31B-весов был выбран 515-МБ MTP-файл архитектуры `gemma4-assistant`; размер и SHA не доказывали его пригодность как основной модели. Пользователь поручил удалить нерабочий элемент и реализовать весь план исправления.

Codex удалил рабочую копию ошибочного MTP-файла и шесть его executor-журналов после backup. В `0.0.53-dev` auxiliary/split GGUF и неправдоподобные веса блокируются, архитектура читается до регистрации, а runnable-статус выдаётся только после реального `llama-server` health-check. Выбор ядра получил широкий catalog/HF fallback без назначения победителя программой; Matrix перенесён на страницу сценария. Повторный ручной тест пользователя ещё требуется.

Пользователь успешно запустил Qwen3.6 27B и уточнил правильную границу ролей: ядро должно выбрать класс LLM и передать только фоновые данные, а исполнитель сам сужает реальную задачу. Codex реализовал отдельный handoff с происхождением данных, повторяемые кнопочные уточнения, свободный ответ, строгие статусы и repair вместо ложного финала. Добавлены ручное завершение сессии и голос исполнителя RHVoice `Elena / Bdl`; ядро сохраняет `Aleksandr / Slt` во всех сценариях. Четыре профиля запрещены инструментам других сценариев без прямой просьбы пользователя. Версия `0.0.54-dev`, ожидается ручная проверка.
Пользователь попросил всегда показывать два варианта исполнителя: установленную модель и отдельную модель для загрузки, с понятными плюсами и минусами. Codex реализовал строгий парный контракт, проверку evidence/policy/ПК и явный выбор пользователя перед resolver. Версия повышена до `0.0.55-dev`; repair сохраняет исправный вариант, если второй кандидат ошибочен.

Во время внутренней диагностики Defender эвристически заблокировал команду Codex с динамической загрузкой AIHub.dll через PowerShell reflection. Проверка подтвердила, что срабатывание относилось к командной строке, код угрозы не выполнялся, а отдельные сканы Debug-сборки и RHVoice installers чистые. Такой способ диагностики исключён из дальнейшей работы.

Последний пользовательский журнал показал, что программа правильно нашла установленную Qwen3.6 27B и три разумные альтернативы разных семейств, но ядро потратило repair на технические противоречия и в итоге предложило две упаковки тех же весов. Пользователь уточнил: программа должна сопоставлять capability-протоколы и ограничивать невозможное, но не назначать победителя; установленная модель может быть лучшим вариантом, а вторым всегда показывается кандидат другого семейства. Codex реализовал доверенный пул в `0.0.56-dev`: программа проверяет inventory/каталог/ПК, ядро выбирает только ID и формулирует плюсы, ограничения и предпочтение. 84/84 теста проходят; ожидается ручной сквозной тест, установщик не пересобирался.

Пользователь провёл сквозной тест `0.0.56-dev` и подтвердил, что новый выбор исполнителя работает значительно быстрее и стабильнее. Ядро предложило установленную Qwen3.6 27B и альтернативу GPT-OSS 20B другого семейства, разумно предпочло установленную модель, после выбора исполнитель запустился и вернул `final_result`. Активное ТЗ признано выполненным. Неудачный продуктовый вид финала исполнителя отмечен как отдельная будущая задача и не блокирует закрытие текущего ТЗ.

Закрытие завершено по регламенту: ТЗ перенесено в архив, активных ТЗ нет, все обязательные проверки пройдены, основной commit `f22af34` опубликован в `origin/main`. Установщик не пересобирался.

Пользователь и Codex разобрали последний тест исполнителя. Отдельная Qwen3.6 27B действительно запускалась, но программа приняла служебную метку `final_result` за готовое резюме. Пользователь решил, что исполнитель не должен самостоятельно завершать работу: его сессия может длиться минуты или часы, а завершает её только человек.

Согласована многоэтапная схема: уточнение цели и задачи, выбор способа решения, сбор данных, сборка результата и будущий экспорт. Переходы выполняются пользователем кнопками; модель может только рекомендовать следующий этап. Готовая версия результата остаётся редактируемой и не закрывает runtime.

Codex создал активное ТЗ и реализовал схему в `0.0.57-dev`. Добавлены состояния `working / stage_ready / result_ready / blocked`, ручная навигация этапов, продолжение после результата, repair фиктивного ответа, защита от преждевременного результата и расширенный executor JSONL. Внутренние проверки прошли: Debug/Release, 88/88 тестов, format, scanner, локализации 433/433 и publish smoke без моделей. Новые модели и установщик не загружались; ожидается ручной тест пользователя.

Пользователь провёл ручной тест `0.0.57-dev` и принял многоэтапную сессию: поведение пока не полное, но соответствует ожиданиям песочницы. ТЗ закрывается по регламенту с переносом в архив, финальными проверками, commit и push. Дальнейшая настройка поведения, файлы, экспорт и визуал остаются отдельными задачами; установщик не пересобирается.

Закрывающие проверки прошли: Debug/Release без предупреждений, `88/88` тестов, format, scanner `216` файлов, локализации `433/433` и publish-smoke `922` файла без моделей.

Основной commit закрытия `8f2bd3f` опубликован в `origin/main`; факт публикации внесён в проектные журналы отдельным документационным commit.

Пользователь согласовал продолжение логики исполнителя: после подтверждения целостности стартовых данных модель должна приступить к самостоятельной работе, сама двигаться от способа решения к сбору данных и сборке результата, а пользователя тревожить только при критической неопределённости. На рабочей фазе нужна кнопка `Выдать результат`, открывающая предварительный документ в отдельном окне без завершения сессии.

Codex создал активное ТЗ и реализовал решение в `0.0.58-dev`: явное подтверждение постановки, автономный структурированный цикл, динамические безопасные web-инструменты, защита от повторов, многострочный ввод и версионные Markdown-снимки результата. Внутренние тесты не скачивают модели и не управляют UI; ожидается ручная сквозная проверка пользователя.

После ручного теста пользователь отказался от автоматического движения исполнителя к финалу. Согласована двухэтапная схема: короткое техническое уточнение, затем практически неограниченный диалог, где исполнитель продолжает собирать данные, а человек сам решает, когда запросить результат.

В `0.0.59-dev` практический экран разделён на рабочую область слева и краткий пересказ доступного результата справа; варианты ответа перенесены в нижнюю панель. Полный Markdown-документ создаётся только ручной кнопкой в отдельном окне и не завершает сессию. Контракт очищен от автоматических финальных состояний, добавлен ограниченный `currentResultSummary` и offline-нормализация handoff. Внутренние проверки прошли, модели не скачивались, UI автоматически не управлялся; ожидается ручной тест пользователя.

Ручной тест подтвердил логику, но обнаружил визуальное наложение: программа скрывала ошибочно названный Viewbox страницы настроек, а старый экран сценария оставался под новым workspace. Пользователь также попросил закрепить три рабочие зоны и растянуть Matrix-дождь на всё окно с полупрозрачным чёрным фоном.

В `0.0.60-dev` контейнеры разделены, первый этап центрирован, второй получил стабильные левую, правую и нижнюю зоны, а `Свой вариант` вошёл в общую сетку ответов. Matrix перенесён в корневой слой клиентской области; фон имеет прозрачность 50%, символы остаются непрозрачными. Внутренние проверки прошли без моделей и автоматического управления UI; ожидается ручная визуальная проверка.

Пользователь подтвердил выполнение ТЗ и поручил закрыть его без публикации. ТЗ переносится в архив, создаётся локальный коммит, `push` не выполняется.

В последнем тесте обнаружено отдельное смысловое несоответствие: правый пересказ и ручной предварительный документ оказались техническим заданием на статью, а не самой статьёй. JSONL подтвердил, что практический этап накапливал требования и метаформулировки `формируется обзор`; snapshot развернул их в ТЗ. По просьбе пользователя выполнен только анализ, без изменения промтов.

Закрытие зафиксировано локальным commit `f101fbd` в ветке `main`. `Push` не выполнялся по прямому указанию пользователя; пользовательский файл изображения остался незатронутым и незастейдженным.

Пользователь предложил развить исправление в буквальное дерево диалога, похожее на семейное, с отдельным окном и красивой анимацией роста. Согласовано, что дерево хранит программа, а не модель: ответы создают выбранные и неактивные ветви, модель получает только компактный активный срез. Кнопка дерева должна находиться рядом с настройками и содержать только знак без текста.

Codex создал активное ТЗ и реализовал `0.0.61-dev`: структурированное дерево executor-сессии, JSONL-события, живое немодальное окно с масштабированием и сворачиванием, а также отдельный `workingResultFragment`. Практический пересказ теперь строится из фактического ответа, а непрошенное техническое задание в предварительном документе отклоняется и ремонтируется. Ожидается ручная проверка пользователя; модели не скачивались и UI автоматически не управлялся.

Пользователь согласовал простой ручной финал: по кнопке можно завершить без сохранения либо сформировать полный итог через прямой вывод или DOCX. Исполнитель вправе только рекомендовать завершение; решение всегда остаётся за пользователем, а при готовности кнопка визуально переезжает к вариантам ответа.

В `0.0.62-dev` добавлены `suggest_finalization`, семантическая защита от модельных команд интерфейса, диалог завершения, отдельная финальная генерация и DOCX через Microsoft Open XML SDK. Ошибки и отмена не закрывают сессию. Внутренние проверки прошли: Debug/Release без предупреждений, `99/99` тестов, scanner и локализации; модели не скачивались, UI не управлялся, установщик не собирался. Ожидается ручной тест пользователя.

Ручной тест подтвердил работу окна завершения и создание DOCX, но кнопка не переехала вниз. JSONL показал, что исполнитель назвал исследование завершённым и не имел критических пробелов, однако продолжил `ask_user` ради необязательного раздела. В `0.0.63-dev` готовность отделена от действия: новый `canFinalize` оживляет кнопку при уже самодостаточном результате, не запрещая ещё один полезный необязательный вопрос. Проверки `101/101` прошли; ожидается повторный ручной тест.

Пользователь подтвердил выполнение всех активных ТЗ. ТЗ дерева сессии и ручной финализации/DOCX перенесены в архив, активных ТЗ больше нет. Закрывающие проверки прошли: Debug/Release без предупреждений, `101/101` тестов, format, scanner, локализации, NuGet vulnerability check и self-contained publish smoke без моделей. Установщик не собирался; актуальное состояние публикуется в GitHub по штатному правилу.

Основной коммит закрытия `5a5cb27` успешно отправлен в `origin/main`; вместе с ним опубликованы накопленные локальные коммиты после предыдущего исключения из публикации.

Пользователь поручил заменить заглушку `Ранее начатое` реальным продолжением любой сессии: завершённой, штатно закрытой или оборванной аварией. Закреплено, что пользовательское название команды `Завершить без сохранения` не меняется, а ядро и исполнитель должны знать, что работают в новом запуске, поднятом из архива. Карточка содержит название, точные даты до секунд, переименование, выбор для удаления и локальную проверку исполнителя.

Codex создал активное ТЗ и реализовал в `0.0.64-dev` атомарные контрольные точки ядра и исполнителя, постоянный `SessionId`, новый `RunId` при каждом восстановлении, резервную ревизию, динамический список карточек, локальную проверку модели и подтверждаемое удаление. Незавершённый аварийный ход не коммитится, а защищённый prompt-маркер запрещает моделям изображать первоначальный запуск. Debug/Release прошли без предупреждений, тесты `109/109`; модели не скачивались, UI автоматически не управлялся, установщик не собирался. ТЗ ожидает ручной тест пользователя.

Пользователь подтвердил полное выполнение ТЗ долговечных сессий. Codex перенёс его в архив, обновил реестр и документы проекта; активных ТЗ не осталось. Повторные Debug/Release проверки и тесты `109/109` прошли. По прямой просьбе пользователя публикация отложена: создаётся локальный коммит без `push`, чтобы накопить больше изменений.

Локальный коммит закрытия `5929513` создан в `main`; `push` не выполнялся.

Пользователь поручил добавить ранний сигнал о планируемой работе с файлами и расширить постоянную кнопку `Свой вариант`. На текущем этапе модели должны видеть только карточки файлов и не получать содержимое или прямой файловый доступ.

Codex создал активное ТЗ и реализовал `0.0.65-dev`: программный нулевой вопрос, общий файловый паспорт сессии, карточки у ядра и исполнителя, добавление файлов через `Свой вариант`, безопасный prompt-паспорт без абсолютных путей и сохранение в долговечном архиве. Добавление или удаление файла не считается ответом и не расходует бюджет ядра; активная модель получает отдельное событие переоценки контекста. Старые сессии остаются совместимыми.

Внутренние проверки прошли: Release без предупреждений, Debug/Release tests `119/119`, format, scanner `257` файлов, локализации `551/551`, `git diff --check`. Модели не скачивались, UI автоматически не управлялся, установщик не пересобирался. ТЗ остаётся активным до ручного теста пользователя.

Ручной тест показал, что исполнитель честно видит только карточки файлов и после добавления нового JPG уточняет его назначение. Пользователь предложил учитывать сценарий, когда файл является не частью задачи, а отдельным примером. Prompt ядра и исполнителя дополнен четырьмя ролями файла и обязательными готовыми кнопками: основная задача, пример/эталон, поясняющий материал, не учитывать. Ручной ввод используется только как запасной вариант. Повторные проверки `119/119` прошли.

Пользователь подтвердил, что файловый паспорт и уточнение роли дополнительного файла работают корректно. ТЗ принято, переносится в архив, активных ТЗ не остаётся. Закрытие выполняется по штатному правилу с финальными проверками, локальным коммитом и публикацией в GitHub; пользовательское изображение исключается из коммита.

Закрывающие проверки прошли: restore, обе сборки без предупреждений, Debug/Release tests `119/119`, format, scanner `257` файлов, локализации `551/551`, `git diff --check` и проверка NuGet без известных уязвимостей. Установщик не пересобирался, модели не скачивались, UI автоматически не управлялся.

Основной коммит закрытия `121908a` успешно отправлен в `origin/main` (`7db6089..121908a`); накопленные локальные изменения опубликованы. Пользовательское изображение осталось незатронутым и незастейдженным.

## 2026-07-24 — подготовка каталога компонентов

Пользователь согласовал идею обязательного файлового минимума в основной
программе и отдельного разворачиваемого каталога дополнительных возможностей в
настройках. Из настроек в будущем можно будет полностью скачать выбранные
компоненты или весь доступный комплект после подтверждения.

Создано активное ТЗ
`ТЗ\2026-07-24_каталог_компонентов_и_загрузка_возможностей.md`. В нём отдельно
перечислены встроенные библиотеки и загружаемые runtimes, описаны capability-
контракты ядра и исполнителя, общий план загрузки модели с зависимостями,
безопасность, восстановление загрузок и критерии готовности.

Прямые ссылки проверены по HTTP-заголовкам и редиректам с фиксацией доступности
и размера. Содержимое архивов и установщиков не скачивалось, временные файлы не
создавались. Код программы, зависимости и установщик не изменялись.

Пользователь уточнил, что просмотрщики должны быть исключительно программной
частью и никогда не должны учитываться LLM. ТЗ дополнено отдельной секцией
`Просмотр файлов`, внутренним приоритетом открытия, системным fallback Windows и
явным запретом передавать сведения о просмотрщиках ядру или исполнителю.

Проверены ссылки WebView2, PDF.js, EPUB.js, LibVLCSharp/LibVLC, OpenSeadragon,
Babylon.js и AvalonEdit. Все 10 адресов доступны; тела файлов не загружались.
Office-просмотр использует уже предусмотренную конвертацию LibreOffice во
временный PDF, поэтому второй экземпляр LibreOffice не нужен.

Пользователь предложил сделать просмотр самостоятельной функцией AI HUB. В ТЗ
добавлена постоянная кнопка документа с лупой рядом с настройками. Она доступна
на всех страницах вне Matrix-блокировки, выбирает локальный файл и открывает его
в отдельном немодальном viewer-окне.

Такой просмотр полностью отделён от ИИ: файл не добавляется в паспорт сессии,
не передаётся модели и не изменяет память или дерево. Функция одновременно
служит простым ручным способом тестировать подключённые viewer-движки.

Пользователь поручил приступить к реализации активного ТЗ. Codex повысил версию
до `0.0.66-dev`, подключил обязательные файловые библиотеки, создал доверенный
менеджер processing/viewer-компонентов, настройки предварительной загрузки и
глобальное read-only окно просмотра файлов.

Ядро и исполнитель видят только абстрактные проверенные capabilities.
Viewer-компоненты полностью исключены из AI-контуров. Перед запуском исполнителя
программа строит план известных зависимостей; исполнитель также может
структурированно запросить недостающую возможность и продолжить ту же сессию.

Внутренние проверки прошли: Debug/Release без предупреждений, тесты `124/124`,
локализации `618/618`, scanner `269` файлов, NuGet без известных уязвимостей и
чистый `git diff --check`. Тяжёлые загрузки, управление UI и пересборка
установщика не выполнялись. Активное ТЗ ожидает ручной тест пользователя.

Пользователь вручную проверил загрузку viewer-компонента и воспроизведение видео
внутри AI HUB. Функция работает, но изменение одной надписи на «Установлено»
оказалось недостаточно заметным. На карточки обоих каталогов добавлена локальная
шкала загрузки с процентом; она следует за конкретным компонентом и его
зависимостями. Внутренние проверки снова прошли, приложение автоматически не
запускалось.

Следующий ручной тест показал архитектурную ошибку: при задаче повышения
разрешения видео каталог нашёл специализированную video-модель, но программа
выдала установленную текстовую Qwen как будто она сама умеет редактировать
видео. Пользователь уточнил, что исправление должно быть универсальным:
`Режим неопределённости` остаётся песочницей без целевой предметной адаптации.

В `0.0.67-dev` добавлен общий runtime-паспорт LLM-координатора и программная
проверка исполнимого комплекта. Теперь тема задачи, роль координатора,
specialist-pipeline, runtime, формат и processing-компоненты учитываются
раздельно. Неизвестная возможность не подменяется похожей, а остаётся
неразрешённой; pipeline без адаптера не показывается как LLM-исполнитель.

Исправлена потеря capability inventory с пустыми путями. Карточки двух
кандидатов теперь получают технические преимущества и ограничения от
программы, а не из свободных утверждений ядра. Произвольный ответ закрывает
текущее измерение capability profile и не допускает молчаливого повтора того же
вопроса. Финальный контракт ядра сокращён до трёх ID; неиспользуемые свободные
assessment-поля удалены.

Внутренние проверки прошли: обе сборки без предупреждений, Debug/Release tests
`134/134`, format, локализации `626/626`, UTF-8 scanner `271` файл и
`git diff --check`. Модели и тяжёлые компоненты не скачивались, UI автоматически
не управлялся, установщик не пересобирался. Активное ТЗ ожидает ручной
сквозной тест пользователя.

Повторный тест с задачей создания песни по MP3-референсу выявил потерю операции:
профиль содержал `audio_generation`, но программа требовала только
`read.audio`. Пользователь подтвердил необходимость общего исправления.

В `0.0.68-dev` модальность и операция нормализуются независимо для изображений,
аудио и видео. Тестовая задача теперь требует одновременно `read.audio` и
`generate.audio`. Координаторы можно сравнить, но при отсутствии утверждённого
provider для генерации карточка помечает комплект неполным, а запуск
блокируется.

Внутренние проверки прошли: Debug/Release без предупреждений, `139/139` тестов,
format, локализации `631/631`, UTF-8 scanner `271` файл и `git diff --check`.
Загрузки моделей и тяжёлых компонентов, управление UI, сборка установщика,
commit и push не выполнялись. ТЗ остаётся активным до ручной проверки.

Повторный ручной тест изображения подтвердил универсальную схему:
текстовая LLM показана координатором, чтение изображения и его редактирование
учитываются отдельными возможностями. Пользователь решил, что дальнейшее
предметное разложение замены фона уже выходит за разумные границы общей
технологической песочницы.

Сценарий честно переименован из `Режима неопределенности` в `Песочницу`
(`Sandbox` в английской локализации). Описания прямо отмечают
экспериментальный характер и отсутствие гарантии полного выполнения любой
задачи. Внутренний ID `Uncertainty`, журналы и архивы оставлены совместимыми.
ТЗ каталога компонентов принято и закрыто в `0.0.69-dev`. Debug/Release
сборки прошли без предупреждений, Debug/Release tests `139/139`, format,
локализации `631/631`, UTF-8 scanner `271` файл и `git diff --check` чистые.
Тяжёлые загрузки, управление UI и сборка установщика не выполнялись.

Пользователь поставил следующую задачу: дать исполнителям реальные инструменты
и научить ими пользоваться. Создано отдельное ТЗ безопасного первого контура.

В `0.0.70-dev` к существующему structured tool-calling подключены
`session_files_list`, `session_file_inspect` и `session_file_read`.
Исполнитель может перечислить только файлы активной сессии, проверить их и
читать поддерживаемое содержимое ограниченными фрагментами по внутреннему ID.
Абсолютные пути, произвольная файловая система, shell и запись модели не
доступны.

Поддержано чтение основных текстовых и офисных форматов, PDF, таблиц, почты,
EPUB, списка архива и read-only схемы SQLite. Для изображений, аудио и видео
пока честно доступны только технические метаданные. Prompt требует проверять
файл инструментом, не выдумывать просмотр медиа и после чтения продолжать
реальную задачу.

Debug/Release собираются без предупреждений, в обеих конфигурациях пройдено
`146/146` тестов, format и UTF-8 scanner чистые. Модели и компоненты не
скачивались, UI автоматически не управлялся, установщик не пересобирался.
Активное ТЗ ожидает ручного теста пользователя.

Пользователь предложил визуально отличать варианты, которыми исполнитель
просит разрешить действие: такие кнопки должны быть зелёными и мягко
пульсировать. Анализ последней сессии также показал, что исполнитель вообще не
вызвал файловые инструменты и выдумал структуру JSON.

В `0.0.71-dev` варианты ответа стали структурированными. Обычный ответ, отказ
и подтверждение безопасного файлового действия различаются программой;
семантические дубли удаляются. Зелёная кнопка теперь является настоящей
командой: её нажатие принудительно вызывает указанный tool, а не отправляет
модели декоративное «да».

Tool-calling отделён от строгой JSON grammar: инструменты работают в основном
запросе, а структура обычного ответа чинится отдельным запросом без tools.
Успешные вызовы сохраняются в checkpoint; результат по прямо указанному
текстовому файлу нельзя создать до его реального чтения. DOCX получил
обязательные свойства секции.

Debug/Release build прошли без предупреждений, в обеих конфигурациях пройдено
`147/147` тестов, format и UTF-8 scanner чистые. UI автоматически не
управлялся, модели и установщик не запускались. Активное ТЗ ожидает повторного
ручного теста.

Пользователь предложил дополнить строгие capability-ID короткими понятными
описаниями: фиксированные компоненты описывает программа, установленный
Qwen 27B получает ручной паспорт, а будущие модели должно описывать ядро после
скачивания.

В `0.0.72-dev` все фиксированные компоненты получили краткие русские и
английские технические паспорта. Они видны в настройках и, только для
исполнительных компонентов, передаются ядру вместе с машинными возможностями.
Просмотрщики по-прежнему полностью скрыты от LLM.

`sm54/Qwen3.6-27B-Q4_K_M-GGUF` получил встроенное описание
LLM-координатора. Для будущих проверенных моделей сохраняется состояние
`pending`; после завершения исполнительной сессии ядро формирует двуязычное
описание из фактов манифеста. Ошибка описания не отменяет установку модели.
Манифест записывается атомарно и сохраняет паспорт при смене runtime-статуса.

Debug/Release собираются без предупреждений, в обеих конфигурациях пройдено
`151/151` тестов, format, UTF-8 scanner (`281` файл) и `git diff --check`
чистые. Программа и модели не запускались, ничего не скачивалось, установщик
не пересобирался. ТЗ ожидает ручной проверки.

Пользователь и Codex пришли к архитектуре возможностей, похожей на безопасный
плагинный контур: LLM называет требуемый результат, а программа отдельно
сопоставляет пакет, доверенный адаптер, tool schema, паспорт и policy. Одной
задаче могут одновременно понадобиться несколько инструментов; исполнитель
может запросить новый комплект в любой момент.

В `0.0.73-dev` добавлен список запросов возможностей с альтернативами,
программный резолвер комплектов и реестр реально вызываемых адаптеров.
Установленный или найденный пакет без адаптера больше не считается рабочим
инструментом и не предлагается к бессмысленной загрузке. Неизвестный capability
может перейти в подтверждаемый внешний поиск, но найденный код не запускается
автоматически.

Фиксированное число технических попыток заменено временным бюджетом ядра:
30–180 секунд, по умолчанию 90. Он ограничивает поиск, repair и технические
повторы, но не обычную генерацию и не загрузку. Повтор одинакового действия
останавливается раньше как топтание на месте. Настройка локализована на русском
и английском.

Debug/Release build прошли без предупреждений, Debug/Release tests `156/156`,
format, локализации `640/640`, UTF-8 scanner (`287` файлов) и
`git diff --check` чистые. UI и модели не запускались, тяжёлые компоненты не
скачивались, установщик не пересобирался. ТЗ оставлено активным до ручной
проверки пользователя.

Последний ручной тест показал следующий разрыв: ядро корректно понимало задачу
по изображению и предлагало LLM-координаторы, но только после выбора выяснялось,
что возможности `read.image_pixels` нет в исполнимом комплекте. Пользователь
подтвердил исправление на общем уровне, без правила под одну задачу.

В `0.0.74-dev` добавлен предварительный `ExecutionRoute`. Он строится по
capability profile и конкретным файлам до выбора координатора, различает
декодирование формата, смысловой анализ и целевое действие. WEBP теперь требует
расширенный декодер, а интерпретация изображения — отдельный semantic-модуль;
текстовая LLM не считается таким модулем автоматически.

Маршрут показывается в финальной карточке и повторно проверяется до загрузки
модели. Заблокированный маршрут оставляет видимую кнопку `Проверить маршрут`
и объясняет недостающие узлы. Debug/Release собираются без предупреждений,
пройдено `160/160` тестов, локализации `656/656`, format и UTF-8 scanner
(`289` файлов) чистые. Приложение не запускалось, модели и компоненты не
скачивались, установщик не пересобирался.

Пользователь уточнил смысл Песочницы. Это не универсальный готовый сценарий, а
интеграционный полигон, где все будущие возможности AI HUB собираются вместе,
проверяются на совместимость и доводятся до состояния, пригодного для переноса
в профильные сценарии.

Главный критерий Песочницы: даже из плохо сформулированной задачи получить
реальный результат нужного вида, пусть низкого качества. Неполный лучший
маршрут должен переходить к упрощённому, пользовательскому или аварийному
рецепту, а не останавливаться на диагностике.

Подбирать нужно полный исполнительный комплект, а не одну LLM. В комплект
могут входить координатор, несколько профильных нечатаемых моделей,
детерминированные инструменты, runtime, адаптеры и экспорт. Ядро переводит
намерение пользователя, а адаптер выполняет техническое преобразование входов
и выходов.

Создано новое активное ТЗ
`2026-07-25_песочница_сквозные_комплекты_и_гарантированный_результат.md`.
Три прежних активных ТЗ условно выполнены и перенесены в архив: их работающие
части сохраняются, а жёсткая блокировка неполного маршрута отменяется новым
направлением. В корне остался один активный ТЗ; `git diff --check` и UTF-8
scanner (`290` файлов) прошли. По просьбе пользователя публикация пока не
выполнялась.

Активное ТЗ дополнено конечным каталогом рабочих паттернов только для
Песочницы. Это одновременно подсказка ядру и тест механики классификации:
перед поиском исполнителей полный каталог без программного предфильтра
передаётся в свежий контекст ядра, после чего ядро выбирает один или несколько
pattern-ID, ставит условные проценты и кратко объясняет решение. Код проверяет
ID, проценты и дубли, сохраняет выбор в карточке, checkpoint и JSONL, а дальше
передаёт только выбранные паттерны и рецепты.

Начальный список содержит 25 направлений для документов, таблиц, изображений,
аудио, видео, презентаций, файлов, переписки, web-исследования и кода, включая
восстановление старых медиа и `other.custom`. Паттерны можно сочетать; названия
конкретных моделей в них не фиксируются.

Для загрузок разделены `AcquisitionPlan` с одним общим подтверждением этапа и
программный `InstallationManifest` с подтверждёнными состояниями компонентов.
Будущие профильные сценарии не получают весь широкий каталог автоматически,
но могут использовать проверенный механизм со своими узкими наборами.
Создан backup `H:\AI_HUB\Backups\20260725_0442_sandbox_work_patterns_spec`.
Код и версия не менялись, публикация не выполнялась.

Из-за приближения контекстного лимита пользователь попросил опубликовать всё
накопленное состояние вместе с активным ТЗ. В публикацию включается и
изображение `Данные_для_внедрения\Фото\I want to believe llm.png`, которое
пользователь определил как задел для будущего элемента.

Перед commit/push пройдены restore, Debug/Release build без предупреждений,
Debug/Release tests `160/160`, format, локализации `656/656`, UTF-8 scanner
`290` файлов и `git diff --check`. Приложение и модели не запускались,
компоненты не скачивались, установщик не пересобирался. Активное ТЗ остаётся
активным и публикуется как план следующей реализации.

После обновления лимитов реализовано активное ТЗ сквозных исполнительных
комплектов Песочницы в версии `0.0.75-dev`. Ядро сначала сверяется с полным
каталогом из 25 рабочих паттернов, после чего программа строит полный комплект:
контракт артефакта, координатор, профильные модели, инструменты, runtime,
адаптеры, рецепты, план получения и манифест установки.

Кандидаты получают детерминированный процент соответствия и объяснение.
Пользователь явно выбирает улучшение комплекта или работу доступными
средствами. Добавлены реальные адаптеры чтения пикселей и Whisper-транскрибации,
ограниченный недоверенный web-поиск неизвестных компонентов после согласия,
материализация физического артефакта и его программная проверка.

Приложение, модели и тяжёлые компоненты во внутренних тестах не запускались и
не скачивались; установщик не пересобирался. Активное ТЗ ожидает
пользовательского теста, commit и push пока не выполняются.

Финальная внутренняя проверка: Debug/Release build без предупреждений и ошибок,
Debug/Release tests `174/174`, format без изменений, локализации `668/668`,
UTF-8 scanner `305` файлов, `git diff --check` без содержательных ошибок.

После пользовательских тестов выяснилось, что оценка координатора и готовность
полного маршрута пока смешиваются. Инструмент чтения пикселей действительно
вызывался, но результат оставался только техническим: размеры, DPI, средний
цвет и яркость без семантического понимания изображения. При этом установленная
LLM получила `90%`, хотя фактическое покрытие задачи было значительно ниже.

Согласовано продолжение архитектуры: во время опроса ядро ведёт динамическую
карту возможных действий, затем программа сопоставляет каждое действие с
реальными моделями, инструментами, runtime и адаптерами. Установленный маршрут
может быть частичным и честно это показывать. Основной скачиваемый маршрут
должен покрывать `100%` обязательных действий и иметь небольшой запас.

Исполнитель должен получать не фиксированный пульт из нескольких команд, а
виртуальную рабочую среду сессии с файлами, временными артефактами, паспортами,
рецептами и возможностью связывать несколько инструментов. Ядро может служить
переводчиком для профильных моделей, которые не умеют читать и писать.

Защита Песочницы остаётся минимальной и преимущественно промтовой: соблюдать
логическую последовательность, отделять факты от предположений и не уходить в
несвязанное направление по собственной инициативе. Пользователь может
спонтанно изменить цель, после чего маршрут перестраивается.

Создано активное продолжение ТЗ
`2026-07-27_песочница_продолжение_динамическая_рабочая_среда.md`. Родительский
файл сохраняет реализацию `0.0.75-dev`. Код и версия не менялись, публикация не
выполнялась.

Дополнительно согласована адаптивная роль исполнителя. После построения карты
действий и предварительного маршрута ядро должно само сформировать короткую
профессиональную установку: кто исполнитель, в чём его специализация, какова
текущая цель, какие данные и инструменты доступны и какой результат ожидается.
Например: специалист по обработке фото, OCR, созданию музыки заданного жанра
или анализу кода.

Роль не является декоративным персонажем и не должна приписывать отсутствующие
возможности. При сложной задаче задаются ведущая профессия и дополнительные
специализации. После смены цели роль пересобирается, а при сжатии или
восстановлении сессии остаётся закреплённой в контексте.

Последний тест выявил уже не ошибку понимания исполнителя, а отдельный разрыв
оркестрации. Исполнитель правильно понял, что для анализа изображения требуется
OCR/Computer Vision, и сформировал `request_capability` для
`extract.image_ocr`, но программа запрещала такое действие до подтверждения
карточки задачи. Contract repair затем превращал системный запрос в обычный
вопрос пользователю.

В `0.0.76-dev` запрос возможности разрешён на любом этапе, если без него нельзя
понять входной файл или продолжить задачу. Это внутренний переход AI HUB, а не
вариант ответа. После разрешения, отказа или загрузки возможности исполнитель
возвращается в тот же этап с сохранённым контекстом.

Строгая JSON-схема восстановления облегчена: из неё убраны дублирующие
legacy-поля и дорогие для grammar ограничения `maxLength`; реальные пределы
по-прежнему контролирует parser. Добавлены диагностика отклонения политикой
этапа и регрессионные тесты.

Внутренние проверки: `178/178` Debug/Release tests, чистая Release-сборка,
формат затронутых файлов, локализации `668/668`, UTF-8 scanner `307` файлов и
`git diff --check`. Приложение и модели не запускались, компоненты не
скачивались, установщик не пересобирался. Commit/push не выполнялись; ТЗ
ожидает пользовательский сквозной тест.

После следующих тестов уточнён главный принцип Песочницы: LLM должна свободно
собирать не только координатора, но весь исполнительный комплект. Код отвечает
за проверяемые факты, совместимость, адаптеры, запуск и подтверждение загрузки,
но не должен заранее выбирать смысловой маршрут вместо ядра.

В `0.0.77-dev` ядро формирует `executionPlan` с обязательными и
дополнительными возможностями, желаемыми компонентами, ролью исполнителя и
объяснением сочетания. Установленный координатор разрешено показывать одним
вариантом и использовать с честным неполным маршрутом. Скачиваемый вариант
отклоняется, если комплект не закрывает все обязательные действия рабочими
адаптерами.

Поиск моделей и внешних компонентов не требует отдельного разрешения.
Пользователь подтверждает только скачивание. Отказ от него больше не запускает
скрытый fallback. Финальный выбор ядра сохраняет web-инструменты, а паспорта
компонентов передают ему простое техническое описание реальных возможностей.

Интерфейс поддерживает один или два честных кандидата. Добавлены регрессионные
тесты полного скачиваемого комплекта и одиночного установленного кандидата.
Приложение и модели не запускались, компоненты не скачивались, установщик не
собирался, commit/push не выполнялись. Restore, Debug/Release build,
Debug/Release tests `182/182`, формат затронутых файлов, локализации `668/668`,
UTF-8 scanner `328` файлов и `git diff --check` прошли.

В `0.0.78-dev` устранён следующий разрыв: известная недостающая возможность
больше не остаётся одной надписью «не хватает модуля». Для OCR и технической
работы с изображениями появились реальные адаптеры Tesseract/ImageMagick,
поэтому ядро может предложить их загрузку единым подтверждаемым планом.

До подтверждения выполняется только подготовка: выбор пакета, зависимостей,
источника и размера. После установки маршрут пересчитывается и новые команды
попадают исполнителю. Семантическое распознавание объектов не имитируется:
пока для него нет проверенного профильного модуля, оно остаётся неизвестной
возможностью, а сессия может продолжить доступными средствами.

Backup:
`H:\AI_HUB\_backups\20260727_missing_capability_acquisition_preflight`.
Приложение, модели и установщики не запускались, компоненты не скачивались.
Финальные проверки `0.0.78-dev`: restore успешно; Debug/Release build без
ошибок и предупреждений; Debug/Release tests `186/186`; формат обоих проектов
чистый; локализации `668/668`; UTF-8 scanner `340` файлов; `git diff --check`
без содержательных ошибок. Commit/push не выполнялись.

В `0.0.79-dev` исправлен общий класс ложных результатов Песочницы: исправный
файл больше не считается доказательством выполненной предметной задачи.
Исполнительная сессия строит универсальный граф действий и принимает факт
выполнения только через квитанции реальных инструментов с компонентами,
хешами, подтверждёнными областями и ограничениями.

Перед итогом модель получает отдельный программный пакет доказательств. Если
обязательное действие не подтверждено, создаётся честный `limited`-отчёт;
выход инструмента совместимого типа имеет приоритет перед текстом модели.
Граф и квитанции сохраняются в контрольной точке и JSONL-журнале.

Backup: `H:\AI_HUB\_backups\20260805_universal_action_evidence`. Проверки без
запуска приложения и моделей: Debug/Release build чистый, Debug/Release tests
`193/193`, формат обоих проектов, локализации `668/668`, UTF-8 scanner `356`
файлов и `git diff --check` прошли. Скачивание, установщик, commit/push не
выполнялись; ТЗ остаётся активным до пользовательского сквозного теста.

В `0.0.80-dev` завершено отделение интеллектуальной пригодности координатора
от покрытия исполнительного маршрута. Программа строит контракт результата с
обязательными действиями и проверяет каждое действие по реально вызываемым
компонентам. Техническое чтение файла не считается смысловым пониманием, а
OCR, анализ содержимого и преобразование остаются разными возможностями.

Для неизвестной обязательной возможности Песочница теперь не останавливается
на пассивном сообщении. До старта исполнителя можно запустить внешний поиск по
назначению действия, цели и рабочему паттерну. Результаты сохраняются в задаче
и сессии, но остаются только непроверенными кандидатами: ссылка не считается
установленным инструментом, не повышает покрытие и не запускается без адаптера
и отдельного решения о загрузке.

Финальные проверки: Release build без предупреждений, tests `200/200`, формат
обоих проектов, локализации `681/681`, UTF-8 scanner `388` файлов и
`git diff --check`. Приложение, модели, загрузки и установщики не запускались;
commit/push не выполнялись.

В `0.0.81-dev` устранён тупик между внешним поиском и реальной установкой
недостающей возможности. Произвольные benchmark, notebook, dataset, статья или
GitHub-репозиторий теперь остаются справочными результатами и не открываются
браузером под видом компонента.

Для `analyze.image.semantic` добавлен первый полный доверенный рецепт:
SmolVLM2 2.2B в GGUF, отдельный мультимодальный проектор, закреплённые URL и
SHA-256, адаптер `adapter.image.semantic` и настоящий инструмент
`session_image_describe`. Инструмент передаёт изображение скрытому локальному
`llama-server` и возвращает фактическое семантическое описание, не подменяя
его OCR или статистикой пикселей.

Backup: `H:\AI_HUB\_backups\20260806_vision_internal_recipe`. Debug/Release
build без ошибок и предупреждений, Debug/Release tests `203/203`, формат обоих
проектов, локализации `681/681`, UTF-8 scanner `411` файлов и
`git diff --check` прошли. Приложение и модели не запускались, тяжёлые
компоненты не скачивались; сквозной запуск оставлен пользователю. Публикация не
выполнялась.

В сессии № 3 повторно разобрана первая реальная vision-сессия `0.0.81-dev`
по JSONL-журналам, контрольной точке, двум DOCX, исходному изображению и
затрагиваемому коду. Подтверждено, что SmolVLM2 успешно описал изображение, а
исполнитель создал содержательные русские фрагменты. Итог сломался позже:
квитанция получила несовместимый тип `file_inspection`, `no_ocr` ошибочно
включил обязательный OCR, repair loop повторял ответ без нового фрагмента, а
аварийный сборщик отбросил накопленную работу и вставил сырой английский JSON.

После установленного SmolVLM2 диагностические Release tests дают `201/203`,
потому что два теста читают живое состояние компонентов. В активное ТЗ
добавлен раздел 28 с обязательными исправлениями и регрессиями для
`0.0.82-dev`. Backup:
`H:\AI_HUB\_backups\20260822_session3_posttest_analysis_spec`. Код и версия
не менялись, приложение и модели не запускались, установщик не пересобирался,
commit/push не выполнялись.

После разрешения пользователя выполнена реализация раздела 28 активного ТЗ в
`0.0.82-dev`. Квитанция vision теперь связывается с конкретным действием и
файлом до проверки типа, проверяется по ID и SHA-256 и подтверждает чтение
пикселей вместе с `analyze.image.semantic`. `no_ocr` больше не создаёт OCR в
outcome contract, маршруте, графе или плане загрузки.

Язык сессии явно передаётся SmolVLM2. Сырой specialist JSON отделён от
пользовательского текста; ограниченная сборка использует подтверждённую
постановку и сохранённые художественные фрагменты. Repair сообщает реальную
причину stage-policy rejection и рано останавливает одинаковые ответы. Окно
результата раздельно показывает состояние маршрута, файла, доказательств и
задачи.

Тесты изолированы от живого `Runtime/Components`: при установленном у
пользователя SmolVLM2 проходят Debug/Release `217/217`. Также прошли restore,
Release build без предупреждений, формат, локализации `699/699`, UTF-8 scanner
`451` файл и `git diff --check`. Backup:
`H:\AI_HUB\_backups\20260822_v082_evidence_result_chain`. Приложение, модели и
установщик не запускались, commit/push не выполнялись. Следующий шаг —
пользовательский сквозной тест той же задачи с изображением.

В следующем пользовательском тесте `0.0.82-dev` анализ снова не появился, но
причина была новой: файл `images.webp` успешно читался WPF и ImageMagick, а
`llama.cpp b9442` получал исходный WebP напрямую и дважды падал до ответа
SmolVLM2. Диагностика терялась из-за необработанной CPU-ошибки и уничтожения
HTTP body/stdout/stderr. Координатор также один раз исказил ID файла, а слабая
гипотеза `image.ocr:5%` ошибочно стала обязательным OCR.

После команды пользователя реализован `0.0.83-dev`: неподдерживаемые vision-
форматы преобразуются в PNG только в памяти; GPU/CPU дают единый безопасный
`semantic_vision_failed` с локальной диагностикой без base64; неверный ID
исправляется лишь при единственном изображении; слабые паттерны ниже 40% не
формируют обязательный маршрут. Исходное изображение не меняется.

Backup: `H:\AI_HUB\_backups\20260822_v083_webp_vision_runtime`. Restore,
Debug/Release build, Debug/Release tests `225/225`, format и локализации
`699/699` прошли; UTF-8 scanner проверил `3163` файла, `git diff --check`
чистый. Модель, приложение, загрузки и установщик не запускались; commit/push
не выполнялись. Следующий шаг — повторить пользовательский тест с тем же WebP.

Два следующих пользовательских vision-теста показали границу общей
Песочницы. В первом правильный WebP и готовый SmolVLM2 не дошли до tool-call:
исполнитель повторял уточнения, старое требование редактирования осталось в
графе, а ручной экспорт создал ограниченный документ без анализа. Во втором
JPEG действительно четырежды передавался vision-модели, но она противоречиво
описала чёрного кота в ведьминской сцене как лошадь, затем как белых кошек с
чайником. Технические квитанции были valid, хотя предметный ответ был ложным.

Пользователь решил не продолжать универсальную полировку без эталонов
профильных сценариев. `Песочница` принята как условно готовая Sandbox Alpha;
её развитие приостановлено до предрелизного состояния всего AI HUB. В
русском и английском описаниях сценария добавлена явная альфа-пометка и
предупреждение о неполных или неточных результатах.

Оба активных ТЗ дополнены решением о паузе, перечнем известных ограничений и
перенесены в `ТЗ\Архив`; активных ТЗ не осталось. Проектные README и ROADMAP
переведены к следующему этапу — созданию эталонных профильных сценариев.
Версия повышена до `0.0.84-dev`. Backup:
`H:\AI_HUB\_backups\20260822_sandbox_alpha_freeze_close_tz`.

Закрывающие проверки прошли: restore, Debug/Release build без предупреждений,
Debug/Release tests `225/225`, format, локализации `699/699`, UTF-8 scanner
`476` файлов и `git diff --check`. DLL сообщает `0.0.84-dev` / `0.0.84.0`.
Приложение, модель и установщик не запускались. Накопленная реализация,
`0.0.84-dev`, архивированные ТЗ и документы опубликованы в `origin/main`:
commit `eb98381` (`feat: publish Sandbox Alpha foundation`).

## 2026-08-22 — карта широкой идеи «Анализ изображений»

Пользователь уточнил, что переданный файл был быстрым наброском, а будущий
сценарий предполагается существенно шире и вариативнее. Реализация будет идти
маленькими самостоятельными шагами. В будущем нужно поддержать несколько
комплектов моделей для компьютеров разной мощности.

Создана
`ТЗ\КАРТА_ИДЕИ_2026-08-22_сценарий_анализ_изображений.md`: исходный набросок
перенесён полностью, новые решения пользователя зафиксированы отдельно, а
выводы Codex помечены как наблюдения, рекомендации, гипотезы, риски и открытые
вопросы. Карта не считается активным ТЗ; активных ТЗ по-прежнему нет, и каждая
реализация должна начинаться отдельным небольшим ТЗ.

Обновлены реестр `ТЗ\README.md` и ROADMAP. Код, интерфейс и версия
`0.0.84-dev` не менялись. Backup:
`H:\AI_HUB\_backups\20260822_image_analysis_idea_map`. Приложение, модели и
установщик не запускались; commit/push не выполнялись.

Проверено: исходный набросок сохранён в карте без расхождений; strict UTF-8
прошёл для шести затронутых файлов; scanner проверил `482` текстовых файла;
`git diff --check` чист.

## 2026-08-22 — первое активное ТЗ «Анализа изображений»

Пользователь согласовал первый маленький этап: карточка сценария над
Песочницей, встроенная страница трёх комплектов и аппаратная рекомендация.
Оценка должна учитывать одновременную работу ядра, визуального аналитика и
локализатора.

Создано активное ТЗ
`ТЗ\2026-08-22_анализ_изображений_вход_и_выбор_комплекта.md`. Предварительно:
Лёгкий использует SmolVLM2 + Florence-base, Средний — Kimi-VL +
Florence-large, Тяжёлый — Qwen3-VL-30B-A3B-Thinking + Florence-large; во всех
комплектах учитывается ядро Qwen3 8B. Комфортные профили: `16/4`, `32/16` и
`64/24` ГБ RAM/VRAM соответственно, с отдельными требованиями к CPU и диску.

Лёгкий и Тяжёлый пока кликабельны только для пояснения. Средний открывает
страницу-подтверждение, не скачивая и не запуская модели. Реализация ещё не
начата, версия остаётся `0.0.84-dev`. Backup:
`H:\AI_HUB\_backups\20260822_image_analysis_bundle_selector_spec`.
Приложение, модели и установщик не запускались; commit/push не выполнялись.

Проверено: в корне `ТЗ` одно активное задание; обязательные составы и
требования присутствуют; исходный набросок карты не изменён; UTF-8 scanner
проверил `489` текстовых файлов; `git diff --check` чист.

## 2026-08-22 — реализован первый экран «Анализа изображений»

По команде пользователя реализовано активное ТЗ выбора комплекта. Над
Песочницей появилась карточка нового сценария. Следующая страница открывается
внутри главного окна и сравнивает Лёгкий, Средний и Тяжёлый комплекты по
одновременной нагрузке ядра, визуального аналитика и локализатора.

Отдельный сервис оценивает RAM, максимальную VRAM одной GPU, логические
процессоры и свободный диск. На ПК пользователя корректно рекомендуется
Тяжёлый комплект; паспортные `23.99 ГБ` VRAM считаются номинальными `24 ГБ`
только в пределах малого допуска измерения. Тяжёлый и Лёгкий показывают
пояснение, Средний открывает честную страницу подтверждения без загрузки и
запуска моделей.

Версия повышена до `0.0.85-dev`. Добавлено `14` тестов, всего `239/239` в
Debug и Release. Сборки без предупреждений, format, локализации `746/746`,
scanner `512` файлов и `git diff --check` прошли. Внутренне проверены обе темы,
оба языка, клики и Escape; русский язык восстановлен, Debug-окно закрыто.
Установщик не собирался, commit/push не выполнялись. ТЗ остаётся активным до
визуального подтверждения пользователя.

## 2026-08-22 — первый этап принят пользователем

Пользователь подтвердил, что реализация работает ожидаемо, и объявил ТЗ
выполненным. ТЗ перенесено в `ТЗ\Архив`, активных ТЗ больше нет. Обновлены
реестр, README и ROADMAP. Создан закрывающий backup
`H:\AI_HUB\_backups\20260822_image_analysis_bundle_selector_close` с
проверкой SHA-256. Финальная проверка прошла: Debug/Release build без
предупреждений, `239/239` тестов в обеих конфигурациях, format без изменений,
локализации `746/746`, проверка UTF-8/кириллицы — `519` файлов без ошибок,
версии DLL `0.0.85-dev` / `0.0.85.0`. Реализация, карта идеи и архивированное
ТЗ опубликованы в `origin/main` commit `74b575c`
(`feat: add image analysis bundle selection`).

## 2026-08-22 — новое ТЗ общей библиотеки AI-моделей

Пользователь уточнил второй этап `Анализа изображений`. Страница Среднего
комплекта должна проверять ядро, Kimi с projector и Florence, скачивать только
недостающее, после готовности показывать `Начать работу` и позволять удалить
только профильный визуальный аналитик. Общие ядро и Florence со страницы
сценария не удаляются.

Управление моделями в настройках должно охватывать весь AI HUB: заранее
назначенные модели профильных сценариев, случайно или динамически найденные
модели `Песочницы`, модели будущих сценариев и общие вспомогательные модели.
После удаления тяжёлых файлов карточка точного артефакта остаётся в постоянной
библиотеке, поэтому модель можно скачать снова без повторения сложного поиска.

Создано активное ТЗ
`ТЗ\2026-08-22_анализ_изображений_установка_среднего_комплекта_и_библиотека_AI-моделей.md`
для будущей версии `0.0.86-dev`. Обновлены реестр ТЗ, карта идеи, ROADMAP и
общее описание проекта. Backup:
`H:\AI_HUB\_backups\20260822_image_analysis_model_library_spec`.
Программа, модели и установщик не запускались; код и версия не менялись,
commit/push не выполнялись.

Проверено: в корне `ТЗ` одно активное задание, обязательные решения найдены,
scanner проверил `527` текстовых файлов без ошибок кодировки,
`git diff --check` чист.

## 2026-08-24 — реализация ТЗ библиотеки моделей

По команде пользователя реализовано активное ТЗ в версии `0.0.86-dev`.
Добавлена долговечная общая библиотека модельных артефактов с импортом
существующих манифестов, сохранением динамических находок Песочницы, точным
повторным скачиванием, проверкой и безопасным удалением только управляемых
файлов. Карточка остаётся после удаления тяжёлых файлов.

Страница Среднего комплекта теперь проверяет ядро, Kimi с projector и общую
Florence, предлагает загрузить только недостающее, проверяет размер/SHA и
runtime, разрешает удалить со страницы сценария только Kimi и открывает после
готовности минимальную встроенную страницу выбора изображения. Реальный анализ
изображения остаётся следующим ТЗ.

Debug и Release собраны без предупреждений; в обеих конфигурациях прошли
`253/253` теста. Format clean, локализации `878/878`, DLL `0.0.86-dev` /
`0.0.86.0`, scanner `571` файл. По просьбе пользователя приложение и тяжёлые
модели не запускались, скачивание/удаление не выполнялось, установщик не
собирался, commit/push не выполнялись. ТЗ ожидает пользовательского теста и не
архивировано.

## 2026-08-24 — исправление индикации продолжающейся загрузки

Пользователь начал реальный download-тест и подтвердил, что сохранённая часть
Kimi продолжает скачиваться после закрытия и повторного открытия программы.
При этом карточка ошибочно оставалась в состоянии `Загрузка приостановлена`, а
скорость не отображалась. Причина: live-progress обновлял только нижнюю полосу,
не заменяя сохранённый snapshot карточек.

Исправлено отображение: во время активного потока показываются `Загрузка
продолжается`, `Загрузка идёт`, растущий сохранённый объём, текущая скорость и
примерное оставшееся время. Механизм скачивания и `.part` не изменялись.
Debug/Release собраны без предупреждений и ошибок, в обеих конфигурациях прошли
`253/253` теста; локализации `878/878`, scanner `571` файл. Codex программу не
запускал; установщик и commit/push не выполнялись, ожидается повторная проверка
пользователя.

## 2026-08-24 — переход на несколько соединений при скачивании моделей

Пользователь сообщил, что Kimi скачивается около десяти часов при гигабитном
интернете, закрыл приложение и разрешил реализовать предложенный
многопоточный вариант. Диагностика показала один последовательный HTTPS-поток
без явных проблем сетевого адаптера, диска или proxy.

Для управляемой библиотеки реализовано HTTP Range-скачивание с автоматическим
выбором до восьми соединений и ручными режимами 1/2/4/8 в настройках. Уже
сохранённый `.part` повторно не загружается; отдельные оставшиеся диапазоны
можно продолжить после отмены или закрытия, после чего они безопасно
собираются и проходят SHA-256. Добавлены учёт и удаление служебных сегментов,
однопоточный fallback и автотесты на временных данных.

По просьбе пользователя точечно удалён прежний тестовый `.part` Kimi размером
`3 773 973 293` байта, поэтому новый ручной тест будет честным, с 0%. AI HUB
Codex не запускал.

Финально Debug и Release собраны без предупреждений и ошибок; в обеих
конфигурациях прошли `255/255` тестов. Format clean, локализации `886/886`,
UTF-8 scanner `346` файлов, DLL `0.0.86-dev` / `0.0.86.0`, `git diff --check`
без ошибок содержимого. Установщик и commit/push не выполнялись.

## 2026-08-24 — Florence runtime и видимая проверка

После полной загрузки комплекта пользователь запустил проверку и получил
`Runtime несовместим` для Florence. По команде пользователя Codex исправил
контур и добавил визуализацию, потому что первый запуск можно было закрыть до
появления окончательного результата.

Диагностика выявила отсутствующие Torchvision/Pillow, несовместимость старого
custom code с Transformers 5.9 и ложное обязательное требование `flash_attn`
со стороны предварительного анализатора импортов Transformers 4.41. Закреплён
совместимый dev-набор версий; `flash_attn` не установлен и не нужен CPU-пути.
Оригинальные файлы модели и их SHA-256 не менялись. Реальный offline smoke-
тест Florence загрузил веса, обработал изображение и вернул `ok`; `pip check`
не обнаружил нарушенных зависимостей.

Страница комплекта теперь отдельно показывает долгую runtime-проверку:
неопределённую полосу, текущую модель, предупреждение не закрывать страницу и
финальный статус. Ошибка отображается краткой первопричиной вместо сырого
stderr. Итогово Debug/Release собраны без предупреждений, в обеих
конфигурациях прошли `257/257` тестов, локализации совпадают `893/893`, scanner
проверил `607` файлов, повторный Florence smoke вернул `ok`. AI HUB Codex не
запускал; остаётся ручная проверка пользователем.

## 2026-08-24 — ТЗ Среднего комплекта принято и закрывается

Пользователь прислал итоговые экраны: Qwen3 8B, Kimi и Florence установлены,
страница сообщает `Средний комплект готов`, а `Начать работу` открывает
предусмотренный выбор изображения. После подтверждения пользователь дал прямую
команду закрыть ТЗ. Этап принят; ТЗ перенесено в архив, активных ТЗ не
остаётся. По обязательному регламенту актуальное состояние опубликовано в
`origin/main`, commit `6d1796f`. Установщик не пересобирался.

## 2026-08-24 — установленная копия проверена пользователем

Пользователь самостоятельно собрал установщик после публикации `0.0.86-dev`,
обновил существующую установленную версию и подтвердил корректную работу
установленной копии. Это зафиксировано как успешный пользовательский smoke-test
пути обновления. Codex установщик повторно не собирал и не запускал. Запись
опубликована в `origin/main`, commit `8353fed`.

## 2026-08-24 — начато ТЗ адаптивного интерфейса

Пользователь показал различия интерфейса на 1080p и 4K: на небольшой высоте
возникала лишняя прокрутка, а на 4K карточки растягивались с большим пустым
пространством и текст выглядел мелким. Согласованы автоматическая адаптивная
компоновка, ползунок текста 90–150% и три режима старта окна: запоминание
последнего состояния, развёртывание и окно 50% рабочей области.

По команде пользователя создано активное ТЗ и реализован первый системный слой
в `0.0.87-dev`. Геометрия окна сохраняется относительно монитора с безопасным
fallback, свёрнутое состояние не восстанавливается. Ключевые страницы получили
адаптивные поля и ширину, карточки комплекта — 1/2/3 колонки без вертикального
растягивания. Debug/Release чистые, Release tests `265/265`, format clean,
локализации `907/907`, scanner `640` файлов. AI HUB по просьбе пользователя не
запускался; требуется его ручная проверка на 1080p и 4K, поэтому ТЗ не закрыто
и код не опубликован.

## 2026-08-24 — первый визуальный тест адаптивности

Пользователь показал, что на широком окне блок `Ранее начатое` уезжает вниз,
центральная карточка комплекта и нижняя навигация расходятся, а мелкие подписи
не реагируют на ползунок текста. Причина — одноразовое программное изменение
уже созданных элементов при наличии поздних шаблонов и фиксированных оболочек.

Постмасштабирование удалено. Главный экран и три страницы анализа изображений
переведены на динамические размеры каждого уровня шрифта и line-height;
сценарные карточки уравнены, лишнее вертикальное растягивание убрано, навигация
привязана к центральным карточкам. Debug/Release чистые, Release tests
`265/265`, scanner `642` файла. Программа Codex не запускалась; ожидается
повторный пользовательский тест активного ТЗ `0.0.87-dev`.

## 2026-08-24 — сохранение рабочего экрана при посещении настроек

Пользователь указал, что возврат из настроек всегда отправляет на главную
страницу и визуально теряет незавершённую работу. Навигация исправлена: перед
открытием настроек запоминается текущая страница, а кнопка `Назад` возвращает
тот же экземпляр страницы и прежнюю строку состояния без перезапуска сценария.
Debug/Release чистые, Release tests `265/265`, локализации `907/907`, scanner
`648` файлов. AI HUB не запускался, установщик не собирался, активное ТЗ и
версия `0.0.87-dev` сохранены.

## 2026-08-24 — адаптивное ТЗ принято и закрыто локально

Пользователь подтвердил выполнение ТЗ и попросил закрыть его без публикации.
ТЗ адаптивного интерфейса перенесено в архив, активных ТЗ не осталось.
Проектные документы отмечают принятую реализацию `0.0.87-dev`, включая
масштаб текста, режимы старта окна, исправленные оболочки и сохранение рабочей
страницы при возврате из настроек. Release build чистый, тесты `265/265`,
локализации `907/907`, scanner `656` файлов. Commit/push намеренно отложены по
прямому указанию пользователя; установщик не собирался и AI HUB не запускался.

## 2026-08-24 — сохранён каркас рабочего экрана анализа изображений

Пользователь попросил сохранить обсуждённый визуал и свой набросок в папке
ТЗ. Создана отдельная подтверждённая концепция, не являющаяся активным ТЗ.
В ней собраны постоянная оболочка, шаги пользователя, адаптивные зоны,
активность, контекстное изображение и результат, декоративная уверенность,
простой первый вариант галереи и единый Matrix-дождь трёх цветов без
наложения капель. Полный исходный текст и PNG сохранены отдельно и сверены по
SHA-256. Карта идеи и реестр ТЗ получили ссылки; активных ТЗ `0`. Scanner
проверил `664` файла. Код, версия, сборка и тесты не менялись; публикация не
выполнялась.

## 2026-08-24 — согласован первый литературный подсценарий

Пользователь подтвердил концептуальный маршрут первой предметной ветки и
попросил сохранить его в ТЗ. `Литературное описание одного изображения` и
будущее описание нескольких изображений зафиксированы как физически разные
подсценарии с отдельными prompt-контрактами. Для одиночной ветки сохранены
выбор и проверка файла, независимые стиль, объём, форма и художественная
свобода, ручные запросы ядру с версиями, предпросмотр и экспорт до завершения,
история и добровольная внутренняя резервная копия. Активное ТЗ не открывалось,
код и версия не менялись, публикация не выполнялась.

## 2026-08-24 — реализован первый литературный подсценарий

По команде пользователя создано активное ТЗ и реализован первый реальный
маршрут `Анализа изображений`: выбор одиночного подсценария, проверка файла,
настройки текста, последовательный Kimi → Qwen3, ролевой Matrix, версии,
ручные уточнения, предпросмотр, DOCX, отдельная история и добровольная
внутренняя копия при завершении. Сбой ядра сохраняет отчёт Kimi для повтора,
а сырой JSON не выводится пользователю. Версия `0.0.88-dev`; Debug/Release
чистые, тесты `269/269`, локализации `1010/1010`, scanner `698` файлов.
Программа по указанию пользователя не запускалась, установщик и публикация не
выполнялись. Активное ТЗ ожидает ручного сквозного теста и приёмки.

## 2026-08-24 — первый сквозной тест: техника работает, содержание нет

Пользователь протестировал реальный маршрут на изображении чёрной кошки или
пантеры у котла с зелёным дымом и свечами. Сессия, версии, DOCX, история и
добровольная внутренняя копия создались корректно. Однако Kimi пропустил котёл
и дым, смешал языки и оборвал отчёт на установленном пределе 900 токенов;
Qwen3 унаследовал слабые наблюдения и выдумал лес и магический сюжет. Ручная
команда про все объекты породила полностью одинаковую версию. Повторная
генерация не перезапускала визуальный анализ.

По скриншотам также подтверждён чёрный текст на тёмных панелях. Исправлено
наследование цвета текста рабочей страницы и контраст состояний верхних шагов.
Смысловые правки пока только занесены в активное ТЗ. Debug/Release и
`269/269` тестов чистые; AI HUB не запускался, версия остаётся `0.0.88-dev`.

Backup:
`H:\AI_HUB\_backups\20260824_image_analysis_dark_theme_and_test_analysis`.

## 2026-08-24 — рабочий экран Анализа изображений и версия 0.1.0

Пользователь подтвердил визуальный контракт первого полноценного сценария и
попросил начать реализацию. Добавлены постоянная карточка выбранного файла,
центральный перечень фактических наблюдений вместо полного текста, шкала
реальных программных событий с подсказками, полноэкранный Matrix-дождь
Песочницы с цветами ролей, а также `Новый анализ` и `На главную` после
завершения сессии. События и наблюдения сохраняются вместе с историей.

По отдельному уточнению версия повышена до `0.1.0-dev`. В правилах закреплено,
что каждый новый полноценный пользовательский сценарий повышает среднюю цифру
`0.X.Y`, после чего небольшие исправления снова считаются младшей цифрой.
Debug/Release и `270/270` тестов чистые, локализации `1028/1028`. AI HUB по
UTF-8 scanner проверил `742` файла, DLL показывает `0.1.0-dev`. AI HUB по
указанию пользователя не запускался; визуальная приёмка и закрытие активного
ТЗ остаются за пользователем. Установщик и публикация не выполнялись.

## 2026-08-24 — исправление рабочего экрана анализа изображений

Пользователь подтвердил, что Kimi должен оставаться полностью внутренним
исполнителем, правая панель должна показывать только короткое перечисление
главных деталей от ядра, а кнопки и ручное уточнение должны жить в центре.
Также согласовано физическое разделение трёх областей и отсутствие общей
прокрутки.

В `0.1.1-dev` Kimi переведён на постоянный английский внутренний prompt,
сырой отчёт исключён из пользовательского UI, ядро формирует отдельную
структурированную контрольную сводку. Слева реализовано интерактивное дерево
событий с автоматическим следованием, справа — фиксированная сводка без
скролла, в центре — версии, правка, предпросмотр, экспорт и завершение. AI HUB
не запускался; сборки и `275/275` тестов прошли, визуальную приёмку выполняет
пользователь.

## 2026-08-24 — литературный подсценарий принят и закрывается

Пользователь провёл дополнительные реальные тесты и подтвердил, что
`Литературное описание одного изображения` на текущем этапе выглядит и
работает приемлемо. Обе последние сессии завершились без технических ошибок;
рабочий экран, быстрая проверка, отдельный предпросмотр, история и внутренняя
копия работают по согласованному контракту. Ошибки распознавания на малом и
сложном изображениях оставлены для будущих эталонных тестов и небольшой
полировки. ТЗ принято в `0.1.1-dev`, переносится в архив; активных ТЗ после
закрытия нет. Создан backup
`H:\AI_HUB\_backups\20260824_close_image_analysis_literary_single_v011`.

Закрывающая техническая приёмка прошла: Debug/Release build без ошибок и
предупреждений, Debug/Release tests `275/275`, format clean, локализации
`1032/1032`, UTF-8 scanner `755` файлов, `git diff --check` чист. DLL и
корневой `VERSION` подтверждают `0.1.1-dev`. AI HUB не запускался, установщик
не собирался; пользовательские тестовые сессии, модели, backup и посторонний
черновик идеи исключены из публикации.
