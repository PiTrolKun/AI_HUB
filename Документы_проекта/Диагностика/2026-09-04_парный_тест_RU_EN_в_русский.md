# Парный серверный тест: RU против EN → RU

04.09.2026, Heavy Qwen2.5-Omni-3B BF16, штатный Transformers worker 0.1.26.
По прямому запросу пользователя выполнены ровно две генерации, без Kokoro.

## Методика

- Одна картинка `C:/Users/Admin/Pictures/Без названия.jpg`, 251×201,
  SHA-256 из паспорта: 189c70b4631fb67808c22b43ce412dceb7d69184bb59d11ea6fe528aa91b78c6.
- Требования взяты из последних RU/EN тестов этой картинки:
  free / fairy_tale / detailed (7–10 абзацев) / with_title, пожеланий нет.
- Каждый запрос самостоятельный: картинка + одно user-сообщение, без
  предыдущего анализа. Вступление про продолжение внутренней беседы заменено
  симметричным указанием описать картинку и проверить утверждения по ней.
  Требования к JSON и фактической основе сохранены.
- RU — русский запрос/ответ; EN — английские инструкции, language: Russian,
  дополнительное `Translate the final answer into Russian. Output only the
  Russian version; keep the JSON field names unchanged.`
- Один прогрев, один worker; порядок RU, затем EN→RU. История между запросами
  не передаётся, веса остаются загруженными, штатная очистка кэша работает.
  Декодирование не менялось, seed специально не фиксировался.
- Лимит ожидания каждого запроса 300 с; он не сработал. AI HUB и другие
  model-worker процессы перед запуском не обнаружены, закрывать их не пришлось.

## Результаты

| Показатель | RU | EN → RU |
|---|---:|---:|
| Внутреннее время обработки | 14,054 с | 15,015 с |
| Включая очистку кэша | 14,229 с | 15,200 с |
| Первый токен | 0,584 с | 0,282 с |
| Входные токены | 475 | 350 |
| Выходные токены | 388 | 448 |
| Абзацы | 5 | 5 |
| Пункты сводки | 8 | 5 |
| Завершение | EOS | EOS |
| Reserved VRAM после очистки | 8,84 GiB | 8,84 GiB |

Оба ответа обёрнуты в Markdown ```json вопреки инструкции. JSON внутри
синтаксически корректен, проверен ConvertFrom-Json; приложение/его C# parser
в этом тесте не запускались. Ответы сохранены без переписывания содержания.

RU: вставки `畸形`, «отверстие в центре глаза», футболка вместо майки;
добавлен абзац о возможном использовании изображения в рекламе/на сайте.
Сказочный стиль почти отсутствует, выбранный объём не выполнен.

EN→RU: вставка `某种`, выдуманные глаза из картона/бумаги, неудачная фраза,
где глаз «высовывает язык». Повторяется мысль о забавном визуальном эффекте;
объём также не выполнен. Оба заголовка одинаковы:
«Смешной персонаж с открытым глазом».

## Вывод

В этой паре перевод инструкции на английский НЕ улучшил русский результат.
Предыдущий успех полностью английских запросов/ответов нельзя автоматически
переносить на EN-инструкцию с RU-ответом. Проблема может зависеть от языка
генерации, формулировки, нагрузки инструкций и способа построения сценария;
двух запросов недостаточно, чтобы установить общую причину. Этот тест также
не сравнивает одношаговую и двухшаговую схемы на одинаковом наборе повторов.
Автоматическая смена языка промптов в продукте на этом основании не внедряется.

## Артефакты и проверки

- `Тесты/Omni_ru_en_prompt_pair_20260904/prompts.json` — точные инструкции;
- там же `ru.json`, `en_to_ru.json` — полные ответы и метрики;
- `protocol.jsonl`, `stderr.log`, `warmup.json`, `shutdown.json` — протокол;
- `Инструменты/tests/run_omni_ru_en_prompt_pair.py` — повторяемый стенд.

Штатный worker завершён командой shutdown, отсутствие тестовых процессов
проверено. Python-синтаксис стенда проверен py_compile, документация diff --check.
Продуктовый код, модели, зависимости и версия не менялись; сборка приложения,
установщик, commit/push не выполнялись. Backup истории:
`_backups/20260904_ru_en_prompt_pair`.
