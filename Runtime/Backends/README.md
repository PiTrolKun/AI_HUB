# Runtime / Backends

Будущая папка для backend-ов или их управляемых runtime-файлов.

Backends нельзя скачивать, устанавливать или запускать без понятного подтверждения пользователя.

Все добавленные backends должны фиксироваться в `Документы_проекта/REESTR.md`.
